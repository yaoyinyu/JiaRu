# 2026-07-06

## Debug sample active-learning warning telemetry

- 为 `prioritize-debug-samples` 报告补充 `warningBreakdown`，用于统计调试样本中的模型运行时、ONNX、fallback 等 warning 分布。
- 将 `warningBreakdown` 透传到 active-learning release trace draft 与 handoff，方便后续不训练真实模型时也能定位模型链路风险。
- 修复 failure summary 测试中残留的乱码断言，保留对模型运行时 warning 归类为 `model` 的稳定验证。
- 验证：`node --no-warnings --experimental-strip-types --test tests/prioritize-debug-samples.test.ts tests/run-debug-sample-active-learning-pipeline.test.ts tests/summarize-failure-cases.test.ts` 通过；`npm.cmd run audit:encoding` 通过。
- 全量验证：
pm.cmd test 通过（253/253）；
pm.cmd run lint 通过；
pm.cmd run build 通过；git diff --check 无空白错误。
## Active-learning warning telemetry release trace contract

- 将 `warningBreakdown` 补入 `build-release-trace-index.ts` 的 active-learning `prioritySummary` 类型契约。
- 在 release trace index 与 training release governance 测试中验证 warning 分布不会从 active-learning draft / handoff 链路丢失。
- 更新 active-learning trace handoff 与 debug sample pipeline 文档，说明 `warningBreakdown` 的传递链路和用途。
- 验证：`node --no-warnings --experimental-strip-types --test tests/build-release-trace-index.test.ts tests/run-training-release-pipeline.test.ts tests/run-debug-sample-active-learning-pipeline.test.ts` 通过。
- 追加验收：`npm.cmd run audit:encoding` 通过；`npm.cmd test` 通过（253/253）；`npm.cmd run lint` 通过；`npm.cmd run build` 通过。
## Release history active-learning summary

- `build-release-history-manifest.ts` 增加 active-learning 汇总，跨版本统计回流样本数量、优先级分布、backend 分布和 warningBreakdown。
- 每个 history entry 保留 `activeLearningImportedSampleCount`、`activeLearningWarningBreakdown` 和 `activeLearningReadinessTotals`，不用打开单个 release trace 也能复盘回流数据影响。
- 更新 release governance 文档，说明 release-history 中新增的 active-learning 摘要字段。
- 验证：`node --no-warnings --experimental-strip-types --test tests/build-release-history-manifest.test.ts tests/register-release-trace-index.test.ts tests/promote-approved-release.test.ts tests/run-release-governance-pipeline.test.ts` 通过。
- 追加验收：`npm.cmd run audit:encoding` 通过；`npm.cmd test` 通过（253/253）；`npm.cmd run lint` 通过；`npm.cmd run build` 通过。
## Training release active-learning comparison

- `compare-training-releases.ts` 增加可选 `--baseline-trace-index` / `--candidate-trace-index`，把 release trace 中的 active-learning 回流样本、warningBreakdown、backendBreakdown 纳入 A/B 对比。
- compare summary 现在会输出 `baseline.activeLearning`、`candidate.activeLearning`、`deltas.activeLearningImportedSamples`、`deltas.activeLearningWarnings`、`deltas.activeLearningBackends`。
- 更新 training release comparison 文档，说明 active-learning trace 对比命令和输出字段。
- 验证：`node --no-warnings --experimental-strip-types --test tests/compare-training-releases.test.ts` 通过。
- 追加验收：`npm.cmd run audit:encoding` 通过；`npm.cmd test` 通过（254/254）；`npm.cmd run lint` 通过；`npm.cmd run build` 通过。
## Release decision active-learning warning review

- `build-release-decision-report.ts` 读取 compare summary 中的 active-learning trace delta。
- 当 `deltas.activeLearningWarnings` 出现正向增加时，决策进入 `manual_review`，提示人工复核模型运行时、fallback 或后处理风险；该规则不作为硬阻断。
- 决策输入新增 `activeLearningImportedSampleDelta`、`activeLearningWarningDelta`、`activeLearningWarningDeltas`、`activeLearningBackendDeltas`。
- 验证：`node --no-warnings --experimental-strip-types --test tests/build-release-decision-report.test.ts tests/compare-training-releases.test.ts` 通过。
- 追加验收：`npm.cmd run audit:encoding` 通过；`npm.cmd test` 通过（255/255）；`npm.cmd run lint` 通过；`npm.cmd run build` 通过。
## Release governance active-learning manual review default

- 为 `run-release-governance-pipeline.ts` 补充测试：当 active-learning warning delta 导致 release decision 为 `manual_review` 时，默认跳过 promotion / trace registration / rollback audit，但仍生成 release trace 供复盘。
- 该场景只有显式 `--allow-manual-review true` 时才会进入 promotion。
- 更新 release governance 文档，说明 active-learning warning manual review 的默认治理行为。
- 验证：`node --no-warnings --experimental-strip-types --test tests/run-release-governance-pipeline.test.ts tests/build-release-decision-report.test.ts` 通过。
- 追加验收：`npm.cmd run audit:encoding` 通过；`npm.cmd test` 通过（256/256）；`npm.cmd run lint` 通过；`npm.cmd run build` 通过。
## Release governance active-learning manual review allow path

- 为 `run-release-governance-pipeline.ts` 补充测试：当 active-learning warning delta 导致 `manual_review` 时，显式 `--allow-manual-review true` 可以继续 promotion、trace registration、rollback audit 和 history 登记。
- history manifest 会保留 `decisionStatus: "manual_review"`，区分人工确认放行与普通自动放行。
- 更新 release governance 文档，说明 active-learning manual_review 的人工放行路径。
- 验证：`node --no-warnings --experimental-strip-types --test tests/run-release-governance-pipeline.test.ts tests/promote-approved-release.test.ts tests/build-release-decision-report.test.ts` 通过。
- 追加验收：`npm.cmd run audit:encoding` 通过；`npm.cmd test` 通过（257/257）；`npm.cmd run lint` 通过；`npm.cmd run build` 通过。

## Training release pipeline active-learning manual review allow path

- 为 `run-training-release-pipeline.ts` 补充集成验收：当 `compare-summary.json` 的 `deltas.activeLearningWarnings` 出现正向增量时，总流水线继承 `manual_review` 决策。
- 显式 `--governance-allow-manual-review true` 时，总流水线可继续 promotion、trace registration、rollback audit 与 history 登记，并保留 `decisionStatus: "manual_review"`。
- 更新 release governance 文档，说明 training release pipeline 层的 active-learning 人工放行参数和验收重点。
- 验证：`node --no-warnings --experimental-strip-types --test tests/run-training-release-pipeline.test.ts tests/run-release-governance-pipeline.test.ts tests/build-release-decision-report.test.ts` 通过（29/29）。- 追加验收：`npm.cmd run audit:encoding` 通过；`npm.cmd test` 通过（258/258）；`npm.cmd run lint` 通过；`npm.cmd run build` 通过。
## Browser worker timeout fallback

- 为 `recognizeNailTexturesInWorker()` 增加 `workerTimeoutMs`，默认 15000ms，避免模型或 Worker 挂起时上传识别流程无限等待。
- 超时后会 terminate 当前 Worker，并通过主线程 fallback 返回结果，同时在 `warnings` 中标记 `worker_timeout_used_main_thread`。
- 更新浏览器接入验收文档，记录 timeout fallback 行为和调试时可用 `workerTimeoutMs: 0` 显式关闭超时。
- 验证：`node --no-warnings --experimental-strip-types --test tests/nail-texture-client-worker.test.ts tests/verify-browser-integration.test.ts` 通过（12/12）。- 追加验收：`npm.cmd run audit:encoding` 通过；`npm.cmd test` 通过（259/259）；`npm.cmd run lint` 通过；`npm.cmd run build` 通过。
## Browser integration worker timeout contract

- `verify-browser-integration.ts` 新增 `client_worker_times_out_to_fallback` 静态契约检查，确保 worker timeout fallback 的关键接线不会被后续改动破坏。
- 契约覆盖 `workerTimeoutMs`、`setTimeout`、Worker terminate、主线程 fallback 调用和 `worker_timeout_used_main_thread` warning 标记。
- 更新浏览器接入验收文档，说明这是 Phase 3 非阻塞行为的静态门禁，不依赖真实 ONNX 模型。
- 验证：`node --no-warnings --experimental-strip-types --test tests/verify-browser-integration.test.ts tests/nail-texture-client-worker.test.ts` 通过（12/12）。- 追加验收：`npm.cmd run audit:encoding` 通过；`npm.cmd test` 通过（259/259）；`npm.cmd run lint` 通过；`npm.cmd run build` 通过。
## Picker worker timeout policy and warning presentation

- `NailArtPicker` 显式传入 `workerTimeoutMs: NAIL_RECOGNITION_WORKER_TIMEOUT_MS`，把 UI 层识别等待预算固定为 15000ms。
- `presentRecognitionWarning()` 增加 `worker_timeout_used_main_thread` 的用户可读提示，避免显示成未知 warning code。
- `verify-browser-integration.ts` 的 `picker_uses_worker_recognition` 契约同步检查 picker 是否传入 worker timeout，防止后续重构丢掉 UI 超时策略。
- 验证：`node --no-warnings --experimental-strip-types --test tests/nail-art-picker-quality.test.ts tests/verify-browser-integration.test.ts tests/nail-texture-client-worker.test.ts` 通过（17/17）。- 追加验收：`npm.cmd run audit:encoding` 通过；`npm.cmd test` 通过（259/259）；`npm.cmd run lint` 通过；`npm.cmd run build` 通过。
## Debug sample recognition options handoff

- debug sample 记录新增 `recognitionOptions.maxCandidates` 和 `recognitionOptions.workerTimeoutMs`，用于追踪 UI 识别时实际使用的候选数与 Worker 超时预算。
- `import-debug-sample.ts` 将该配置透传到 annotation 的 `image.debug.recognitionOptions`，后续数据审计和回放可以还原识别参数。
- `worker_timeout_used_main_thread` 归类为模型运行时 warning，active-learning 优先级会把 Worker 超时回退纳入运行风险复盘。
- 目标验证：`node --no-warnings --experimental-strip-types --test tests/nail-texture-debug-sample.test.ts tests/import-debug-sample.test.ts tests/nail-texture-debug-priority.test.ts tests/prioritize-debug-samples.test.ts` 通过（15/15）。
- 全量验收：`npm.cmd run audit:encoding` 通过；`npm.cmd test` 通过（259/259）；`npm.cmd run lint` 通过；`npm.cmd run build` 通过；`git diff --check` 无空白错误。
- Git 图片检查：仓库内 `image` 无未跟踪文件；`E:\AI Project\Codex\JiaRu_image` 位于仓库外，不会参与提交。

## Worker recognition option contract

- `RecognizeNailTextureRequest` 新增 `workerTimeoutMs`，`client-worker.ts` 会把归一化后的 Worker 超时预算一起发给 Worker。
- `nail-texture-recognition.worker.ts` 将 `request.workerTimeoutMs` 透传给统一识别入口，保证 UI、client-worker、Worker 边界上的识别参数一致可查。
- `verify-browser-integration.ts` 的静态契约同步检查 `workerTimeoutMs` 是否进入请求和 Worker 识别调用，降低后续重构时丢失超时策略的风险。
- 目标验证：`node --no-warnings --experimental-strip-types --test tests/nail-texture-client-worker.test.ts tests/verify-browser-integration.test.ts` 通过（12/12）。
- 契约修正：`verify-browser-integration.ts` 同时接受 `workerTimeoutMs` 简写属性和显式 `workerTimeoutMs: workerTimeoutMs`，避免真实代码使用对象简写时被误判。
- 回归验证：失败组 `tests/build-real-model-first-run-record.test.ts tests/run-real-model-final-audit.test.ts tests/run-training-release-pipeline.test.ts tests/verify-browser-integration.test.ts` 通过（22/22）。
- 全量验收：`npm.cmd run audit:encoding` 通过；`npm.cmd test` 通过（259/259）；`npm.cmd run lint` 通过；`npm.cmd run build` 通过；`git diff --check` 无空白错误。
- 图片残留检查：仓库内 `image` 无未跟踪内容；本轮未提交、未推送。

## Duplicate candidate suppression

- `rankNailTextureCandidates()` 新增高重叠候选去重逻辑：先按调整后的质量分排序，再用保守的近似 box IoU 阈值压掉重复候选。
- 默认 `duplicateOverlapThreshold` 为 `0.55`，用于合并同一甲面上的重复模型行，同时尽量保留相邻手指/相邻甲面的独立候选。
- `postprocessNailTextureDetections()` 继承该去重能力，因此手指建议分配与从左到右排序会基于去重后的候选执行。
- 目标验证：`node --no-warnings --experimental-strip-types --test tests/nail-texture-quality.test.ts tests/nail-texture-preprocess-postprocess.test.ts tests/nail-texture-mask-pipeline.test.ts` 通过（15/15）。
- 全量验收：`npm.cmd run audit:encoding` 通过；`npm.cmd test` 通过（261/261）；`npm.cmd run lint` 通过；`npm.cmd run build` 通过；`git diff --check` 无空白错误。
- 图片残留检查：仓库内 `image` 无未跟踪内容；本轮未提交、未推送。

## Low-score debug candidates

- `RecognizeNailTexturesOptions` / Worker request 新增 `includeLowConfidenceCandidates`，用于显式打开低分候选 debug 查看模式。
- 默认行为保持不变：`score < 0.35` 仍会隐藏；debug 模式下会保留低分候选并追加 `low_score_debug_candidate` warning。
- UI warning 映射新增该提示，避免内部 code 裸露给用户或测试日志。
- 目标验证：`node --no-warnings --experimental-strip-types --test tests/nail-texture-quality.test.ts tests/nail-texture-preprocess-postprocess.test.ts tests/nail-art-picker-quality.test.ts tests/nail-texture-client-worker.test.ts` 通过（30/30）。
- 全量验收：`npm.cmd run audit:encoding` 通过；`npm.cmd test` 通过（264/264）；`npm.cmd run lint` 通过；`npm.cmd run build` 通过；`git diff --check` 无空白错误。
- 图片残留检查：仓库内 `image` 无未跟踪内容；本轮未提交、未推送。

## Debug low-score option handoff

- debug sample 的 `recognitionOptions` 新增 `includeLowConfidenceCandidates`，只在显式开启低分候选 debug 查看时记录。
- `NailTextureAnnotationDocument.image.debug.recognitionOptions` 同步保留该字段，`import-debug-sample.ts` 的 annotation 输出可复盘低分候选显示策略。
- 目标验证：`node --no-warnings --experimental-strip-types --test tests/nail-texture-debug-sample.test.ts tests/import-debug-sample.test.ts tests/nail-texture-dataset.test.ts` 通过（14/14）。
