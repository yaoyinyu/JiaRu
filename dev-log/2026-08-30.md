# 2026-08-30 开发日志

## candidate28/29结论与candidate30预注册（提交并推送）

- 开始任务前读取技术白皮书v1.1.544的§1维护规则、§3状态总表（candidate28行、困难负样本行）与§13最新变更记录，并对照当前磁盘JSON与Git确认`master`与`origin/master`同在`a420934`，工作区改动尚未提交。
- candidate28 v5正常早停于epoch 53、最佳epoch 33，权重SHA-256为`de90366f…d9b`（45,174,121字节）；部署512的val30在阈值0.50为122/144匹配、22漏检、12误检，未保持candidate21的128匹配，替换规则`replacementRuleSatisfied=false`，在val阶段否决。未运行受保护回归test100、未导出、登记或部署。
- candidate21与candidate28按预注册alpha0.02/0.05/0.08/0.10/0.15/0.20插值。alpha0.15与alpha0.20在阈值0.45完全并列（128匹配/16漏检/16误检、`f1AtIou50` 0.8889），按“同指标下选对candidate21参数扰动更小者”的保守规则锁alpha0.15为candidate29，权重SHA-256 `1524ba9f…6f02`。读取受保护回归前已固定权重、阈值与后处理。
- candidate29在已消费test100受保护回归上为519/554匹配、467完整mask、35漏甲、13重复、16额外、12无效、20图漏甲、55图可直接提取；相对candidate21改善1个完整mask、少2个额外候选和5个无效mask、多2张可直接提取图，但完整mask比例0.84296、缺甲图率0.20、加权伪实例率0.11372仍未过正式门，不得进入困难负样本发布门、导出、登记或部署，该回归不得再用于alpha、阈值、样本或后处理选择。
- candidate30预注册“甲面联合近景”增强：只从candidate28已审计train角色274张/1652 mask生成确定性近景，父图全部polygon必须各出现一次、20%上下文、裁剪面积不高于原图85%、polygon距边至少2像素、面积缩减至少15%，任何丢失/裁断/越界/缩减不足必须跳过或失败。保留原图、160张既有批准困难负样本与未增强val30，test保持空，近景继承父图来源组；从candidate29 alpha0.15初始化，继续640、AdamW 1e-5、freeze10、无mosaic、`mask_ratio=1`、`overlap_mask=false`，唯一选择证据仍是val30。
- 工程改动：`evaluate.py`新增`--plots/--no-plots`（`BooleanOptionalAction`，默认开），内存受限的正式评估可关闭Ultralytics绘图但仍保留预测标签与artifact索引，并把`plots`写入配置回显；`train-yolo-seg.py`新增`load_hand_roi_input_auditor()`，按输入报告`decision == approved_hand_roi_candidate_training_input`自动分派手部ROI审计器，其余仍走原候选审计器。新增`materialize-hand-roi-boundary-dataset.py`、`audit-hand-roi-boundary-dataset.py`与`tests/materialize-hand-roi-boundary-dataset.test.ts`。
- 验证：显式Python 3.13置于PATH首位后串行运行两个相关测试文件，`tests` 15/15通过（含新增的`--no-plots`用例）；`git diff --check`通过（仅既有LF/CRLF提示）。未运行训练、受保护回归、导出、登记或部署，产品继续HOLD。
- 文档同步：白皮书升至v1.1.545，§3状态表把candidate28改为val30否决并新增candidate29、candidate30两行，§13追加变更记录；README功能概览补candidate28否决、candidate29锁定与candidate30预注册三段，Phase 4路线图新增/更新四项。实施规范§29为本轮之前已写入的内容，本轮一并纳入提交。
- 提交推送：按用户明确要求以中文提交信息创建提交并推送至`origin/master`；推送前用GitHub API复核令牌对该仓库具备`push`权限，未使用`--amend`、`--force`，未跳过钩子。`output/`下的12MB审核接触表与源图属本地中间产物，历史上从未纳入跟踪，本轮继续排除。
