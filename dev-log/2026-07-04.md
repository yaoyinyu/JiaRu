## 2026-07-04

- Replaced the corrupted `NailArtPicker.tsx` with a clean maintainable version while keeping the external `NailAssignment` contract unchanged.
- Preserved the worker-based auto detection flow, cancel handling, region metadata, manual region editing, finger assignment, texture extraction, and debug JSON export.
- Verification passed:
  - `npx.cmd eslint src/components/NailArtPicker.tsx src/components/nail-art-picker-quality.ts src/lib/nail-texture-recognition/client-worker.ts src/lib/nail-texture-recognition/types.ts`
  - `node --no-warnings --experimental-strip-types --test tests/nail-texture-client-worker.test.ts tests/nail-texture-model-runtime.test.ts`
- Fixed remaining source-encoding/display issues in `src/app/ar-tryon/page.tsx`, `src/lib/texture.ts`, and `src/components/nail-art-picker-quality.ts`, and normalized the touched files to UTF-8 so Turbopack can build them reliably.
- Restored readable Chinese UI copy for the AR try-on page and readable Chinese review/warning messages for nail texture candidate quality.
- Verification passed:
  - `node --no-warnings --experimental-strip-types --test tests/nail-art-picker-quality.test.ts tests/nail-texture-client-worker.test.ts tests/nail-texture-model-runtime.test.ts`
  - `npx.cmd eslint src/app/ar-tryon/page.tsx src/lib/texture.ts src/components/nail-art-picker-quality.ts src/components/NailArtPicker.tsx`
  - `npm.cmd run build`
- Upgraded the old green-circle ground-truth check into a reusable fixture mechanism.
- Added `src/lib/nail-detection-fixture.ts`, a reusable `scripts/build-nail-detection-fixture.ts`, and a checked-in fixture `model/fixtures/nail-detection-reference-5188.json`.
- Updated `scripts/verify-nail-detection.ts` and `tests/nail-image-detection.test.ts` to use shared fixture-based matching instead of machine-local image paths.
- Verification passed:
  - `node --no-warnings --experimental-strip-types --test tests/nail-detection-fixture.test.ts tests/nail-image-detection.test.ts tests/batch-verify-nail-detection.test.ts`
  - `node --no-warnings --experimental-strip-types scripts/verify-nail-detection.ts model/5188.jpg_wh860.jpg --fixture model/fixtures/nail-detection-reference-5188.json --output-dir model/debug/fixture-verify --prefix fixture-check`
  - `npx.cmd eslint --no-ignore src/lib/nail-detection-fixture.ts scripts/build-nail-detection-fixture.ts scripts/verify-nail-detection.ts tests/nail-detection-fixture.test.ts tests/nail-image-detection.test.ts`
- Added batch fixture-directory verification coverage for `scripts/batch-verify-nail-detection.ts`, including automatic fixture matching by image file name or stem.
- Extended the batch report with `fixturePath`, `fixtureDir`, and `matchedFixtureCount`, and verified that the matched fixture path is also carried into the per-image debug JSON.
- Verification passed:
  - `node --no-warnings --experimental-strip-types --test tests/batch-verify-nail-detection.test.ts tests/nail-image-detection.test.ts tests/nail-detection-fixture.test.ts`
  - `npx.cmd eslint --no-ignore scripts/batch-verify-nail-detection.ts tests/batch-verify-nail-detection.test.ts`
- Manual batch verification passed on a directory containing only `5188.jpg_wh860.jpg` with `model/fixtures`: `ok=true`, `matchedFixtureCount=1`.
- Updated `scripts/batch-verify-nail-detection.ts` to automatically skip fixture-linked annotation images when `--fixture-dir` is provided, so green-circle review images are not misclassified as source inputs during batch precheck.
- Added coverage for the annotation-skip path and confirmed that a real `model + model/fixtures` batch run now succeeds with `ok=true`, `matchedFixtureCount=1`, and `skippedAnnotationCount=1`.
- Verification passed:
  - `node --no-warnings --experimental-strip-types --test tests/batch-verify-nail-detection.test.ts tests/nail-image-detection.test.ts tests/nail-detection-fixture.test.ts`
  - `npx.cmd eslint --no-ignore scripts/batch-verify-nail-detection.ts tests/batch-verify-nail-detection.test.ts`
- Made seed-batch workspaces fixture-aware: bootstrap/scaffold now create fixtures/, and generated README, audit, checklist, and workflow commands pass --fixture-dir.
- Acceptance passed: 6 targeted tests, ESLint, and an end-to-end seed-batch run (ok=true, matchedFixtureCount=1, skippedAnnotationCount=1).

- Hardened the local seed-batch pipeline: it now imports detection fixtures, filters fixture annotation images before manifest creation, runs batch overlay verification as a hard gate, and stops before annotation prep on precheck failure.
- Acceptance passed: targeted bootstrap/local-pipeline/batch tests, related seed-batch regression tests, ESLint, diff whitespace check, and control-character check.

- Added a non-blocking Phase 1 readiness snapshot to `run-phase1-intake-pipeline.ts`, so each intake run now records the current 200-image, 800-mask, label-audit, negative-test, and complex-background gate status without blocking small batch ingestion.
- Updated `docs/phase1-intake-pipeline.md` to clarify that pipeline `ok=true` means the batch was ingested successfully, while `readinessSnapshot.ok` tracks full Phase 1 completion.
- Acceptance passed: Phase 1 intake/readiness targeted tests, related dataset-tool regression tests, ESLint, diff whitespace check, and control-character check.

- Added the same non-blocking Phase 1 readiness snapshot to `import-reviewed-batch.ts`, so single-step reviewed imports now record the current dataset gate status even when the full reviewed-batch pipeline is not used.
- Updated `docs/run-reviewed-batch-import-pipeline.md` to document the import-level `readinessSnapshot` and clarify that pipeline success is separate from full Phase 1 completion.
- Acceptance passed: reviewed import/pipeline/readiness targeted tests, related Phase 1 regression tests, ESLint, diff whitespace check, and control-character check.

- Hardened `validate-intake-batch.ts` with image decode and dimension checks, so declared batch images must exist and be readable before Phase 1 intake continues.
- Extended the batch preflight report with `invalidImageFiles` and per-file `imageChecks`, and updated `docs/nail-dataset-batch-intake-sop.md` to document the new gate.
- Acceptance passed: validate-intake and Phase 1 intake targeted tests, related data-tool regression tests, ESLint, diff whitespace check, and control-character check.

- Hardened `audit-sources-csv.ts` with disk consistency checks: each source record now verifies that its image and annotation files exist, that annotation JSON is readable, and that `annotationCount` matches the JSON annotation length.
- Updated `docs/nail-dataset-intake-and-annotation-spec.md` to document the new source audit errors for missing assets and annotation-count drift.
- Acceptance passed: sources audit targeted tests, related Phase 1 data-tool regression tests, ESLint, diff whitespace check, and control-character check.

- Added a split integrity gate to Phase 1 readiness reporting: every annotation document must appear exactly once in train/val/test, and split.json must not reference missing images.
- Updated `docs/phase1-readiness-gate.md` to document `missingFromSplit`, `unknownInSplit`, and `duplicateInSplit` diagnostics.
- Acceptance passed: readiness targeted tests, related Phase 1 data-tool regression tests, ESLint, diff whitespace check, and control-character check.

- Hardened `verify-model-artifact.ts` manifest validation: browser model manifests now reject unsafe `modelFile` paths, non-ONNX model files, unsupported backends, and label order that would break class-index compatibility.
- Updated `docs/training-release-verification.md` to document the stricter browser artifact contract.
- Acceptance passed: model artifact/training release targeted tests, verify-browser-integration and verify-real-model-readiness artifact tests, ESLint, diff whitespace check, and control-character check. Longer run-training-release-pipeline final-audit/governance tests still fail at the current `browserIntegrationOk=false` final-audit gate after release verification passes; this is a pre-existing long-chain integration issue to address separately.

- Fixed the long-chain final-audit browser integration false negative: `verify-browser-integration.ts` now accepts both direct `result.warnings` references and safe copied `warnings: [...result.warnings]` summaries from the picker UI.
- Updated browser integration coverage to use the copied warnings shape, matching the production `NailArtPicker` summary contract.
- Acceptance passed: targeted browser integration / first-run / final-audit tests, full `run-training-release-pipeline` long-chain test (10/10, sandbox-external due python dry-run spawn), ESLint, diff review, and control-character check.

- Extended the Phase 4 texture quality gate with a shape-preservation check: `verify-texture-quality-gate.ts` now reports `roughRectangleRate` and blocks releases when too many annotation polygons collapse into rough rectangles.
- Added configurable thresholds for rough rectangle detection and documented the new gate in `docs/texture-quality-gate.md` v1.1.
- Updated quality-gate tests to cover curved polygons, rough rectangle failures, persisted reports, and final-audit integration.
- Acceptance passed: texture quality/final-audit targeted tests, full `run-training-release-pipeline` long-chain test (10/10, sandbox-external due python dry-run spawn), ESLint, diff review, and control-character check.

- Added a Phase 5 failure-classification audit gate: `scripts/audit-failure-classification.ts` validates `failure-classification.csv` headers, required fields, category/severity enums, template-row replacement, duplicate keys, and basic category coverage.
- Added `tests/audit-failure-classification.test.ts` covering reviewed rows, template-only failure, invalid enums, missing required fields, and persisted reports.
- Added `docs/failure-classification-audit.md` and linked the audit command from `model/training/README.md`, so failure classification can be checked before summarizing or feeding release trace.
- Acceptance passed: failure audit and failure summary targeted tests, ESLint, diff review, and control-character check.

- Added a Phase 5 rollback audit gate: `scripts/audit-release-rollback.ts` verifies release-registry currentVersion, rollback candidates, manifest snapshots, model files, and snapshot/registry consistency before relying on rollback readiness.
- Added `tests/audit-release-rollback.test.ts` covering a complete two-version registry, missing rollback candidate, and missing model file failure paths; existing model release registry tests still pass.
- Updated `docs/model-release-registry.md` to v1.1 and added the rollback audit command to `model/training/README.md`.
- Acceptance passed: rollback audit/model registry targeted tests, ESLint, diff review, and control-character check.

- Added persisted output support to the Phase 5 A/B comparison script: `compare-training-releases.ts` now accepts `--output` and writes the same structured compare summary that it prints to stdout.
- Updated `tests/compare-training-releases.test.ts` to verify the persisted `compare-summary.json`, keeping release decision and governance regression coverage green.
- Updated `docs/training-release-comparison.md` to v1.1 and documented the `--output` path for release decision / governance handoff.
- Acceptance passed: compare-training-releases, release decision, and release governance targeted tests; ESLint; diff review; and control-character check.

- Added a Phase 3 recognition performance gate: `scripts/verify-recognition-performance.ts` reads detection debug JSON / NailArtPicker debug samples, enforces desktop/mobile elapsedMs budgets, reports average/p50/p95/max timing, and writes an optional performance report.
- Extended `verify-nail-detection.ts` debug JSON with `elapsedMs`, so real detection/final-audit artifacts can feed the performance gate.
- Added `tests/verify-recognition-performance.test.ts` and `docs/recognition-performance-gate.md`; linked the command from `model/training/README.md`.
- Acceptance passed: recognition performance/debug-sample/client-worker targeted tests, ESLint, a real fallback detection run on `model/5188.jpg_wh860.jpg`, performance gate verification on the generated debug JSON, diff review, and control-character check.

- Added a Phase 1 training-source authorization gate: `model/training/audit-training-source-authorization.ts` separates internal verification material from release-training material.
- Release mode now blocks web-sourced records, internal-test-only licenses, missing provenance/license fields, ambiguous licenses, and user/merchant sources without explicit training authorization.
- Internal mode allows internal-test-only material for debug/regression while still warning on incomplete provenance.
- Updated `docs/nail-dataset-intake-and-annotation-spec.md` and `model/training/README.md` with the new gate, commands, and recommended license wording.
- Acceptance passed: `node --no-warnings --experimental-strip-types --test tests/audit-training-source-authorization.test.ts tests/audit-sources-csv.test.ts`, `npx.cmd eslint model/training/audit-training-source-authorization.ts tests/audit-training-source-authorization.test.ts`, and full `npm.cmd test` outside the sandbox after Python dry-run subprocesses were blocked by sandbox `spawn EPERM`.
- Current real dataset still has no `model/datasets/nail-texture-v1/metadata/sources.csv`, so the new gate is ready but cannot certify real training data until source records are ingested.

- Integrated the release-training source authorization gate into `scripts/run-training-release-pipeline.ts`.
- Real training now runs `model/training/audit-training-source-authorization.ts --mode release` before `train-yolo-seg` unless `--dry-run`, `--skip-train`, or explicit `--skip-source-authorization` is used.
- Added `--source-authorization-dataset-root` and persisted the report at `model/exports/<version>/training-source-authorization-release.json`; pipeline reports now expose `artifacts.sourceAuthorization`.
- Added regression coverage proving missing release authorization blocks the pipeline before Python training starts.
- Updated `docs/training-release-verification.md` and `model/training/README.md` to document the default hard gate and the limited-use skip flag.
- Acceptance passed: targeted authorization tests, `tests/run-training-release-pipeline.test.ts` outside the sandbox because Python dry-run subprocesses hit sandbox `spawn EPERM`, `npx.cmd eslint --no-ignore scripts/run-training-release-pipeline.ts tests/run-training-release-pipeline.test.ts`, and full `npm.cmd test` outside the sandbox with 186/186 passing.

- Added a combined Phase 1 pretraining gate: `model/training/verify-training-dataset-readiness.ts` runs `audit-sources-csv`, `audit-training-source-authorization --mode release`, and `audit-phase1-readiness` as one report.
- The combined report persists source audit, source authorization, and Phase 1 readiness artifacts, and fails training readiness when any subgate fails.
- Updated `scripts/run-training-release-pipeline.ts` so real training now runs the combined dataset readiness gate before Python training; the pipeline report now exposes `artifacts.trainingDatasetReadiness`.
- Added `tests/verify-training-dataset-readiness.test.ts` for empty-dataset failure and a 220-image / 800-mask release-authorized passing dataset; updated pipeline regression expectations for the combined pretrain step.
- Updated `docs/training-release-verification.md` and `model/training/README.md` to document the combined readiness gate and its training-pipeline behavior.
- Acceptance passed: targeted combined-readiness/source-authorization tests, pipeline regression test outside the sandbox because Python dry-run subprocesses hit sandbox `spawn EPERM`, ESLint with `--no-ignore`, and full `npm.cmd test` outside the sandbox with 188/188 passing.

- Added reviewed-batch training readiness snapshots: `model/training/import-reviewed-batch.ts` now runs `verify-training-dataset-readiness.ts` after import and records `trainingDatasetReadinessSnapshot` alongside the existing Phase 1 `readinessSnapshot`.
- The reviewed import remains successful for small batches, while the new snapshot exposes whether the whole dataset is ready for formal training and which source/authorization/Phase 1 gates still fail.
- Updated `tests/import-reviewed-batch.test.ts` to assert the combined readiness snapshot and subgate sequence, and updated `docs/run-reviewed-batch-import-pipeline.md` to explain the new report field.
- Acceptance passed: reviewed import/pipeline/combined-readiness targeted tests, ESLint with `--no-ignore`, and full `npm.cmd test` outside the sandbox with 188/188 passing.

- Propagated reviewed-batch training readiness into release handoff artifacts.
- `scripts/build-initial-release-trace-draft.ts` now writes a `trainingReadiness` summary with overall status, report path, authorization mode, source/authorization/Phase 1 gate statuses, current image/mask totals, and failing step names.
- `scripts/build-reviewed-batch-release-handoff.ts` now carries the same summary, preferring the live reviewed-import snapshot and falling back to the release trace draft when needed.
- Updated the reviewed-batch end-to-end test to verify both `release-trace-draft.json` and `reviewed-batch-release-handoff.json`, and documented the new fields in `docs/reviewed-batch-release-handoff.md` v1.1.
- Acceptance passed: reviewed import/pipeline targeted tests, ESLint with `--no-ignore`, and full `npm.cmd test` outside the sandbox with 188/188 passing.

- Closed the Phase 5 governance gap for training dataset readiness.
- `scripts/build-release-decision-report.ts` now treats an explicit failed `artifacts.trainingDatasetReadiness` as a hard `hold_candidate`, records the input/artifact evidence, and lists failed readiness subgates in the reason.
- `scripts/build-release-trace-index.ts` now preserves normalized `trainingReadiness` evidence from the authoritative training pipeline, with release authorization mode, three subgate states, image/mask totals, and failing steps; legacy reports without the field remain compatible.
- Added decision, formal trace, and end-to-end governance regression coverage proving a passing model cannot be promoted from a non-ready dataset and promotion is skipped.
- Updated `docs/training-release-verification.md` with the governance hard-gate contract.
- Acceptance passed: 8/8 focused decision/trace tests, 3/3 governance pipeline tests, ESLint with `--no-ignore`, and full `npm.cmd test` outside the sandbox with 191/191 passing.
- Completed the Phase 2 evaluation-visualization artifact loop required by the model plan.
- `model/training/evaluate.py` now runs Ultralytics validation with plots/JSON/text predictions enabled, writes all outputs to a deterministic `evaluation-artifacts/` directory, creates `evaluation-artifacts.json`, and links artifact counts/index paths from `metrics.json`.
- Added `scripts/verify-evaluation-artifacts.ts` as a hard gate for schema, required split, confusion-matrix evidence, and prediction-versus-ground-truth visualizations; PR curves and empty per-image labels remain explicit warnings.
- Integrated the gate immediately after real evaluation in `scripts/run-training-release-pipeline.ts`; failed visual evidence now stops export/release, while report paths and the parsed index are preserved in the pipeline report.
- Added synthetic verifier tests and a mocked-Ultralytics real Python execution test proving metrics and artifact indexes are physically written.
- Added `docs/model-evaluation-artifacts.md` and updated the training README/pipeline documentation.
- Acceptance passed: Python compile and dry-run, targeted ESLint with `--no-ignore`, diff/control review, and full `npm.cmd test` outside the sandbox with 194/194 passing.
- Hardened the Phase 2 evaluation-artifact gate from index-name checks to physical artifact verification.
- `verify-evaluation-artifacts.ts` now requires every indexed file to exist on disk, be non-empty, and be a regular file; it rejects absolute/escaping paths, duplicate entries, mismatched artifact directories, and inconsistent total/plot/label/JSON counts.
- Semantic evidence now only counts physically verified files, so a fabricated confusion-matrix or prediction filename cannot satisfy the release gate.
- Added regression coverage for empty files, missing files, duplicate entries, path traversal, and forged counts; updated `docs/model-evaluation-artifacts.md` with the stronger contract.
- Acceptance passed: 3/3 focused tests, ESLint with `--no-ignore`, and full `npm.cmd test` outside the sandbox with 195/195 passing.
- Closed the Phase 2 usable-texture extraction-rate release gap.
- Fixed `build-release-decision-report.ts` to consume the actual `texture-quality-gate.json` schema (`rates.directlyUsableRate` / `rates.contaminationRate`) while retaining compatibility with earlier top-level fields.
- Added `inputs.phase2ExtractionRateOk`; a measured test-split usable extraction rate below 0.80 now hard-holds the candidate and cannot be bypassed through manual-review promotion.
- Kept the distinction between Phase 2 and Phase 4: rates at or above 0.80 can proceed to manual review when the stricter 85%/contamination/shape gate still fails.
- Added decision-level and end-to-end governance regression tests proving a 0.75 candidate is held and promotion is skipped; updated `docs/release-decision-report.md` to v1.2.
- Acceptance passed: 9/9 focused decision/governance tests, ESLint with `--no-ignore`, diff review, and full `npm.cmd test` outside the sandbox with 197/197 passing.
- Added an automated Phase 0 repository text-encoding audit.
- `scripts/audit-text-encoding.ts` recursively scans supported source/document extensions under `src`, `scripts`, `model/training`, `docs`, and `tests` with fatal UTF-8 decoding and rejects NULs, replacement characters, private-use code points, and high-confidence GBK/UTF-8 mojibake fragments.
- Avoided scanner self-detection by constructing corruption signatures and negative fixtures from Unicode code points rather than embedding corrupted text literals.
- Added `npm.cmd run audit:encoding`, `tests/audit-text-encoding.test.ts`, and `docs/text-encoding-audit.md`.
- Current authoritative scan passed 254 text files with 0 failures; 19 UTF-8 BOM files are reported for visibility but remain allowed.
- Acceptance passed: 2/2 focused tests, real full-repository encoding audit, ESLint with `--no-ignore`, diff review, and full `npm.cmd test` outside the sandbox with 199/199 passing.
- Propagated Phase 2/4 texture-quality evidence into the formal Phase 5 release trace.
- `build-release-trace-index.ts` now writes `quality.phase2ExtractionRateOk`, actual directly usable rate, the fixed 0.80 Phase 2 requirement, Phase 4 texture-quality gate status, and contamination rate from the release decision report.
- Legacy decision reports without these inputs remain explicit `null` rather than being treated as passing.
- Cleaned two prior formatting joins in the trace-index TypeScript interfaces while touching the contract.
- Extended formal trace and end-to-end governance tests, including a held 0.75 candidate whose trace still preserves the quality evidence; updated `docs/release-governance-pipeline.md` to v1.1.
- Acceptance passed: 8/8 focused trace/governance tests, encoding audit (254 files, 0 failures), ESLint with `--no-ignore`, diff review, and full `npm.cmd test` outside the sandbox with 199/199 passing.
## 2026-07-04 - Release-test-split evidence gate for texture quality

- Added representative evidence fields to `scripts/verify-texture-quality-gate.ts`:
  - `--evidence-scope local-debug|release-test-split`
  - `--min-documents`
  - `--min-candidates-with-debug`
  - `--min-candidates-with-polygon`
  - persisted `evidence` summary in `texture-quality-gate.json`.
- Fixed unreadable mojibake next-step text in the texture quality gate output.
- Hardened `scripts/build-release-decision-report.ts` so a candidate is held when Phase 2 texture extraction rates are present but not backed by release-test-split evidence.
- Propagated `phase2ExtractionEvidenceOk` and `phase2ExtractionEvidenceScope` into `scripts/build-release-trace-index.ts` for formal release trace review.
- Updated docs:
  - `docs/texture-quality-gate.md`
  - `docs/release-decision-report.md`
  - `docs/release-governance-pipeline.md`
- Acceptance:
  - Target tests: `node --no-warnings --experimental-strip-types --test tests/verify-texture-quality-gate.test.ts tests/build-release-decision-report.test.ts tests/build-release-trace-index.test.ts tests/run-release-governance-pipeline.test.ts` => 17/17 passed.
  - ESLint targeted check passed for touched scripts/tests.
  - Encoding audit: `npm.cmd run audit:encoding` => 254 files, 0 failures, 18 files with BOM.
  - Full suite: `npm.cmd test` => 201/201 passed.
- Remaining blocker for full MVP completion: still needs a real authorized release dataset and a real trained/exported ONNX model before the end-to-end browser/mobile release claim can be made.
## 2026-07-04 - Recognition performance gate wired into release decisions

- Connected Phase 3 recognition performance evidence to release governance:
  - `scripts/build-release-decision-report.ts` now accepts `--performance-report` and also reads `artifacts.recognitionPerformance` from the training pipeline report when present.
  - A failed performance report now hard-holds the candidate and prevents promotion.
  - Decision inputs now expose performance profile, budget, p95/max latency, and slow sample count.
  - `scripts/build-release-trace-index.ts` now writes a `performance` snapshot into formal release traces.
  - `scripts/run-release-governance-pipeline.ts` now accepts and forwards `--performance-report`.
- Updated docs:
  - `docs/recognition-performance-gate.md`
  - `docs/release-decision-report.md`
  - `docs/release-governance-pipeline.md`
- Acceptance:
  - Target tests: `node --no-warnings --experimental-strip-types --test tests/build-release-decision-report.test.ts tests/build-release-trace-index.test.ts tests/run-release-governance-pipeline.test.ts tests/verify-recognition-performance.test.ts` => 19/19 passed.
  - Target ESLint check passed for touched scripts/tests.
  - Encoding audit: `npm.cmd run audit:encoding` => 254 files, 0 failures, 18 files with BOM.
  - Full suite: `npm.cmd test` => 203/203 passed.
- Remaining blocker for full MVP completion: still needs a real authorized release dataset and a real trained/exported ONNX model before browser/mobile performance can be proven with production-like evidence.
## 2026-07-04 - Training release pipeline forwards performance evidence into governance

- Connected Phase 3 performance evidence at the top-level training release orchestration layer:
  - `scripts/run-training-release-pipeline.ts` now accepts `--governance-performance-report`.
  - Default governance performance report path is `<train-output-dir>/performance-report.mobile.json`.
  - The training release pipeline report now reads this file into `artifacts.recognitionPerformance` when present.
  - When `--run-governance` is enabled, the pipeline forwards the report to `run-release-governance-pipeline.ts` via `--performance-report`.
- Updated the release-governance integration test so the full chain proves performance evidence reaches release decision and formal trace.
- Updated docs:
  - `docs/training-release-pipeline.md`
  - `model/training/README.md`
- Acceptance:
  - Target ESLint check passed for `scripts/run-training-release-pipeline.ts` and `tests/run-training-release-pipeline.test.ts`.
  - Encoding audit: `npm.cmd run audit:encoding` => 254 files, 0 failures, 18 files with BOM.
  - Full suite: `npm.cmd test` => 203/203 passed.
- Note: targeted sandbox run for `tests/run-training-release-pipeline.test.ts` still hits Python subprocess `spawn EPERM` in dry-run tests; full suite was run outside the sandbox with the approved `npm.cmd test` rule.
- Remaining blocker for full MVP completion: still needs a real authorized release dataset and a real trained/exported ONNX model before true browser/mobile performance can be proven with production-like evidence.
## 2026-07-04 - Release governance auto-runs rollback audit after promotion

- Closed the Phase 5 rollback verification gap in the one-click governance path:
  - `scripts/run-release-governance-pipeline.ts` now accepts `--rollback-audit-report` and defaults to `<report-dir>/rollback-audit-report.json`.
  - After successful promotion, governance automatically runs `scripts/audit-release-rollback.ts` against the promoted manifest and registry.
  - A successful promotion now still fails the overall governance report if rollback audit fails, preventing an unsafe “promoted but not rollbackable” state.
  - `release-governance-pipeline-report.json` now includes `paths.rollbackAuditReportPath` and `artifacts.rollbackAudit`.
- Updated governance and training-release tests to use real `register-model-release.ts` baseline registry entries instead of hand-written `{ version }` stubs, so rollback evidence includes manifest snapshots and model files.
- Updated docs:
  - `docs/release-governance-pipeline.md`
  - `docs/training-release-pipeline.md`
  - `docs/model-release-registry.md`
- Acceptance:
  - ESLint targeted check passed for touched governance/training tests and script.
  - Target tests: `node --no-warnings --experimental-strip-types --test tests/run-release-governance-pipeline.test.ts tests/audit-release-rollback.test.ts` => 8/8 passed.
  - Sandbox-only targeted training pipeline test still shows known `spawn EPERM` on Python dry-run startup, while governance-enabled cases passed before the dry-run failures stopped the command summary.
  - Full suite outside the sandbox: `npm.cmd test` => 203/203 passed.
  - Encoding audit: `npm.cmd run audit:encoding` => 254 files, 0 failures, 18 files with BOM.
- Remaining blocker for full MVP completion: still needs a real authorized release dataset and a real trained/exported ONNX model before production-like rollback/performance/browser evidence can be claimed.

## 2026-07-04 - MVP readiness audit for the nail texture recognition plan

- Added a planning-level MVP readiness gate so the project cannot confuse engineering scaffolding with a truly completed nail-texture-recognition MVP.
- New script: `scripts/audit-nail-texture-mvp-readiness.ts`.
  - Checks Phase 1 dataset readiness evidence from `metadata/phase1-readiness.json`.
  - Checks that the training/evaluation/export/conversion/audit/release scripts are present.
  - Checks that the browser model manifest is valid and the referenced ONNX file exists.
  - Checks browser integration markers for model runtime, worker, fallback adapter, and `NailArtPicker` recognition wiring.
  - Checks `package.json` validation commands: `test`, `lint`, and `build`.
- Added npm command: `npm.cmd run audit:mvp-readiness`.
- Added regression tests in `tests/audit-nail-texture-mvp-readiness.test.ts` covering both all-evidence-present and real-gap failure cases.
- Added docs: `docs/nail-texture-mvp-readiness-audit.md`.
- Current real project audit result is intentionally `ok: false` with 3/5 checks passing and 2/5 failing:
  - Missing real Phase 1 dataset evidence: 200 images / 800 valid masks / covered test split.
  - Missing real browser ONNX asset referenced by `public/models/nail-texture-seg/manifest.json`.
- Acceptance so far:
  - Target test: `node --no-warnings --experimental-strip-types --test tests/audit-nail-texture-mvp-readiness.test.ts` => 2/2 passed.
  - Target ESLint check passed for the new script and test.  - Current real project audit command: `npm.cmd run audit:mvp-readiness` => expected `ok: false`, 3/5 checks passed, 2/5 failed for missing real dataset/model evidence.
  - Encoding audit: `npm.cmd run audit:encoding` => 257 files, 0 failures, 18 files with BOM.
  - Full suite: `npm.cmd test` => 205/205 passed.
## 2026-07-04 - Phase 3 ONNX Runtime Web dependency gate

- Closed a Phase 3 browser-runtime packaging gap: `model-runtime.ts` dynamically imports `onnxruntime-web`, but the package was not previously declared in `package.json` / `package-lock.json`.
- Installed and locked `onnxruntime-web@^1.27.0` via `npm.cmd install onnxruntime-web`.
- Strengthened browser integration verification:
  - `scripts/verify-browser-integration.ts` now accepts `--package-json` and checks `package_declares_onnxruntime_web`.
  - Added a regression test proving browser integration fails when all code markers exist but `onnxruntime-web` is missing from package dependencies.
- Strengthened MVP readiness verification:
  - `scripts/audit-nail-texture-mvp-readiness.ts` now requires `onnxruntime-web` in dependencies or devDependencies as part of `validation_commands`.
  - Updated readiness tests to include the runtime dependency in the all-evidence-present fixture.
- Updated docs:
  - `docs/nail-texture-mvp-readiness-audit.md`
  - `docs/browser-integration-verification.md`
- Current real project readiness still correctly reports `ok: false`: Phase 3 package dependency is now present, while real Phase 1 dataset evidence and real ONNX model asset are still missing.
- Acceptance so far:
  - Target tests: `node --no-warnings --experimental-strip-types --test tests/verify-browser-integration.test.ts tests/audit-nail-texture-mvp-readiness.test.ts tests/nail-texture-model-runtime.test.ts` => 15/15 passed.
  - Target ESLint check passed for touched scripts/tests.
  - `npm.cmd run audit:mvp-readiness` => expected `ok: false`, with `onnxRuntimeWebVersion: ^1.27.0` and remaining failures limited to real dataset/model evidence.  - Encoding audit: `npm.cmd run audit:encoding` => 257 files, 0 failures, 18 files with BOM.
  - Full suite: `npm.cmd test` => 206/206 passed.
  - Dependency audit note: `npm.cmd audit --omit=dev` reports 2 moderate vulnerabilities from Next's nested `postcss`; npm suggests `audit fix --force`, but that would install a breaking/downgraded Next version, so no automatic fix was applied.
## 2026-07-04 - MVP readiness now requires release-mode source authorization

- Strengthened `scripts/audit-nail-texture-mvp-readiness.ts` so MVP readiness cannot pass on quantity/split evidence alone.
- Added `training_source_authorization` check:
  - Reads `model/datasets/nail-texture-v1/metadata/training-dataset-readiness-release.json`.
  - Requires `authorizationMode: "release"`.
  - Requires `artifacts.sourceAuthorization.ok === true`, `mode === "release"`, and zero error-severity authorization issues.
- Updated tests so the all-evidence-present fixture includes release-mode training dataset readiness, and the real-gap fixture now proves missing authorization evidence is reported separately from data quantity and ONNX gaps.
- Updated docs: `docs/nail-texture-mvp-readiness-audit.md`.
- Current real project readiness now correctly reports `ok: false`, 3/6 checks passing and 3/6 failing:
  - Missing Phase 1 dataset quantity/test coverage.
  - Missing release-mode source authorization readiness report.
  - Missing real ONNX model asset.
- Acceptance so far:
  - Target tests: `node --no-warnings --experimental-strip-types --test tests/audit-nail-texture-mvp-readiness.test.ts tests/verify-training-dataset-readiness.test.ts tests/audit-training-source-authorization.test.ts` => 7/7 passed.
  - Target ESLint check passed for the touched readiness script and tests.
  - `npm.cmd run audit:mvp-readiness` => expected `ok: false`, now showing the additional `training_source_authorization` gap.  - Encoding audit: `npm.cmd run audit:encoding` => 257 files, 0 failures, 18 files with BOM.
  - Full suite: `npm.cmd test` => 206/206 passed.
## 2026-07-04 - MVP readiness reports actionable next commands

- Extended `scripts/audit-nail-texture-mvp-readiness.ts` so each check now exposes a `commands` array.
- Added top-level deduplicated `nextCommands` to turn readiness failures into an executable work queue.
- Current failed checks now recommend:
  - Phase 1 readiness audit, collection plan, and first-batch checklist generation.
  - Release-mode training dataset/source authorization verification.
  - Training release pipeline, model artifact verification, and browser integration verification.
- Passing checks return empty command arrays, so the report does not recommend already-satisfied work.
- Added path quoting/normalization so commands remain usable when the Windows workspace path contains spaces.
- Updated `tests/audit-nail-texture-mvp-readiness.test.ts` to verify:
  - all-evidence-present reports have no next commands;
  - gap reports include Phase 1, authorization, and training/model commands;
  - top-level commands are deduplicated.
- Updated docs: `docs/nail-texture-mvp-readiness-audit.md` v1.2.
- Acceptance so far:
  - Target tests: `node --no-warnings --experimental-strip-types --test tests/audit-nail-texture-mvp-readiness.test.ts` => 2/2 passed.
  - Target ESLint check passed.
  - Real `npm.cmd run audit:mvp-readiness` output includes 7 actionable commands for the current 3 failed checks.  - Encoding audit: `npm.cmd run audit:encoding` => 257 files, 0 failures, 18 files with BOM.
  - Full suite: `npm.cmd test` => 206/206 passed.
## 2026-07-04 - One-command MVP readiness evidence refresh

- Added `scripts/refresh-nail-texture-mvp-readiness.ts`.
  - Refreshes release-mode source authorization and Phase 1 dataset evidence through the existing training dataset readiness verifier.
  - Always runs the aggregate MVP audit afterward, even when prerequisite gates fail.
  - Persists the training readiness report, MVP readiness report, and orchestration report for traceable acceptance evidence.
- Added npm command: `npm.cmd run audit:mvp-readiness:refresh`.
- Added regression coverage in `tests/refresh-nail-texture-mvp-readiness.test.ts` proving an empty dataset still produces both structured failure artifacts.
- Updated `docs/nail-texture-mvp-readiness-audit.md` to v1.3 with command behavior and default artifact paths.
- Real repository acceptance correctly remains non-passing:
  - Source inventory is missing (`sources.csv`).
  - Phase 1 has 0/200 images and 0/800 valid masks.
  - The real ONNX file referenced by the browser manifest is missing.
  - Despite those expected failures, all three readiness artifacts were written successfully.- Final acceptance:
  - Target test: `node --no-warnings --experimental-strip-types --test tests/refresh-nail-texture-mvp-readiness.test.ts` => 1/1 passed.
  - Target ESLint with `--no-ignore` => passed with 0 errors and 0 warnings.
  - Real refresh command => expected non-zero gate result; persisted training, MVP, and orchestration evidence with 3/6 MVP checks passing.
  - Encoding audit => 259 files, 0 failures, 18 files with BOM.
  - Full suite outside sandbox => 207/207 passed.
- Remaining blocker for complete MVP: a real release-authorized dataset and trained/exported ONNX model are still required.
## 2026-07-04 - Structured missing-source authorization evidence

- Fixed `model/training/audit-training-source-authorization.ts` so a missing `sources.csv` no longer throws an unhandled `ENOENT` stack trace.
- The authorization gate now persists a normal report with `ok: false`, `recordCount: 0`, and issue code `missing_sources_csv`, then exits non-zero as a gate should.
- Added regression coverage in `tests/audit-training-source-authorization.test.ts` for console output and the persisted report.
- Updated `docs/nail-texture-mvp-readiness-audit.md` with the structured missing-source behavior.
- Real refresh acceptance confirms the authorization report now exists, the training-readiness step contains structured stdout, and the authorization artifact is embedded in the aggregate report.- Acceptance:
  - Target chain tests => 7/7 passed.
  - Real `npm.cmd run audit:mvp-readiness:refresh` => expected non-zero gate result, with structured `missing_sources_csv` evidence and no unhandled authorization exception.
  - Target ESLint with `--no-ignore` => 0 errors, 0 warnings.
  - Encoding audit => 259 files, 0 failures, 18 files with BOM.
  - Full suite outside sandbox => 208/208 passed.
- Remaining blocker for complete MVP: collect a real release-authorized dataset and train/export the real ONNX model.
## 2026-07-04 - Empty source inventories cannot pass training gates

- Closed a provenance-gate loophole where a present but header-only `sources.csv` could pass both source integrity and authorization audits.
- `model/training/audit-sources-csv.ts` now emits `empty_sources_csv`, persists `ok: false`, and exits non-zero when no source records exist.
- `model/training/audit-training-source-authorization.ts` independently enforces the same invariant in both internal and release modes.
- Added regression tests for both gates; missing-file behavior remains separately covered by `missing_sources_csv`.
- Updated `docs/nail-texture-mvp-readiness-audit.md` to document that an empty inventory is not valid provenance evidence.- Acceptance:
  - Source/auth/training readiness target tests => 13/13 passed.
  - Target ESLint with `--no-ignore` => 0 errors, 0 warnings.
  - Encoding audit => 259 files, 0 failures, 18 files with BOM.
  - Full suite outside sandbox => 210/210 passed.
- Remaining blocker for complete MVP: populate the now-strict source inventory with real authorized samples, reach Phase 1 quantity/coverage gates, and train/export the ONNX model.
## 2026-07-04 - Phase 3 cancellation now stops worker computation

- Closed a Phase 3 cancellation gap: aborting recognition previously rejected only the caller Promise while ONNX/postprocessing continued inside the Worker.
- `client-worker.ts` now terminates the active Worker on abort, removes abort listeners, settles every pending request, and lazily recreates a Worker for the next recognition.
- Worker errors now reset the failed Worker instead of retaining a poisoned instance.
- Explicit worker disposal now rejects pending calls instead of silently clearing them and leaving unresolved Promises.
- The transferred `ImageBitmap` is closed after conversion in the Worker; a bitmap created after an already-aborted request is also closed on the client.
- Strengthened `verify-browser-integration.ts` with cancellation-termination and transferred-bitmap-release contract checks.
- Updated browser integration verification and real-model UI SOP documentation.
- Acceptance:
  - Worker cancellation/browser contract target tests => 6/6 passed.
  - Runtime/recognition related target tests => 13/13 passed.
  - Target ESLint with `--no-ignore` => 0 errors, 0 warnings after cleaning stale fixture warnings.
  - Encoding audit => 259 files, 0 failures, 18 files with BOM.
  - Full suite outside sandbox => 210/210 passed.
  - `npm.cmd run build` => Next.js 16.2.9 Turbopack production build and TypeScript checks passed; `/ar-tryon` was generated successfully.
- Remaining real-device acceptance: once the ONNX asset exists, manually cancel a long-running WebGPU/WASM inference and confirm CPU/GPU activity drops immediately.
## 2026-07-04 - Phase 3 UI cancellation wired end to end

- Audited `NailArtPicker` after implementing true Worker termination.
- Confirmed automatic detection already creates an `AbortController`, passes its signal through `computeImageDetectedNailRegions`, and exposes a skip button.
- Fixed the remaining close-path race: the top-right Close button now aborts and clears active detection synchronously before invoking the parent `onCancel` callback.
- Added `picker_supports_end_to_end_cancellation` to `verify-browser-integration.ts`; it requires the controller, signal forwarding, active abort ref, skip button, and close button wiring together.
- Updated browser integration verification and real-model UI SOP documentation.
- Acceptance:
  - UI cancellation/browser contract target tests => 6/6 passed.
  - Target ESLint with `--no-ignore` => 0 errors, 0 warnings.
  - Encoding audit => 259 files, 0 failures, 17 files with BOM.
  - `npm.cmd run build` => Next.js 16.2.9 Turbopack build and TypeScript checks passed; `/ar-tryon` generated successfully.
  - Full suite outside sandbox => 210/210 passed.
  - In-app browser manual click verification was attempted, but no active browser tab was available to attach to; real ONNX cancellation remains explicitly unproven until the model asset exists and the UI is manually exercised.
## 2026-07-04 - Removed worker pixel-array expansion hotspot

- Removed a severe main-thread preparation hotspot from `client-worker.ts`: `Array.from(source.data)` expanded every RGBA byte into a normal JavaScript number array before copying it back into a typed array.
- Added `prepareWorkerImagePixels()`:
  - Reuses the existing `Uint8ClampedArray` with zero copy on the normal browser `ImageData.data` path.
  - Uses native typed-array `set` only for generic array-like compatibility inputs.
  - Rejects malformed RGBA lengths with a structured error before constructing `ImageData`.
- Added unit and integration coverage for buffer identity, generic copy behavior, invalid length rejection, and the worker request path.
- Added `client_worker_avoids_pixel_array_expansion` to browser integration verification so the expensive pattern cannot silently return.
- Controlled 1920x1080 microbenchmark:
  - Legacy `Array.from + set`: 168.91 ms, plus an 8,294,400-element temporary number array and an extra 8,294,400-byte typed array.
  - Optimized buffer reuse: 0.0053 ms and the exact same input buffer.
- Updated browser integration and recognition performance documentation.- Production-build correction:
  - The first build exposed that DOM `ImageData` rejects typed arrays backed by `SharedArrayBuffer` at the type-contract level.
  - Zero-copy is now restricted to normal `ArrayBuffer`-backed `Uint8ClampedArray` input; SharedArrayBuffer input is copied into a normal ArrayBuffer and has dedicated regression coverage.
- Acceptance:
  - Worker pixel preparation/browser contract target tests => 10/10 passed.
  - Target ESLint with `--no-ignore` => 0 errors, 0 warnings.
  - Encoding audit => 259 files, 0 failures, 17 files with BOM.
  - `npm.cmd run build` => Next.js 16.2.9 Turbopack production build and TypeScript checks passed after the ArrayBuffer compatibility correction.
  - Full suite outside sandbox => 214/214 passed.
- Remaining performance acceptance: real ONNX desktop/mobile end-to-end reports are still required; the microbenchmark proves removal of this client-side hotspot only.
## 2026-07-04 - Phase 3 performance timing is end to end

- Corrected the Phase 3 performance metric: client-worker results previously exposed only the Worker-internal recognition time, excluding pixel preparation, `createImageBitmap`, queueing, and message transfer.
- `NailTextureRecognitionResult.elapsedMs` now reports end-to-end time from the public worker call boundary and remains no smaller than the Worker-reported time.
- Added `workerElapsedMs` as a diagnostic metric and propagated it through:
  - `NailArtPicker` summary and UI;
  - local debug sample export;
  - imported dataset annotation debug metadata;
  - browser integration contract verification.
- `verify-recognition-performance.ts` continues to gate on end-to-end `elapsedMs`, while reporting `averageWorkerMs` and `averageClientOverheadMs` when diagnostic timing is available.
- Added deterministic timing tests proving a 50 ms end-to-end call with 17 ms Worker time is reported as 50/17 rather than incorrectly passing as 17 ms.
- Updated recognition performance and browser integration documentation.
- Acceptance:
  - End-to-end timing, debug export/import, performance gate, and browser contract target tests => 21/21 passed.
  - Target ESLint with `--no-ignore` => 0 errors, 0 warnings.
  - Encoding audit => 259 files, 0 failures, 15 files with BOM.
  - `npm.cmd run build` => Next.js 16.2.9 Turbopack production build and TypeScript checks passed.
  - Full suite outside sandbox => 214/214 passed.
- Remaining real performance acceptance: collect desktop/mobile debug samples from the real ONNX model and require total `elapsedMs` < 800/1500 ms before release.
## 2026-07-04 - Capped full-resolution detection buffers before getImageData

- Closed the next main-thread memory hotspot in `NailArtPicker`: automatic detection previously created a natural-resolution canvas and called full-resolution `getImageData()` before entering the Worker.
- Added `input-scaling.ts` with tested geometry and candidate remapping helpers.
- Detection canvas longest edge is now capped at 800 pixels before pixel readback.
- Candidate center/width/length values are remapped to original-image coordinates; masks remain normalized full-image grids and are preserved for extraction against the original image.
- End-to-end `elapsedMs` now starts before canvas scaling/readback, so this preparation cost is included in the Phase 3 performance gate.
- Added `picker_caps_detection_input_and_remaps_candidates` to browser integration verification.
- Memory bound example: an 8000x6000 RGBA detection buffer drops from 192 MB to 1.92 MB (about 100x less additional pixel memory).
- Updated performance, browser integration, and real-model UI acceptance documentation.
- Acceptance:
  - Input scaling/browser contract/image detection target tests => 15/15 passed.
  - Target ESLint with `--no-ignore` => 0 errors, 0 warnings.
  - Encoding audit => 261 files, 0 failures, 11 files with BOM after whitespace cleanup.
  - `npm.cmd run build` => Next.js 16.2.9 Turbopack production build and TypeScript checks passed; `/ar-tryon` generated successfully.
  - Full suite outside sandbox => 218/218 passed.
  - Recognition performance gate using `model/debug/perf-check` => desktop budget 800 ms, sample elapsed 21.8 ms, ok=true.
  - `git diff --check` => passed; only Git LF/CRLF normalization warnings remain.
- Remaining real performance acceptance: collect real ONNX desktop/mobile debug samples with `workerElapsedMs` populated; current perf sample still uses fallback backend.

## 2026-07-04 - Hardened browser ONNX artifact readiness gate

- Strengthened `verify-model-artifact.ts` so a manifest can no longer pass with a tiny placeholder ONNX file.
- Added required model size evidence: `minModelKb` defaults to 256KB, `idealModelMb` defaults to 8MB, and `maxModelMb` remains the MVP 15MB limit.
- Added `sizeTier` reporting: `missing`, `placeholder`, `ideal`, `mvp`, or `too_large`.
- Kept 8MB as an optimization warning tier rather than a hard release blocker; 15MB remains the hard MVP upper bound.
- Wired the same credible-artifact check into `audit-nail-texture-mvp-readiness.ts` so `browser_model_asset` requires more than mere file existence.
- Updated real-model SOP, MVP readiness audit, and training release verification docs with the 256KB placeholder guard and 8MB/15MB size targets.
- Updated release/real-model/browser test fixtures that represent passing artifacts from 1KB/2KB placeholders to 300KB credible stubs, while retaining explicit placeholder failure tests.
- Acceptance:
  - Model artifact/MVP readiness/real-model/browser/training-release target tests => 16/16 passed.
  - Broader release/final-audit/training-pipeline related target tests => 28/30 passed inside sandbox; the two failures were dry-run Python subprocess `spawn EPERM` sandbox restrictions, then passed in full suite outside sandbox.
  - Target ESLint with `--no-ignore` => 0 errors, 0 warnings.
  - Encoding audit => 261 files, 0 failures, 11 files with BOM.
  - `npm.cmd run build` => Next.js 16.2.9 Turbopack production build and TypeScript checks passed.
  - Full suite outside sandbox => 221/221 passed.
  - Current `npm.cmd run audit:mvp-readiness` => expected `ok=false`, 3/6 checks passing; `browser_model_asset` now reports `sizeTier: missing`, `minModelKb: 256`, `idealModelMb: 8`, `maxModelMb: 15`.
  - `git diff --check` => passed; only Git LF/CRLF normalization warnings remain.
- Remaining blocker for full MVP: real release-authorized dataset evidence and a real exported ONNX segmentation model are still required before the goal can be marked complete.

## 2026-07-04 - Added ONNX manifest integrity metadata

- Added release integrity metadata to `model/training/export-onnx.py`: exported browser manifests now include exact `modelSizeBytes` and `sha256` for the copied ONNX file.
- Strengthened `verify-model-artifact.ts`:
  - verifies `manifest.modelSizeBytes` when present;
  - verifies `manifest.sha256` when present;
  - supports `--require-integrity` for release-grade checks;
  - reports `computedSha256`, `integrityRequired`, and `integrityOk`.
- Updated `verify-training-release.ts` to call artifact verification with `--require-integrity`, so formal release candidates cannot pass without hash and byte-size evidence.
- Updated `audit-nail-texture-mvp-readiness.ts` so `browser_model_asset` also requires matching `sha256` and `modelSizeBytes`, not just file existence and size tier.
- Extended `NailTextureModelManifest` with optional `modelSizeBytes` and `sha256` fields.
- Updated model integration, MVP readiness, and training release documentation with the new integrity metadata contract.
- Acceptance:
  - Artifact/training-release/MVP/browser/real-model target tests => 16/16 passed.
  - Target ESLint with `--no-ignore` => 0 errors, 0 warnings.
  - Encoding audit => 261 files, 0 failures, 11 files with BOM.
  - `npm.cmd run build` => Next.js 16.2.9 Turbopack production build and TypeScript checks passed.
  - Full suite outside sandbox => 223/223 passed.
  - Current `npm.cmd run audit:mvp-readiness` => expected `ok=false`, 3/6 checks passing; `browser_model_asset` still fails because the real ONNX is missing and now also requires matching `manifest.sha256` / `manifest.modelSizeBytes`.
  - `git diff --check` => passed; only Git LF/CRLF normalization warnings remain.
- Remaining blocker for full MVP: real release-authorized dataset evidence and a real exported ONNX segmentation model are still required before the goal can be marked complete.

## 2026-07-04 - 发布 registry 与回滚模型完整性门禁

- `register-model-release.ts` 在登记版本时计算并保存模型文件的精确 `modelSizeBytes` 与 SHA-256。
- `audit-release-rollback.ts` 对 registry 中每个版本重新计算文件大小和摘要，并校验 manifest 快照中已有的完整性字段。
- `switch-model-release.ts` 在切换或回滚前校验目标模型；文件被篡改、截断或替换时拒绝切换，保持当前 manifest 不变。
- 增加回归用例，覆盖正常登记/切换、审计通过、模型文件登记后被修改时审计失败和回滚拒绝。
- 更新模型版本 registry 与发布治理文档，明确完整性字段和失败行为。
- 验收：
  - 聚焦测试：16/16 通过。
  - 相关 ESLint：0 错误、0 警告。
  - UTF-8 编码审计：261 个文件，0 失败。
  - Next.js 生产构建及 TypeScript：通过。
  - 全量测试（沙箱外，含 Python 子进程）：225/225 通过。
  - `git diff --check`：修复本阶段两处行尾空格后通过；仅保留 Git 的 LF/CRLF 提示。
  - 清理本轮测试生成的 Python 3.14 `__pycache__` 文件；保留并恢复仓库原有的受跟踪 Python 3.13 字节码文件。
- 当前 MVP 就绪审计仍为预期失败（3/6 通过）：真实数据集为 0 图/0 mask、缺少 release 授权的 `sources.csv`、真实 ONNX 文件及其完整性 manifest 尚未提供。这些是外部训练资产缺口，不是本阶段代码异常。
## 2026-07-04 - Local AI-generated nail image intake

- Added recursive local image intake support to `init-intake-batch.ts` so nested folders such as `image/claude/2026_7_4` and `image/Worke Buudy/2026_7_4` can produce one manifest with safe relative paths.
- Hardened `validate-intake-batch.ts` with recursive file indexing, safe relative path rejection, and `sharp` decode checks before any image enters the dataset pipeline.
- Added reviewed-annotation preparation quality reporting in `prepare-reviewed-annotations.ts`, including candidate-count mismatch warnings, manual-review queue output, and `--fail-on-issues` strict mode.
- Fixed single-source dataset splitting: when all images share one `sourceGroup`, `buildDatasetSplit()` now creates a stable 70/15/15 proportional split instead of placing the whole batch in one bucket.
- Imported the local AI-generated batch from `E:/AI Project/Codex/JiaRu/image`:
  - recursive manifest: `model/datasets/nail-texture-v1/metadata/ai-nail-2026-07-04.manifest.json`;
  - manifest validation: 300/300 images decoded successfully, no missing/unsafe/invalid files;
  - Phase 1 intake pipeline: `ok=true`;
  - dataset totals after intake: 300 annotation JSON files, 1411 fallback-generated nail polygons;
  - split after fix: 210 train / 45 val / 45 test;
  - label audit: 300 files, 0 errors, 0 warnings;
  - YOLO segmentation conversion: 300 labels converted.
- Added `docs/ai-generated-nail-image-intake.md` with the exact local commands, generated report paths, acceptance evidence, and remaining manual/model work.
- Acceptance:
  - Focused tests: 13/13 passed.
  - Target ESLint: 0 errors, 0 warnings.
  - `split-dataset.ts`: 210/45/45.
  - `audit-labels.ts`: 0 errors, 0 warnings.
  - `convert-annotations.ts`: 300 converted.
  - `audit-phase1-readiness.ts`: expected `ok=false`; image/mask/label/split gates pass, but negative and complex-background test coverage are still missing.
  - `npm.cmd run audit:mvp-readiness`: expected `ok=false`; 3/6 checks pass, with remaining failures in Phase 1 coverage/source readiness aggregate and missing real ONNX model asset.
- Important caveat: these are fallback bootstrap masks from AI-generated images. They are useful for local training preparation, but should be human-reviewed before real model training/release.
## Phase 1 coverage marking tool

- Added `model/training/mark-phase1-samples.ts` for human-reviewed Phase 1 coverage labels.
- The tool can mark complex-background samples, reviewed negative samples, and move selected files into the test split.
- Added safeguards so negative samples with existing polygons are rejected unless `--clear-annotations` is passed intentionally.
- Documented the review workflow in `docs/ai-generated-nail-image-intake.md`.