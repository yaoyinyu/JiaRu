# 2026-09-20 开发日志

## 循环016两阶段技术路线全面验证

按本轮用户要求，先读取`AGENTS.md`、Goal四段、技术白皮书§3/§11.1/§12.40/§13及当前源码与机器报告，再把“YOLO只做候选、独立模型做逐甲边界”拆为部署、Stage A候选召回、Stage B旧权重复用、Stage B专用训练四个可证伪问题。未修改生产manifest、共享阈值、正式质量门、数据角色或发布身份规则；未读取或回流旧val30、test100及发布留出。

### 部署可行性

- 新增`export-nail-roi-segformer-feasibility.py`，以确定性随机权重导出SegFormer-B0兼容静态ONNX，固定输入`1×3×384×384`、输出`1×1×96×96`。制品14,948,346字节、SHA-256 `511a5790…26f9`，ONNX checker和ORT CPU通过。
- 在真实Headless Chromium 153中验证同一制品：ORT WASM与WebGPU均初始化成功并返回有限值。WASM约291.5ms、WebGPU约950.5ms只保留为当前无头环境诊断，不外推终端时延。浏览器报告SHA-256 `2e86adbe…56d1`；首次`file://`加载WASM失败和默认Python服务器把`.mjs`作为`text/plain`导致失败均保留，修复MIME后才得到成功证据。
- 结论：现有`/ar-tryon`的ORT WebGPU/WASM浏览器栈可以承载独立ROI分割器；该项不证明模型质量。

### Stage A候选召回审计

- 新增`build-development-cycle-016-mediapipe-roi-audit.py`，固定MediaPipe Hands最多2手/10候选、检测阈值0.35与单一ROI几何，在clean98 58张正图/354实例上与batch5 YOLO既有匹配结果取并集。
- 两次Chromium运行计数一致：YOLO 327/354，MediaPipe自身73/354，并集336/354=`0.94915254`；仅46/58图完整，仍有12图/18实例无ROI，候选上限10。报告SHA-256 `15ec747e…e5ad`，决定`stage_a_fixed_union_reachability_fail`。
- 结论：MediaPipe可补回9个YOLO漏实例，但不能作为“每枚完整甲面都有ROI”的保底；旧组合关闭。

### Stage B旧权重上限审计

- 新增预注册计划与`audit-development-cycle-016-oracle-roi-boundary.py`，对batch5锁定权重执行唯一一次真值1.6倍方形ROI oracle实验，固定512输入、0.25阈值、2px边界容差，并以真值选择最大IoU预测；该设置是乐观上限，不是产品流程。
- 354个ROI只有28个产生预测；IoU均值`0.00310480`、Boundary IoU均值`0.00017110`、2px Boundary F1均值`0.00063984`，联合通过0。报告SHA-256 `17bb6ac7…1fbb`，决定`stage_b_existing_yolo_oracle_roi_fail`。
- 结论：现有全图YOLO权重存在严重ROI分布偏移，不能复用为逐甲精修器。

### 专用ROI分割pilot

- 新增`materialize-development-cycle-016-roi-dataset.py`，只从cycle012训练角色的290张正图/1731 mask派生384逐甲ROI。确定性sourceGroup拆分得到1428 train/97组、303 val/21组、交叠0，文件树SHA-256 `0b5f09e8…76cb`，重放通过。
- 查验`nvidia/mit-b0`模型卡及其官方许可证后确认第3.3条仅允许非商业用途，因此未加载该预训练权重。pilot使用Transformers中的SegFormer-B0兼容架构并从项目确定性随机权重训练，避免未来商业上线许可债。
- 新增预注册计划与`train-development-cycle-016-roi-segformer-pilot.py`，固定30轮、batch12、AdamW 0.0003、BCE+Dice+0.2 Sobel边界损失和来源组隔离验证。最佳epoch27：val IoU`0.74888171`、Boundary IoU`0.08546670`、2px Boundary F1`0.24723864`、联合实例通过率0；未过`0.85/0.90/0.80`pilot门。报告SHA-256 `8940c268…1c69`，决定`roi_segmenter_internal_pilot_fail`。
- 结论：专用模型相对旧YOLO真值ROI的IoU 0.003形成明确学习信号，但当前配方远未证明95%边界质量可达；首轮配方关闭，不晋升候选、不导出生产权重。

### 文档与状态

- Goal活动目标已从已完成的两阶段首轮审计切换为303个隔离验证ROI逐实例边界误差归因；下一轮将区分真值边界、1/4输出步幅、ROI上下文与损失设计，不先启动第二轮训练。
- 技术白皮书更新§3、§11.1并新增§12.41；文档版本与§13变更记录同步为v1.1.669。相关实现进度文档追加循环016首轮验证状态。
- 当前准确状态：两阶段运行架构可部署，旧组件复用失败，首个专用ROI配方失败，95%质量可达性尚未验证。无正式候选、无`releaseIdentity`、无生产ONNX，candidate57 TEST HOLD与产品HOLD不变。

### 重放与检查

- clean98物化报告重放：98图、58正图/354 mask、40困难负图，文件树`c4a12153…e1bd`一致。
- cycle012物化报告重放通过；循环016 ROI数据集、ONNX可行性、oracle ROI和训练报告各自重放通过。
- Python 3.13对本轮四个脚本执行`py_compile`通过；浏览器运行结果由Playwright CLI真实Chromium复验。
- 文本编码审计1095文件、0失败；`git diff --check`无空白错误（仅既有LF/CRLF提示）。completion audit v3最新报告SHA-256 `b89b6b26…737f`，18门5通过/13失败，`ok=false`、`decision=hold`、`releaseIdentity=no_approved_release_candidate`，因此Goal保持ACTIVE、产品保持HOLD。

### 逐实例误差归因与第二轮单变量pilot

- 新增预注册误差归因计划及`audit-development-cycle-016-roi-error-attribution.py`。脚本绑定ROI物化报告、文件树和首轮训练报告SHA-256，安装训练器同款只读图片守卫；对303个sourceGroup隔离val ROI固定0.5阈值推理，保存303份预测、最差24例和最佳12例叠加图。报告SHA-256 `e1673b52…0b66`重放通过。
- 归因结果：首轮均值IoU`0.74888171`、2px Boundary F1`0.24723864`、对称边界距离`11.43975062px`；真值nearest降到96再bilinear恢复384的步幅上限IoU`0.96299971`、Boundary F1`0.89934909`，仅`48.844884%`实例达到0.90。低色差三分位Boundary F1`0.18104251`，高色差组`0.33358797`；高复杂度组IoU`0.70509772`，低复杂度组`0.79268464`。决定不能继续使用固定1/4双线性最终输出。
- 预注册并完成第二轮唯一变量实验：新增全分辨率RGB-logit浅层残差细化头，其余数据、来源组拆分、随机初始化、30轮、优化器、阈值和`0.85/0.90/0.80`门不变。训练耗时1187.33秒；最佳epoch30的IoU`0.75931897`、Boundary IoU`0.09232614`、Boundary F1`0.26289865`、联合通过率0，相对首轮仅`+0.01043726/+0.00685944/+0.01566001/0`。报告SHA-256 `46f944b1…01af`重放通过，决定仍为`roi_segmenter_internal_pilot_fail`，配方关闭、不晋升、不导出。
- 本轮没有读取clean98、旧val30、test100、发布测试或独立留出进行训练/调参；ROI数据集物化重放仍为290源图/1731实例、1428 train/303 val、97/21来源组且交叠0。下一唯一活动改为固定最差24例与最佳12例的原分辨率真值边界审计，先判断监督误差，再选择真值纠正或边界监督中的一个变量；审计前不再训练。
- Goal、白皮书§3/§11.1/§12.41与§13 v1.1.669、实施进度文档已同步本轮归因和第二轮失败结果。无正式候选、无`releaseIdentity`、无生产ONNX，产品继续HOLD。

### 固定36例真值审计与全303例复审启动

- 新增预注册计划和`build-development-cycle-016-truth-boundary-review.py`。固定36例不按视觉结果换样；构建器绑定误差归因、ROI与cycle012报告哈希，安装只读图片守卫。原图/标签、cropBox、ROI JPEG与mask PNG均36/36精确重建，生成9页原始像素裁块与polygon边线审核证据。
- 原分辨率逐例裁决：27通过、5过分割、1混合错误、3低清不可确认，覆盖13个来源组；4例共享`decoration_tip_contamination`，达到预注册系统误差阈值。最终报告SHA-256 `b8bbd512…4531`重放决定`full_validation_truth_reaudit_and_correction_before_training`。
- 按验证真值规则，现有303实例val折在全量复审、缺陷修正和v2重建前不得用于模型选择或阈值校准；两轮pilot失败、数值和权重保持历史诊断身份，不改写为通过。
- 预注册并构建全303例审核包：按sourceGroup和实例ID固定排序生成76页，原图/标签、cropBox与ROI字节303/303精确重建，审核报告SHA-256 `9f369e01…3ae9`重放通过。首次构建错误把`expected`写成36，失败目录移为`rejected-expected-count-bug`保留；修复后重新物化，未把失败写成通过。
- 完成首个24例分片：16通过、5过分割、2欠分割、1混合错误；分片报告SHA-256 `b67d3445…48d1`重放通过。当前累计24/303，剩余279例；下一唯一活动为完成分片002—013并冻结修正/排除清单，随后重建sourceGroup零交叠v2。
- 白皮书同步更新§3、§11.1并新增§12.42，头部版本和§13最新条目同步为v1.1.670；Goal和实施进度同步。未读取旧val30/test100/发布测试或独立留出，产品HOLD不变。
- 最终重放：固定36例最终报告、全303例审核包和24例分片均返回`ok=true`；Python 3.13编译通过；文本编码审计1098文件/0失败；`git diff --check`无空白错误（仅既有LF/CRLF提示）。

## 循环016全量验证真值复审推进至96/303

- 继续按全量报告固定顺序完成分片002—004共72例原分辨率四区审核。分片002为12通过、6过分割、1欠分割、1混合错误、4低清边界不可确认；分片003为22通过、2过分割；分片004为20通过、4低清边界不可确认。
- 三个新增分片决策文件SHA-256分别为`48b561c3…d0f`、`3cecd9e9…a31`、`0e7e6830…d9c`，均由`--verify-shard`重放为`ok=true`。连同分片001累计96/303：70通过、13过分割、3欠分割、2混合错误、8低清边界不可确认，剩余207。
- 当前303实例val折继续保持仅历史诊断身份；尚未完成的分片005—013、修正/排除清单和sourceGroup零交叠v2重建完成前，不启动第三轮训练、不校准阈值。无正式候选、无`releaseIdentity`，产品HOLD不变。
- Goal活动目标及下一步已移除完成的分片002—004，切换为剩余分片005—013；白皮书§3、§11.1、§12.42与§13同步为v1.1.671，实施进度同步累计审核状态。
- 最终检查：全303审核包重放为303/303原图/标签、cropBox及ROI字节精确匹配，报告`9f369e01…3ae9`；分片001—004全部`ok=true`，累计计数由决策文件重新聚合为96/303且剩余207；Python 3.13编译、1098文件编码审计和`git diff --check`均通过（仅既有LF/CRLF提示）。首次把`--verify-report`误传为报告文件导致路径重复，随后按脚本接口改传审核目录并成功重放，未把该调用错误写成证据失败或通过。

## 循环016全量验证真值复审推进至168/303

- 继续按全量报告固定顺序完成分片005—007共72例原分辨率四区审核。分片005为17通过、3过分割、4低清边界不可确认；分片006为11通过、7过分割、4混合错误、2低清边界不可确认；分片007为23通过、1欠分割。
- 三个新增分片决策文件SHA-256分别为`7cf335f4…8920`、`7d86854e…3e98`、`48b895b0…d082`，均由`--verify-shard`重放为`ok=true`。连同分片001—004累计168/303：121通过、23过分割、4欠分割、6混合错误、14低清边界不可确认，剩余135。
- 当前303实例val折继续保持仅历史诊断身份；尚未完成的分片008—013、修正/排除清单和sourceGroup零交叠v2重建完成前，不启动第三轮训练、不校准阈值。无正式候选、无`releaseIdentity`，产品HOLD不变。
- Goal活动目标及下一步已移除完成的分片005—007，切换为剩余分片008—013；白皮书§3、§11.1、§12.42与§13同步为v1.1.672，实施进度同步累计审核状态。
- 最终检查：全303审核包重放为303/303原图/标签、cropBox及ROI字节精确匹配，报告`9f369e01…3ae9`；分片001—007全部`ok=true`，累计计数由决策文件重新聚合为168/303且剩余135；Python 3.13编译、1098文件编码审计和`git diff --check`均通过（仅既有LF/CRLF提示）。

## 循环016全303验证真值复审完成并冻结修正清单

- 完成分片008—013剩余135例原分辨率四区审核并逐分片重放。六个分片分别为：23通过/1过分割，19通过/5混合错误，13通过/6过分割/5混合错误，21通过/1过分割/2混合错误，19通过/4过分割/1混合错误，14通过/1过分割。
- 全303例最终聚合为230通过、36过分割、4欠分割、19混合错误、14低清边界不可确认，覆盖21来源组。合并裁决`eafe2df2…5fc7`绑定13个分片哈希，最终报告`4aa2ff6e…1298`重放为`ok=true`、`decision=full_validation_truth_reaudit_and_correction_before_training`。
- 扩展`build-development-cycle-016-truth-boundary-review.py`：最终报告实例数改为从绑定审核报告推导，旧36例最终报告继续重放通过；新增修正/排除manifest构建与校验模式，验证输入哈希、最终裁决顺序、源图级排除传播和动作计数。
- 修正manifest `ff05e428…6825`按源图硬门把14个低清实例所在4张图/3来源组整体排除，共25实例；其余冻结57实例返修、221实例保留。下一唯一活动是完成57个polygon原分辨率返修并重建预计278实例/18来源组、与train来源组零交叠的v2验证真值；在此之前不训练、不校准阈值，产品HOLD不变。
- Goal已移除完成的分片复审活动；白皮书§3、§11.1、§12.42与§13同步为v1.1.673，实施进度同步全量复审与修正清单状态。
- 阶段验收：全303审核包、旧36例最终报告、新303例最终报告、修正manifest和分片001—013均重放通过；聚合计数为303/303且剩余0。首次并行启动多个Python/OpenCV复验进程时，manifest出现`MemoryError`、shard002出现页面文件不足导致的DLL加载失败，另一个PowerShell初始化失败；未重建证据，按项目资源竞争规则只对受影响检查串行复验，随后三项全部通过。Python 3.13编译、1098文件编码审计、Goal四段结构、白皮书头部/§13版本一致性及`git diff --check`通过（仅既有LF/CRLF提示）。

## 循环016验证真值v2返修工作区落地

- 新增预注册计划`nail-texture-development-cycle-016-val-v2-repair-plan-v1.json`与构建器`build-development-cycle-016-val-v2-repair-workspace.py`。计划只绑定修正manifest `ff05e428…6825`、全303审核报告`9f369e01…3ae9`和cycle012物化报告`f47b62f1…61fc`，固定57返修、221保留、25实例/4源图排除和预计v2 278实例/18来源组，继续禁止读取受保护集或在v2复核前训练。
- 构建器安装Ultralytics只读图片守卫，逐项复验cycle012源图与标签SHA-256；生成25张整图上下文、57组2.2倍原始像素裁块/旧polygon叠加、浏览器逐点编辑入口和待审数据。编辑器支持拖动、按边插点、右键删点、撤销、重置、逐项视觉通过状态与裁决导出；导出绑定计划、冻结清单、审核报告和cycle012报告四项路径/SHA。
- 首版工作区报告`4c0ed0fb…c7ce`暴露导出`workspaceReportSha256`因自引用为空的问题；未将其作为可用入口，保留历史产物后修正为直接绑定四项不可变输入。规范v2工作区报告`54eceb5f…3b28`、资产聚合哈希`768c509f…ed11`重放为`verified_repair_workspace`；57个身份唯一，覆盖25张受影响源图，全部状态仍为`pending_original_resolution_manual_repair`，没有自动批准polygon。
- 抽查`real_training_20260712_0010__nail-05`原始像素叠加图可清楚显示冻结裁决中的侧缘尖刺污染，原图上下文、裁块和节点一致。cycle012 ROI物化报告随后重放通过，仍为290源图/1731实例、1428 train/303 val、97/21来源组且零交叠，文件树哈希`0b5f09e8…76cb`未变。
- 使用Playwright CLI驱动真实Chromium完成编辑器冒烟：页面加载57项，原图/裁块/旧边界资产均返回200；切换至第2项后节点数由24正确更新为42，状态可`pending→reviewed_pass→pending`往返，导出的`repair-decisions-v1.json`为57项且全部恢复pending，并绑定plan、correctionManifest、reviewReport、cycle012DatasetReport四个输入SHA。唯一控制台错误是浏览器自动请求未提供的`favicon.ico`返回404，不影响核心编辑或导出；未把冒烟状态写回规范证据。
- Goal活动目标和下一步已切换为只使用规范v2工作区完成57/57原分辨率人工返修裁决，之后才合成221保留与25排除并重建v2。白皮书§3、§11.1、§12.42及§13 v1.1.674、实施进度文档同步；无正式候选、`releaseIdentity`或生产ONNX，产品HOLD不变。

> 本条目由 **Codex** 编写（2026-09-20）。
