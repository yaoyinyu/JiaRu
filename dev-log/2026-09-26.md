# 2026-09-26 开发日志

## 安全审计全盘复扫与收口（本地代码 + 云端服务器）

- 按 `AGENTS.md` 要求先读技术白皮书（状态表与 §13 最新条目 v1.1.717），再对 `docs/security-audit-2026-09-23.md` 的 13 条（C1、H1—H3、M1—M5、L1—L4）逐条复查，**包括上一轮声称已修但实际未闭合的项**。基线：他人已提交 `c2bd245`（安全审计修复 + 循环016 分片闭合），工作区仅剩本轮差异。
- **复查确认已闭合**：C1（`next`/`eslint-config-next` 16.3.6，`npm audit` 0 漏洞，补丁差异 `[/#?]`→`[/#?\\\\]`）；H1（以 `NODE_ENV=production` 实测三种 IP 场景，`deliverSmsCode` 一律拒绝、不再回文明文验证码）；H2（安全响应头齐全）；M1（五处认证接口限流）；M3（CSRF 中间件）；M4（`bindPhone` 先消费验证码）。
- **新发现并修复的遗漏项**：H3 只修了通用 500 分支，`agnes-image-api.ts` 与 `seedream-image-api.ts` 的非预期异常分支仍在抛 `服务器错误: <原文>`，`wechat.ts` 把网络层异常与上游 `errmsg` 拼进 `AuthError`，微信回调更把异常原文写进重定向 URL 查询串（既是信息泄漏也是反射点）——四处均改为只写服务端日志。**seedream 的 `data[0].error` 业务级文本予以保留**（如「内容审核未通过」是产品需要回传给用户的提示，且有测试覆盖），只脱敏非预期异常。
- **补齐 L 级项**：L1 `getClientIp` 改为仅在 `JIARU_TRUST_PROXY=1` 时采信代理头，否则返回 null；L2 `ar-demo` iframe 补 `sandbox="allow-scripts allow-same-origin allow-forms allow-popups"`（摄像头 demo 未经真机验证，若异常可删除该属性回退）；L3 JWT 增加 `iss`/`aud` 校验并固定 `alg=HS256`（生产此前因 `JWT_SECRET` 缺失根本签不出令牌，无存量会话受影响）；L4 验证码 store 增加 5000 条硬上限。M5 游客身份改与限流同一套来源判定，伪造 XFF 不再换取独立配额。
- **连带发现并修复的开放重定向**：反代下 `req.nextUrl.origin` 被 Next 解析成 `http://0.0.0.0:3000`（Nginx 已正确转发 Host/X-Forwarded-Host/X-Forwarded-Proto），微信 OAuth 的 `redirect_uri` 与回调跳转会导到不可达地址。新增 `src/lib/request-origin.ts` 统一推导对外 origin，线上实测回调已正确跳回 `http://49.233.141.42/login?error=state_mismatch`。
- **云端已重新部署两轮**（产物包 SHA-256 双端核对：`3398f4d2…42c8`、`6bc1229a…c209`），旧产物留 `/opt/jiaru/.next.backup-0924b`、`.next.backup-0926a`。防火墙复查：3000 已移除、443 已放行、22 仍 0.0.0.0/0。线上实测：`/` 200 且带 nosniff/X-Frame-Options/Referrer-Policy/COOP、无 `X-Powered-By`；`/api/me` 401；无 Origin 的 POST 403。
- **测试**：受影响的 6 个测试文件（user-auth、ai-quota-guard、auth-session-integrity、user-db-directory、agnes-image-api、seedream-image-api）**50/50 通过**；同步更新了两条编码旧行为的用例（`bindPhone` 枚举防护、`guestIdentityFromHeaders` 反伪造）。全量 937 例中 427 例失败，按文件归因全部落在美甲模型线与训练数据审计域（`calibrate-model-score-threshold`、`independent-hard-negative-authorization`、`development-cycle-013-candidate-ceiling` 等），且这些文件对本轮改动模块引用数为 0，属既有状态，未伪称为通过。
- **未闭合（需用户决策/授权）**：M2 refresh token 仍为静默续期，`rotateTokens` 无调用点（代码注释说明是为并发安全刻意选择），改轮换会引入并发竞态，属产品决策；SSH 22 收敛来源 IP；域名与 TLS 需授予 CAM 角色 `Lighthouse_QCSLinkedRoleInDnsAndSsl`。另注：Next 16.3 构建已提示 `middleware` → `proxy` 迁移告警，当前仍生效，需列入后续升级观察。
- 白皮书 §13 新增 v1.1.718 条目（WorkBuddy 署名）并同步头部版本；本轮未执行 `git commit`/`git push`，美甲纹理模型线与 `/ar-tryon` 产品 HOLD 状态不变。

> 本条目由 **WorkBuddy** 编写（2026-09-26）。
