# 2026-08-20 开发日志

## 建立「提交推送前同步README」持久规则并补齐README用户系统内容（用户要求）

- 用户要求：每次提交推送时记得修改README中对应的部分。
- 任务启动复核白皮书 v1.1.489 变更记录、§3 功能状态总表（用户账户行与 AI 行）及 README 现状：功能概览表、项目结构、核心特性详解、环境变量表、文档索引、隐私与数据、路线图均缺少用户系统（v1.1.472—477 落地）内容，且隐私节残留已过时的「无账户体系」表述；AI 生图增强（v1.1.478—488）相关内容在上轮提交时已同步。
- 规则固化：`AGENTS.md` 行为规则新增——每次 `git commit`/`git push` 前必须检查并同步更新 `README.md` 中与本次改动对应的部分（功能概览表、项目结构、核心特性详解、环境变量表、文档索引、隐私与数据、路线图等），随提交一并推送；不得在 README 缺失或过期时提交推送。
- README 更新（用户系统部分）：
  1. 功能概览表新增「登录/账号」行（`/login`、`/account`，✅，手机号+验证码登录即注册、微信 OAuth；初始版已落地，商户/云同步后续规划）；
  2. 项目结构补充：`login/page.tsx`、`account/page.tsx`、`api/auth/`、`api/me/`、`lib/auth/`、`components/Icon.tsx`、`lib/ai-hand-anatomy-prompt.ts`、`lib/agnes-image-api.ts`；
  3. 核心特性详解新增「👤 用户系统」章节（登录即注册、手机号+验证码+人机验证、微信 OAuth+补绑手机号、HS256 JWT+SQLite 会话、/account 账号页、user-system-plan.md 链接）；
  4. 环境变量表新增 `JWT_SECRET`/`JIARU_DB_PATH`/`SMS_PROVIDER`/`WECHAT_APP_ID`/`WECHAT_APP_SECRET`（含默认值与生产要求说明）；
  5. 文档索引新增 `docs/user-system-plan.md`；
  6. 隐私与数据表新增「用户系统」行（登录信息仅存服务端数据库、不出售），删除「无账户体系」过时表述；
  7. 路线图「待规划」中「用户账户、云同步」更新为「用户系统 Phase 1 剩余项：商户体系、云端作品/收藏/历史、AI 配额、注销导出、游客数据合并」。
- 验证：README 表格结构完整（功能概览表 10 行、环境变量表 8 项）；白皮书同步至 v1.1.490（变更记录追加，头部版本号同步）。纯文档变更，无接口/状态/数据变化，未运行训练、推理、val/test、导出、登记或部署；按用户要求随提交推送。
