# 2026-09-16 开发日志

## WorkBuddy：收编提交推送 v1.1.652—656（Codex 三批模型线 + WorkBuddy 白皮书治理）

用户 00:35 发起「了解当前项目进展情况，继续推进项目进度」，经确认选择「提交推送当前成果」。

### 并发安全

Codex 最后活动 09-15 19:07（dev-log/白皮书收尾），提交时 09-16 00:47，**静止约 5.5 小时**。

### 收编清单（15 项 = 9 修改 + 6 新增）

- 模型线（Codex v1.1.652—654）：循环015 第二/三/四批各 10图/60甲真值（累计 43图/265 mask、43/90、缺口 381）；工作区构建器移除固定 12/13/23 累计数限制改按 1—133 派生；新增 batch3/batch4/incremental 加速审计脚本与 3 个专项测试。
- 文档治理（WorkBuddy v1.1.655—656）：白皮书剔除 13 条纯提交记录、§13 顺序修复 5 簇、同号异常与 v1.1.465 历史缺口登记、§3 数据治理行更正；Goal 切至 53/133 与 53/90。

### 收编前验证（全部实测）

- 全量 `npm.cmd test` 串行：**959/959 通过、0 失败**（13 分 1 秒）。
- `npm.cmd run audit:encoding`：通过；模型线 4 个 Python 脚本 `py_compile` 通过。
- `git diff --check` 干净（仅既有 LF/CRLF 提示）；`git fsck --no-dangling` 无异常。
- README 文档索引 grep 实测 = v1.1.656，与白皮书头部、§13 最新一致。

### 提交与推送

- 提交 `97676e7`：「提交循环015第二批至第四批真值与PCA多点SAM标注提速验证，白皮书治理至v1.1.656」（15 files，+1076/−101）。
- 推送按既定模板一次成功（`595db15..97676e7 master -> master`），并重建被沙箱抹掉的 `.git/refs/remotes/origin/master` 跟踪引用。
- 按用户 2026-09-15 规则：本提交推送轮次不新增白皮书 §13 条目、不产生版本号变更。

### 其他确认

- 用户 9-14 批准的逐甲终审物化计划（per-nail-review-evidence T1—T5）已被 Codex 的 PCA多点SAM 流水线全面超越完成；该脚本随 v1.1.651 入库但工作流已由 Codex 终审链取代，留作后续归档清理候选。

> 本条目由 **WorkBuddy** 编写（2026-09-16）。

## WorkBuddy：循环015第五批源图供给 ImageGen 试画验证与止损（纯技术验证，无代码/数据变更）

用户指示「按照 goal 推进」，Goal 唯一活动为第五批 10 图（53/133、53/90）。经用户确认选择 A 方案：先试画评估 WorkBuddy ImageGen 画手质量，不达标立即止损转产品线。

### 试画与审核结果（4 张，约 20—40 credits）

- 单手裸粉透明长杏仁甲：5/5 手指、5/5 甲面可见，解剖自然——但右下角「AI生成 WORKBUDDY>」水印。
- 双手十甲深酒红短方甲：10/10 手指、10/10 甲面可见，指节褶皱略重但可信——同样水印。
- 深肤色珊瑚橙短圆甲：5/5 手指但拇指甲面被姿势遮挡（4/5 甲面）——同样水印。
- `footnote=""` 决定性验证（薄荷绿单手）：5/5 甲面质量良好，**水印依然存在**。

### 止损判决

- **画手质量好于预期（4 张零六指），但「AI生成」标识为平台合规强制水印，参数无法关闭**；源图门 `noTextLogoOrWatermark` 硬项不可通过。
- 修图去水印 = 删除法规强制 AI 内容标识（不可行），且破坏 `generatorOutputPath` SHA-256 证据链语义。
- **判决：ImageGen（混元）路线对循环015源图供给结构性不可用，与 intake 审计生成器白名单无关**。未改动任何审计脚本、测试或合同；未消耗训练/推理预算；未污染任何证据数据集。试画图留存 `C:\Users\YaoYinyu\.workbuddy\tmp\cycle015-batch5-trial\` 不入库。
- **第五批源图供给回归 Codex 内置生图**（历史 43 张全部通过无水印验证，原生兼容流水线）。WorkBuddy 按用户事先批准的决策转做产品线评审遗留。
- 本条为纯技术验证与止损记录，无代码/数据/模型/门变更，按规则不计入白皮书。

> 本条目由 **WorkBuddy** 编写（2026-09-16）。

## WorkBuddy：评审 #6/#7 修复完成——测试定位、验证链全绿、白皮书 v1.1.657

按用户事先批准的决策（ImageGen 止损后转产品线评审遗留），完成 2026-09-05 评审 #6（生产短信与 token 续期）与 #7（验证码防护与手机换绑一致性）的代码修复、测试与文档登记。

### 代码变更（12 修改 + 3 新增）

- **#6-静默续期**：`service.ts` 新增 `renewFromRefresh`（不轮换会话、不撤销令牌，并发安全；refresh 剩余寿命 < 15 天时滑动重签）；新增 `POST /api/auth/refresh` 路由；`http.ts` 新增 `requireUserWithRenewal`/`applyRenewedCookies`；`/api/me`（GET/PATCH）、身份列表/解绑、改进计划、绑手机共 6 处调用点续期化；生图路由保持简单版 `requireUser`。
- **#7-验证码**：`captcha.ts` 重写为 5×7 点阵笔画路径渲染（`GLYPH_STROKES` → 缩放+扰动+旋转 `<polyline>`，SVG 无 `<text>` 节点、不泄露答案）；`verification_codes` 增 `consumed_at` 列（PRAGMA 检查后 ALTER 迁移），消费=标记、发送台账与生命周期分离；`consumeValidatedCode` 统一校验。
- **#7-换绑**：`db.ts` 新增 `transaction`（BEGIN IMMEDIATE/COMMIT/ROLLBACK）、`rebindIdentityPhone`、`clearUserPhone`；`bindPhone` 事务内一致更新 `users.phone` 与 phone identity，UNIQUE 冲突转译 `phone_taken`；解绑 phone 同步清空资料。
- **其他**：`package.json` 增 `engines.node>=22.15.0`；README 认证段/目录树/文档索引同步。

### 测试与验证（全部实测）

- 新增 `tests/auth-session-integrity.test.ts` 7 用例。其中滑动重签用例曾失败：自研 JWT payload 无随机成分（仅 `{sub,sid,typ,iat,exp}`），登录与续期同秒时重签 token 与原 token 字节级相同，真实时钟下 `notEqual` 断言依赖秒边界必败（断言设计缺陷，非实现缺陷）；改用 `node:test` mock 时钟（登录后 tick 30 分钟显式构造「临近过期」）后确定性通过。方法论：涉及 token 时间的断言不得依赖真实时钟秒边界。
- 单文件 7/7；认证三文件 21/21；全量 `npm.cmd test` 串行 **966/966 全过**（0 失败 0 跳过，12 分 19 秒；基线 959+新增 7）。
- ESLint 本轮 12 文件 0 错误；`tsc --noEmit` 本轮文件 0 错误（存量 171 处与 v1.1.649 基线一致，均为模型线测试既有问题）；`git diff --check` 干净。

### 文档登记（v1.1.657）

- 白皮书：§13 表首新增 v1.1.657 条目（WorkBuddy 署名）；文档头 v1.1.656→v1.1.657；§5 账号接口段追加更正（refresh 续期入口、验证码发送台账、手机换绑一致性三项缺口闭合，短信适配仍未接入）。README 认证实现/图形验证码/目录树/文档索引（v1.1.656→v1.1.657）四处同步。全部修改 grep 实测落盘。
- 过程坑：对同一文件并行发多个 Edit 会互相覆盖（后写者基于旧内容写回）——同一文件多处修改必须串行执行。

### 遗留

短信供应商适配（需用户提供凭据）；评审 #8 编辑器原尺寸导出、#9 AI 比例映射/AR 纹理释放、#11 浏览器回归未做。未 commit/push（未经用户要求）。

> 本条目由 **WorkBuddy** 编写（2026-09-16）。

## WorkBuddy：评审 #8/#9 四缺陷修复——编辑器显示/输出分离、比例就近映射、AR 纹理释放，v1.1.658

按用户指示（「除了短信供应商适配，其他的你能直接做吗」——评估后 #8/#9 可直接做，#11 需引入 Playwright 待裁决），完成 2026-09-05 评审 #8（编辑器）与 #9（AI/AR）四项修复。

### 代码变更（4 修改 + 2 新增）

- **#8-A 原尺寸导出**：`src/components/NailCanvas.tsx` 重构为笔级笔画记录（`strokesRef`），预览画布按最长边 400×600 缩放；保存时创建原图尺寸离屏画布，重放原图＋全部笔画（点坐标与笔刷半径按 `outScale` 等比换算），插值语义与实时绘制逐点一致，4096 方图导出 4096 方图。
- **#8-B 重置回原图基线**：重置＝清空笔画重放（与撤销栈解耦），修复 20 份 ImageData 快照淘汰导致 25 笔后重置回第 6 笔；撤销改全量重放，顺带消除 20 步上限。
- **#9-A 比例就近映射**：`pickReferenceRatio` 抽取至 `src/lib/ai-image-size.ts`，区间切分改 log 空间最近距离（横竖对称），修复 3:4→2:3、2:3→9:16 错判；`/ai-generate` 页面改用共享实现。
- **#9-B AR 纹理释放**：新增 `src/lib/ar-texture-release.ts` 的 `releaseRemovedBitmaps`（kept/closed 双 Set 集合 diff，重复引用只 close 一次）；`applyTextureToAll` 改用之，修复多指共享时逐项检查互相「看到对方」全部跳过 close 的泄漏。handleCropConfirm/handlePickingConfirm/removeTexture 语义正确未动（最小回归面）。
- 纯逻辑模块 `src/lib/canvas/strokes.ts`（previewScale/scaleStroke/interpolateStrokePoints/strokeDots）配套可测；paintDot/paintStroke 提升模块层消 hooks 依赖告警。

### 测试与验证（全部实测）

- 新增 `tests/review-p2-canvas-and-texture.test.ts` 9 用例＋`tests/ai-image-size.test.ts` 追加 4 用例＝13 项；全量 `npm.cmd test` 串行 **979/979 全过**（0 失败 0 跳过，12 分 23 秒；基线 966＋新增 13）。
- ESLint 0 错误 0 警告；`tsc --noEmit` 本轮文件 0 错误（存量 171 处与 v1.1.649 基线一致）；`git diff --check` 干净。

### 文档登记（v1.1.658）

- 白皮书：头部 657→658；§13 表首新增条目（WorkBuddy 署名）；§4.2 复核段后追加更正（两项缺陷闭合）；§9.7 评审表后追加更正（「编辑器/AI/AR」行四项闭合，历史行保留不改写）。
- README：文档索引 v1.1.657→v1.1.658（编辑器/比例功能描述核对后无事实性过期，不改写）。
- 全部修改 grep 实测落盘；白皮书四处修改全程串行 Edit（同文件并行互覆盖坑延续规避）。

### 遗留

- **#11 浏览器回归**：需引入 Playwright 等浏览器测试框架（新依赖＋配置的框架级决策）；其中「上传→25 笔→重置」「原尺寸导出」两条用例依赖 #8 修复后行为，待用户选择方案。
- 短信供应商适配（需用户提供凭据）。未 commit/push（未经用户要求）。

> 本条目由 **WorkBuddy** 编写（2026-09-16）。

## WorkBuddy：评审 #11 浏览器回归落地（Playwright 6/6）＋TextureCropper StrictMode 竞态修复，v1.1.659

用户裁决「引入 Playwright 做」后落地评审 #11 全部六条浏览器关键行为回归。

### 基础设施（3 新增＋2 修改）

- devDependency `@playwright/test@^1.63.0`（Chromium 153）；`playwright.config.ts`：testDir=e2e、workers=1、1440×900 headless、fake camera 启动参数＋camera 授权、webServer 以 `next dev --port 3399` 独立实例注入 `JIARU_DB_PATH`（`.playwright-tmp/e2e-user.db`，绝不碰真实 `data/`）与显式 `JWT_SECRET`；testMatch 限定 `**/*.spec.ts`，与 Node test runner（tests/*.test.ts）互不干扰。
- `e2e/seed-auth.ts`：`node --experimental-strip-types` 直接 import `src/lib/auth`（createUserDb/createAuthService），`wechatLoginOrRegister` 后从 refresh payload 解 sub/sid 签过期 access，供用例⑤种子会话。
- `e2e/helpers.ts`：pngBuffer/pngDataUrl（≥320px）/1×1 占位。
- `.gitignore` 追加 `/test-results/`、`/playwright-report/`、`/.playwright-tmp/`。

### 六条用例与首轮→二轮

①编辑器 25 笔重置回原图基线（toDataURL 逐字节）＋撤销/重置后可用；②4096² 原尺寸导出（PNG 签名＋IHDR 宽高 4096）；③3:4 参考图拦截 `/api/generate-ai` 请求体断言 `ratio:"3:4"`（付费 API 全 mock）；④mock 640×640 生成图→图库收录→`/ar-tryon?collect=` 桥接；⑤种子会话过期 access→`/api/me` 200＋Set-Cookie 新 access（refresh 静默续期端到端）；⑥AR 纹理上传→裁剪→确认→双指→应用到全部→五指计数与逐指文案。首轮 4/6 的三处修正：④「我的收录」命中 2 元素（strict mode violation）改断言收录卡链接可见性（更强）；1×1 mock 图过不了 AR 侧 320px 分辨率门改 640×640；⑥暴露**真实应用 bug**——`TextureCropper` 加载 effect 在 StrictMode 双挂载下 cancelled onload 吊销两次挂载共享的 blob URL，致「确认使用」时 `extractTexture` 重新加载失败、`onConfirm` 永不触发；删除两处吊销（URL 生命周期归页面 `prepareUploadUrl` 管）后 dev/生产行为一致。

### 验证（全部实测）

- Playwright **6/6 全绿**（20.9 秒）；ESLint 全项目 0 错误 0 警告（修 1 处新文件 unused-page warning）；`tsc --noEmit` 171 处与 v1.1.649 基线一致零新增；全量 `npm.cmd test` 串行 **979/979**（0 失败 0 跳过，12m34s，TextureCropper 变更后复验）；`git diff --check` 干净；测试文件实测 Node 214＋e2e 6 用例。

### 文档登记（v1.1.659）

- 白皮书：头部 658→659；§13 表首新增条目（WorkBuddy 署名）；§9.7 v1.1.658 更正段后追加 v1.1.659 更正（「工程与维护」行浏览器回归缺口闭合；CI 工作流与 scripts 类型检查留待后续）。
- README：验证命令块测试数 163→214 并补 `npx.cmd playwright test` 行；目录树 tests/ 后加 e2e/ 行；文档索引 658→659。
- 全部串行 Edit＋grep 实测落盘。

### 遗留

- CI 工作流、scripts 类型检查（评审 #11 关联工程项，已在 §13 范围声明留待后续）。
- 短信供应商适配（需用户提供凭据）。未 commit/push（未经用户要求）。

> 本条目由 **WorkBuddy** 编写（2026-09-16）。
