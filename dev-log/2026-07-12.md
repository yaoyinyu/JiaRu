# 2026-07-12 开发日志

## 技术白皮书初始化

- 创建 `docs/technical-whitepaper.md`，作为各功能模块对接、接口契约和使用方式的统一维护入口。
- 盘点并标记首页、图库、图片试色、AI 生图、AR 试戴、纹理自动识别、模型产物、数据集、训练与发布治理的当前状态。
- 明确区分已完成、待验证、进行中、占位、未完成和阻塞，避免将合成/烟雾模型误认为正式模型。
- 整理 `POST /api/generate-ai`、浏览器纹理识别、Worker、ImageBitmap 纹理、模型 manifest、数据集和训练脚本接口。
- 在 `AGENTS.md` 增加持续维护规则：每项影响接口或功能的任务结束前必须更新白皮书，同一天日志只使用一个文件。
- 在 `README.md` 增加白皮书与每日开发日志入口，便于后续开发者快速定位。
- 验证：文本编码审计通过（292 个文件，0 个失败），本次文档差异检查通过。

## 技术白皮书全面审查与强制门禁

- 逐项对照当前源码、`package.json`、环境变量示例、模型文件、数据集 readiness 和正式模型最终审计复核白皮书。
- 将白皮书升级为 v1.1.0，新增信息来源优先级、冲突处理规则，以及“任务开始前必读、任务结束前必更新”的完整门禁。
- 修正三条数据流被误画为同一 AR 链路的问题，明确 AI 生图、图库、编辑器和 AR 当前没有自动互通。
- 补充编辑器实际接受 `image/*` 且缺少运行时文件校验、图库 `gallery` 参数未消费、AR 单手和 jsDelivr CDN 依赖等限制。
- 明确 `.env.local.example` 当前会启用 smoke 模型，正式 ONNX 仍缺失；补充模型 backend、Worker 降级与 warning 读取要求。
- 进一步按真实 TypeScript Props 修正 `NailCanvas`/`ArView` 接口：当前数组长度和手指索引未由类型系统强制；区分 Worker 超时 fallback 与取消抛出 `AbortError` 的不同语义。
- 补齐 `TextureCropper`、`NailArtPicker`、`NailAssignment`、Worker 主入口及 ImageBitmap 所有权契约；记录 `ambiguous` 朝向仍允许绘制的 fail-open 风险和识别输入/候选/超时参数。
- 将隐私说明调整为待验证，记录“保存试戴效果图”文案领先于当前 AR 实现及外部资源请求边界。
- 更新 `AGENTS.md`：任何任务开始前必须读取并核对白皮书，结束前必须同步白皮书变更记录和当天唯一开发日志，未同步不得声明完成。
- 增加可观察凭证：首次进度更新须点名已读取的白皮书章节和状态，最终答复须点名已同步的白皮书变更记录与当天日志。
- 最终验证：6 个页面路由和唯一 HTTP API 均被白皮书覆盖；框架版本、正式模型缺失、blocked 状态及 smoke 配置说明与当前文件一致；`git diff --check` 通过；文本编码审计通过（294 个文件，0 个失败）。本次没有运行时代码变更，因此未重复执行功能测试或真机测试。

## 端侧实施技术规范评审

- 评审 `docs/nail-texture-local-inference-implementation-spec.md` v1.0，并对照纹理识别 runtime、预处理源码、正式 manifest、训练报告和实施进度档案核对事实。
- 结论：产品边界、端侧架构、数据治理、分阶段质量/性能门禁和回滚框架完整，可继续作为目标实施规范。
- 发现文档的第 3.2 节及第 16–17 节存在状态滞后：Worker 环境识别、ORT 静态导入和 letterbox 已实现，不能继续列为未完成；真实数据工作也已进入部分实施。
- 正式 ONNX 仍缺失，生产模型状态保持 blocked；独立真实测试集、真机性能和正式发布证据仍未闭环。
- 白皮书同步为 v1.1.1，新增“9.2 端侧实施技术规范复核”和对应变更记录；本次未修改被评审规范及任何运行时接口/状态。
## 实拍素材目录纠正与审核标注入库

- 核验 `真实素材/2027_7_11` 已迁移到 `真实素材/2026_7_11`：新目录 21 张与首批有效素材逐文件 SHA-256 一致，缺少项仅为用户明确永久排除的 `ca2d...jpg`。
- 更新首批当前 intake/selected manifest 的来源路径，并从选中图片及有效数据集的图片、原始标注、YOLO 标签、来源表和 split 中移除禁用图片；历史审计报告保留原路径，作为当时事实，不参与训练读取。
- 将 19 张视觉审核通过标注覆盖进 `nail-texture-v1`，共 156 个甲面；重建 `sources.csv`、split 和 YOLO segmentation 标签。有效集更新为 322 图、1577 个 mask，split 为 231/45/46。
- 新增 `真实素材/2026_7_12` 113 张独立实拍素材已完成语料和视觉初筛：113/113 可解码、无完全重复，112 张正样本、1 张 hard negative，18 张社交界面/拼图素材预留测试复核；尚未把 fallback 候选作为真值导入。
- 验证：来源审计 `ok=true`、0 问题；标签审计 0 错误、1 条允许的 `empty_negative_sample` warning；19 份 accepted 标注与有效数据集 SHA-256 全部一致。白皮书同步为 v1.1.2，正式模型状态仍保持 blocked。
## Git 推送失败诊断

- 核对本地 `master` 比 `origin/master` 领先 1 个提交；远端仍为 `8cf6956...`，本地待推送提交为 `7dbb661...`，确认失败推送未落到远端。
- 待推送提交包含 `weights/clip/ViT-B-32.pt`（353,976,522 字节），超过 GitHub 单文件 100 MB 限制；还包含大量调试图和模型资产，连接重置不是单纯扩大 HTTP buffer 可以根治的问题。
- 建议在未推送提交上用 `git rm --cached` 移出权重和生成资产，补充 `.gitignore` 后 amend；确需版本化的模型权重改用 Git LFS 或外部模型存储。
- 白皮书同步为 v1.1.3；本次仅记录仓库治理风险，无运行时接口或功能状态变化。

## Git 历史清理与大文件排除

- 经用户确认，对尚未推送的本地提交历史进行清理：以 `origin/master` 为父提交，将原有两次本地提交重写为一个干净提交，并保留 `refs/codex/backup/pre-asset-clean` 作为本地恢复引用。
- 更新 `.gitignore`，排除模型权重、训练实验、运行输出、调试目录，以及辅助材料中的图片和覆盖图；这些文件继续保留在本机，不再进入后续提交。
- 使用 `git rm --cached` 移除 `weights/clip/ViT-B-32.pt` 的 Git 跟踪，本地 353,976,522 字节权重文件未删除。
- 复核范围：相对 `origin/master` 的提交差异不含图片文件，新增 Git 对象无超过 100 MB 的文件；本次没有改变运行时接口或功能状态。
- 白皮书同步为 v1.1.4，新增“Git 历史清理与大文件排除”变更记录。

## 新增实拍素材 v3 辅助标注复核

- 重新核对正式数据集：首批 21 张授权实拍图、174 个审核甲面已全部导入；正式有效集当前为 322 图、1585 个 mask，原进度文档中的 19 张/156 个和 1577 个 mask 已过期。
- 使用 `nail-texture-seg-real-prelabel-v3` 以 conf=0.35 对新增 113 张素材生成审核候选：109 张有候选、4 张无候选，共 694 个候选；产物明确保持 `candidate_only_not_training_truth`。
- 对原 63 张返修项完成整批高分辨率 contact sheet 视觉复核：conf=0.35 提升 13 张，随后以 conf=0.20 高召回候选再提升 8 张；所有提升项均覆盖全部可见甲面且无明显背景污染。新增批次隔离审核包现为 71 图（70 正样本、1 hard negative）、471 个 mask，剩余 42 张继续返修。
- 对隔离审核包运行 `audit-labels.ts`：71 文件、0 错误、1 条允许的空负样本 warning，审核通过。
- 新增批次尚无明确商业训练和长期回归授权，已登记 `USER-AUTH-02`；未确认前不导入正式数据集、不作为正式模型或发布证据。
- 白皮书同步为 v1.1.5；正式模型状态仍为 blocked，无运行时接口变化。

## real-prelabel-v3 隔离 ONNX 工程验证

- 将辅助标注模型导出为 512 FP32 ONNX；文件 11.03MB，低于 15MB MVP 上限，SHA-256 与 manifest 一致。
- ONNX Runtime 实际执行成功，输出协议为 `[1,37,5376]` 与 `[1,32,128,128]`；`verify-real-model-readiness.ts` 构建并验证 fixture，解码得到 5 个带 mask 候选。
- 本项仅做必要工程快检并记为“辅助标注用途 PASS”；因第二批素材正式授权未确认，产物保留在 Git 忽略的 `model/exports/`，不得进入正式注册或 promotion。
- 白皮书同步为 v1.1.6；正式模型状态继续 blocked。

## real-seed-v1 正式候选质量快检

- 使用原始 `model/training/dataset.yaml` 对仅基于当前授权正式集训练的 real-seed-v1 执行 512、test split 评估；46 张图片、221 个实例。
- 结果：box mAP50=0.380、mask mAP50=0.367；相对 512 基线分别下降 0.143 和 0.101，超过 0.02 退化上限。
- `assess-model-metrics.py` 返回 `ok=false`，该候选按门禁拒绝，不继续 ONNX 导出、注册或发布；标记 `M2-T6-SEED-CANDIDATE` 为“PASS（拒绝候选）”。
- 白皮书同步为 v1.1.7；正式模型继续 blocked。

## 实施规范清单状态回填

- 对照进度标记和当前机器证据，回填 `docs/nail-texture-local-inference-implementation-spec.md` 第 16 节复选框。
- 已完成项包括 MVP 范围、目标设备、素材数量，以及 Worker/ORT、真实 ONNX 冒烟、协议、letterbox、后处理、内存、fixture、FP32 尺寸评估、量化决策、发布门和 fallback/回滚共 11 项工程能力。
- 新增批次授权、真实数据闭环、真机报告、失败样本和 Beta 人工质量审核保持未完成，不用已有工程测试冒充真实验收。
- 白皮书同步为 v1.1.8；无运行时接口变化。

## 第二批授权导入与真实候选 v6 验证

- 用户选择 A，确认 `真实素材/2026_7_12` 的 113 张素材可用于商业模型训练和长期回归；两个批次 manifest 的授权字段已更新。
- 扩展 `import-reviewed-batch.ts`，支持按审核 CSV 只导入 `pass` 行及指定审核标注目录；71 张通过项（70 正样本、1 hard negative）和 471 个 mask 正式入库，42 张返修项保持隔离。
- 发现整批共用单一 `sourceGroup` 会使 split 偏离 70/15/15；导入器改为按 deerplanet/more/other 集合隔离来源，重建后正式集为 393 图、2056 mask，split=289/45/59，来源授权、标签、split 比例和 release readiness 全部通过。
- 新增 `build-real-source-isolated-dataset.py`，建立 92 图/645 mask 的无泄漏真实实验集：train/val/test=66/13/13，deerplanet 仅进入 test，more 仅进入 val。
- v4 混合续训候选独立 test box/mask mAP50=0.429/0.397，拒绝；v5 来源隔离候选为 0.848/0.836，box 略低于 0.85，拒绝；v6 以 640 训练、512 部署评估达到 0.853/0.848，通过候选精度门。
- v6 FP32 ONNX 为 11,566,102 字节（11.03MB），SHA-256 `d122da819a4b1c70a954a55d84b26ed53fc33fb9bd8374a9e69c0e106909e57d`；资产完整性、ORT `[1,37,5376]`/`[1,32,128,128]` 输出及 TypeScript 7 候选 fixture 均通过。
- Chromium + WebGPU 采集 29 次热推理，端到端 P50/P95/最大值=108.9/133.7/159.5ms，Worker P95=100ms，0 控制台错误/警告；性能门通过。
- 发布结论保持 HOLD：独立真实 test 只有 13 张，尚未达到规范建议的 100–200 张，且 Android/iPhone/iPad 真机、峰值内存和 Beta 人工直接可用率未验收。本次未覆盖生产 manifest。
- 同步白皮书 v1.1.9 的状态总表、数据集/训练接口、v4–v6 结论、已知限制和变更日志；同步实施进度与规范清单。

## v6 返修标注审核与 v7 冻结测试复评

- 将第二批剩余 42 张返修图单独整理，使用 v6 权重以 640、conf=0.20、maxDet=20 生成 317 个候选；新增通用分页联系表工具并输出 5 页高分辨率审核材料。
- 逐图检查所有可见甲面、重复 mask、合并 mask 与背景污染；仅 5 张/27 mask 满足严格提升标准，37 张继续返修。新增 `promote-reviewed-prelabels.py`，只把审核 CSV 中 `pass` 且存在于候选目录的标注转为训练真值，并清除 `reviewRequired`。
- 第二批累计 76 张通过（75 正样本、1 hard negative）、498 mask；正式数据集更新为 398 图/2083 mask，split=292/45/61。导入报告、来源授权、标签、split 比例、Phase 1 与 release readiness 全部通过。
- 重建来源隔离实验集为 97 图/672 mask，train/val/test=69/15/13；deerplanet 13 图/102 mask 保持冻结 test，more 15 图仅进入 val，未发生来源泄漏。
- v7 从 v6 权重续训，16 epochs early stop，最佳 epoch=1；冻结 test 的 box/mask mAP50=0.840/0.833，box 低于 0.85，且均低于 v6 的 0.853/0.848。发布门拒绝 v7，未导出 ONNX、未注册、未发布，v6 保持最佳候选。
- 用户授权充分使用本机 CPU/GPU/内存后，对剩余 37 张继续执行 v6 1024/conf=0.10、maxDet=30 推理，生成 458 个候选并完成五页联系表审核；低阈值带来的重复、皮肤污染和拼图误检抵消了小甲面召回，0 张新增提升。自动返修路线到达收益拐点，剩余项保持人工返修。
- 白皮书同步为 v1.1.10，更新状态总表、数据 readiness、v4–v7 结论和变更记录；实施进度及规范清单同步为 76 张通过、37 张返修。
