# 2026-07-21 开发日志

## 规范val30部署512评估与阈值校准

- 继续执行端侧实施规范，完成规范val30到真实部署512评估和阈值校准的工程桥接。正式证据只接受`canonical_validation_dataset_materialized_pending_role_isolation_audit`及`approved_as_calibration_truth`，旧`experiment_only_source_isolated_real_dataset`固定为诊断用途且不能生成manifest阈值。
- `calibrate-model-score-threshold.py`会调用validation最终审计器，独立重放truth index、物化报告、角色隔离、30张图片、30份annotation/label、144个完整mask、polygon合法性、同图严格零交叠及当前文件哈希；伪造终审PASS、交叉角色泄漏、标签/制品漂移、孤儿或未知预测都会拒绝。
- `evaluate.py`绑定dataset YAML、权重、评估制品索引及逐文件SHA-256，并为每张评估图显式记录预测；零预测必须以`path=null`、`prediction_count=0`登记，不能从清单消失。所选split在加载Ultralytics前递归核验非空，既有metrics/runtime/artifacts不能复用。
- 第一次真实评估发现Ultralytics在规范源数据`labels/val`写入`val.cache`。校准终审以孤儿文件正确拒绝；该cache移动到失效证据目录隔离保存，规范源数据重新终审为30/30、144 mask、0 invalid/overlap/orphan。评估器随后改为将所选split逐文件复制到输出侧runtime dataset，禁止硬链接，并在物化后及推理结束后重算源树路径与逐文件SHA。
- 第二次真实评估发现Windows `Path`排序与规范POSIX相对路径字符串排序不一致，校准器再次正确拒绝。评估器改为统一按POSIX相对路径字典序生成`files`与`file_records`，失效运行另目录保留，不覆盖。
- 第三次从全新证据目录运行成功。v6权重SHA-256为`48f13b2b…e535`，规范dataset YAML SHA-256为`991dea6f…e544`；源树推理前后inventory SHA-256均为`a9d64857…c930`，源`val.cache`不存在。
- v6在30张/144 mask、部署512上的box/mask mAP50为0.6241/0.5630，mAP50-95为0.3436/0.2789。confidence=0.05时召回0.7569，但每图误检9.8333、单图最多28候选；0.10—0.50召回均低于0.75。因此0.05—0.50没有阈值同时满足召回≥0.75、每图误检≤1、单图候选≤10。
- 正式校准报告决定为`no_threshold_meets_validation_constraints`，`calibrationEligible=false`、`manifestScoreThreshold=null`。该结论证明工程校准门可用，同时拒绝v6依靠降阈值进入manifest；冻结test拒绝结论和生产manifest均未改变。
- 新增进度标记`M2-T6-CANONICAL-VAL30-CALIBRATION`：状态为工程门PASS、v6阈值拒绝。候选训练仍因hard negative仅1/100、缺99张而HOLD；该用户输入继续跳过，不使用模糊、残缺、拼图、模板或独立甲片凑数。

## 证据路径与哈希

- 有效证据根目录：`E:\AI Project\Codex\JiaRu_image\审核工作区\2026_7_14_A_v1\val-annotation-workspace-v1\v6-deploy512-calibration-evidence-v1`。
- `val-metrics.512.json` SHA-256：`af8dd020406b27ed5935fa75e436e9a7b2e84cc3a24893e7ce574967858de0d7`。
- `evaluation-artifacts.json` SHA-256：`7f1a9f16631bacb76d8249bf5043c1391d2bb73a931bcf7f4f6f49d82649ae9c`。
- `score-threshold-calibration-v1.json` SHA-256：`7586926f50958f90ae6a5d5e33da6b7c2697e9ac425591c7394592e6f7be275e`。
- 两个失效运行分别保留在`v6-deploy512-calibration-evidence-v1-invalidated-source-cache`和`v6-deploy512-calibration-evidence-v1-invalidated-path-order`，不得作为阈值或发布证据。

## 当前边界

- 本阶段没有启动新模型训练，没有修改生产manifest，没有把val用于冻结test发布质量，也没有把冻结test用于阈值选择。
- 素材、运行副本、预测与校准JSON均位于Git外审核工作区；仓库只保留代码、测试和状态文档。
- Codex未执行`git commit`或`git push`。工作期间用户侧已有提交到达`master`，本阶段继续只保留当前本地差异。

## 文档同步

- 白皮书更新至v1.1.195：状态表新增规范val30校准门，§7补只读源数据/制品契约和真实v6结果，§8.1补评估/校准接口，§11.1纠正训练启动剩余缺口，并追加变更记录。
- 实施规范工程清单新增规范val30桥接完成项；进度文档更新日期至2026-07-21并生成`M2-T6-CANONICAL-VAL30-CALIBRATION` PASS标记。

## 验证

- 校准器与Python训练脚本专项：25/25 PASS；覆盖规范终审深重放、伪造PASS、跨角色泄漏、标签/制品漂移、孤儿、输出覆盖、零预测、空split、只读runtime副本和Windows稳定排序。
- 首次将全量测试、构建和lint并行运行时出现3个与本阶段无关的文件系统/子进程敏感用例失败；三个失败文件随后串行7/7通过。为避免把资源竞争误判为代码回归，最终验收按串行执行并新增AGENTS持久规则。
- `npm.cmd test`串行全量：523/523 PASS。
- `npm.cmd run lint`：PASS。
- `npm.cmd run build`：Next.js 16.2.9生产构建与TypeScript检查PASS，`/device-benchmark`等12个页面/路由生成成功。
- `npm.cmd run audit:nail-texture-completion`：按预期退出1并输出`decision=hold`；328个进度标记、316个PASS、2/10完整发布门通过。阻塞码为用户失败案例、模型质量回归、代表性发布测试、移动真机和Beta质量审核。
- `npm.cmd run audit:encoding`：428文件、0失败。
- README严格UTF-8、0 NUL、12个代码围栏成对、31个标题、6个表格分隔行、末尾换行全部通过；`git diff --check`通过。
- 全部验证均未执行`git commit`或`git push`。

## GitHub 443连接超时只读诊断

- 用户执行`git -c core.hooksPath=NUL -c core.fsmonitor= push --porcelain origin`时，在21.595秒后收到`Failed to connect to github.com port 443`。错误发生在建立HTTPS连接阶段，尚未进入鉴权、分支更新或对象传输，因而不是提交信息、分支名称、GitHub贡献图或仓库大文件错误。
- 只读复核确认当前分支为`master`，远端地址为`https://github.com/yaoyinyu/JiaRu.git`；`github.com`解析到`20.27.177.113`，GitHub首页HTTPS返回200，`git ls-remote --exit-code origin HEAD`约1秒成功返回`fae266b78fdf9875b3a5465b1e054578d5ac7262`。
- 当前没有`HTTP_PROXY`/`HTTPS_PROXY`环境变量，WinHTTP为直连；GitHub官方状态页显示Git Operations正常。综合判断为推送发生时的瞬时网络链路超时，目前Git智能HTTPS握手已恢复。
- 本轮未执行`git commit`、`git push`、`git fetch`，也未修改Git代理、TLS、凭据、分支或远端配置。用户可直接重试原推送；若仍偶发，先再次运行`git ls-remote origin HEAD`，成功后再推送，不应关闭TLS校验或重复改分支。

## Git网络诊断文档同步

- 白皮书更新至v1.1.196：在§9已知限制补充2026-07-21同类超时复核结果，并追加v1.1.196变更记录；本次无模型接口、训练状态或生产manifest变化。

## 候选训练、val校准与冻结test三路编排

- 将候选发布编排收紧为三套互斥dataset：候选训练集只进入训练，规范val只用于阈值校准，冻结release-test只用于最终质量判定。候选模式新增六个强制参数，禁止显式通用`--split`、三路同路径、`--skip-evaluate`、`--skip-export`和无绑定的`--skip-train`恢复。
- 固定执行顺序为候选输入预检、冻结test预检、训练环境门、训练、规范val评估、阈值校准、冻结test评估、候选ONNX导出和最终release verifier。val与test使用独立metrics/runtime/artifacts目录；dry-run只输出全计划，不启动Python、Ultralytics或GPU。
- `materialize-frozen-release-test-evaluation.py`报告升级为schema v2：绑定冻结manifest路径/SHA/`itemsSha256`、训练sources身份、三份dataset YAML、评估manifest、core/stress逐项记录及递归逐文件/聚合SHA；test必须非空，train/val必须为空，输出与报告事务式提交且不可复用。
- 新增`verify_evaluation_report()`及`--verify-report --expected-dataset`，重新读取冻结manifest、训练sources、图片、annotation、YOLO label、polygon合法性、任意非零交叠、三类隔离和当前输出树；伪造外层PASS、写后漂移或dataset错配均拒绝。
- `calibrate-model-score-threshold.py --verify-report --expected-weights`会重放规范val全证据；`export-onnx.py --candidate-mode --calibration-report`禁止手工`--score-threshold`，阈值只能从ready且权重一致的报告派生，manifest新增`scoreThresholdEvidence`绑定报告、dataset、metrics、artifact index和weights SHA-256。
- `verify-training-release.ts --candidate-mode`要求release metrics严格为test，重算冻结test dataset哈希、不可变inventory、评估制品清单与逐文件SHA、每图显式预测记录和聚合哈希，再核对校准报告、权重、manifest阈值及证据字段。val metrics替换、权重漂移、报告伪造、预测制品漂移或手工阈值均不能通过。

## 三路编排验证与边界

- 联合专项串行63/63 PASS；覆盖冻结test真实物化/深验、伪造PASS、label漂移、dataset错配、来源重叠、任意非零polygon交叠、校准证据漂移、非ready决定、权重错配、候选导出、三路CLI约束、skip/resume、val/test互换及最终manifest证据漂移。相关Python脚本`py_compile`通过。
- `npm.cmd run audit:nail-texture-completion`按预期退出1并刷新证据为329个进度标记、317个PASS、2/10完整发布门通过，决定继续`hold`；阻塞仍为用户失败案例、模型质量回归、代表性发布test缺33张、四类移动真机和Beta质量审核。
- 首次全量`npm.cmd test`并行执行为540/541：旧候选输入测试仍尝试已禁止的`--skip-evaluate/--skip-export`并解析早期CLI拒绝的空stdout。该恢复训练断言已由新的三路流水线专项完整覆盖，因此从输入审计用例移除重复历史分支；随后单文件10/10通过。
- 为落实`AGENTS.md`既有串行验收规则，将`package.json`的测试入口固定为`--test-concurrency=1`。重跑全量541/541 PASS（约216秒），不再依赖操作者记忆额外参数。
- 最终`npm.cmd run lint`、Next.js 16.2.9生产构建与TypeScript检查通过，12个页面/路由生成成功；`npm.cmd run audit:encoding`为428文件、0失败。
- 新增进度标记`M2-T6-CANDIDATE-THREE-WAY-PIPELINE`为工程门PASS。v6规范val报告仍是`no_threshold_meets_validation_constraints`，hard negative仍仅1/100且缺99张，因此未启动训练、未生成候选ONNX、未修改生产manifest，也未使用冻结test调阈值。
- 白皮书更新至v1.1.197：同步功能状态表、§8.1数据集契约、训练脚本接口、§11.1阻塞项及v1.1.197变更记录；实施规范和进度表同步三路工程门。
- 本阶段未执行`git commit`或`git push`；共享工作区中的外部提交继续视为用户侧操作。
