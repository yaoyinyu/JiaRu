# 2026-09-11 开发日志

## 提交推送 2026-09-10 全天工作区差异并补正 README 循环013 状态（白皮书 v1.1.635）

用户 08:36 发起「提交并推送」。任务开始前读取白皮书 §13（至 v1.1.634）、`dev-log/2026-09-10.md` 与 README，确认 Codex 与 WorkBuddy 的 v1.1.629—v1.1.634 工作已完整记录（含 v1.1.630 的 26 项盘点与"长时间未提交"风险提示）。

### 并发安全确认

Codex 最后活动 09-10 22:21，提交时 09-11 08:37，**静止约 10 小时 16 分钟**，未收编在制半成品。

### 收编清单（31 项 = 15 修改 + 16 新增，git status 实测）

**新增 16**

- 循环012：`audit-development-cycle-012-positive-truth.py`、`build-development-cycle-012-decision.py`、`materialize-development-cycle-012-dataset.py` + 3 个专项测试
- 循环013：`analyze-development-cycle-013-candidate-ceiling.py`（1221 行）、`materialize-development-cycle-013-roi-dataset.py`、`nail_texture_cycle013_mask_replacement.py`、`verify-development-cycle-013-plan.py`、`build-development-cycle-013-closure.py` + 4 个专项测试
- 文档：`docs/training-throughput-speedup-measures.md`

**修改 15**

- `README.md`、`docs/technical-whitepaper.md`、`docs/nail-texture-current-goal.md`、`docs/nail-texture-completion-audit.md`、两份实施文档、`dev-log/2026-09-10.md`
- 5 个训练脚本：`audit-development-cycle-012-source-selection.py`、`audit-real-material-yolo-prelabels.py`、`build-sam-prompts-from-annotation-workspace.py`、`generate-yolo-prelabels.py`、`train-yolo-seg.py`
- 3 个测试：`build-sam-prompts-from-annotation-workspace.test.ts`、`generate-yolo-prelabels-workspace.test.ts`、`model-training-python-scripts.test.ts`

### README 补正（两处滞后）

1. **文档索引** v1.1.629 → v1.1.635（Codex 在 v1.1.630—634 期间未再更新该行，与 09-10 补正过的"分层滞后"同模式）。
2. **Phase 4 路线图**：循环013 条目由「未完成」改为「已证伪关闭」，补入可达性判决核心数字（21 枚漏甲仅 5 枚有可附着候选、双轴门不可达、瓶颈=候选供给）与 Wilson 分辨率结论（58 图规模下相对信号在噪声内、需 932 张/组）；新增吞吐诊断条目（`workers: 0→8` 提速约 22%、措施固化文档）与循环014 里程碑（候选供给侧单变量）。
3. **模型状态段**：循环013 从「唯一活动里程碑」改为「已证伪」，下一活跃里程碑改为循环014。

### 本轮收编的技术结论（来自 v1.1.629—634）

- 循环012 唯一训练在 clean98 上退化（25 漏甲、18 漏甲图、召回 `0.92937853`、杂散率 `0.12429379`），五项相对信号全部失败，配方关闭不晋升。
- 循环013 在消耗唯一训练/推理预算前先做**零推理可达性判决**：21 枚漏甲中仅 5 枚有可附着候选、16 枚按构造无候选；即便 stage2 成功率 100% 且 mask 完美，漏甲图 13→11（门≤5）、杂散率 `0.0960452`→`0.07627119`（门≤0.02）双轴仍不可达，判决 `unreachable_by_mask_replacement_stage2`——瓶颈是 stage1 候选供给，不是替换成功率。
- 报告验证器升级为逐项绝对路径+SHA-256 深重放：重建 98 图接受候选集、1190 项逐图逐实例同构检查 0 不一致。
- 训练吞吐诊断：稳态 34.41 s/epoch（训练 26.06 + 验证 8.35）；`workers: 0→8` 为唯一零语义代价杠杆（预计提速约 22%）；验证段约 94% 成本来自指标与绘图而非推理；措施固化文档明确 `workers` 位于冻结合同内、属受控变更。
- 证据完整性事件：独立基准脚本未装只读守卫误改 10 张 JPEG，已按 SHA 逐文件恢复并以权威重放确认 `verified`；守卫要求升级为 AGENTS.md 持久规则。

### 验证

- `npm.cmd run audit:encoding`：1011 个文本文件、0 失败。
- Python 3.13 置于 PATH 首位后专项测试全过：循环012/013 共 8 个测试文件 **50/50**（含 `model-training-python-scripts` 既有合同测试）。
- `git diff --check` 通过（仅既有 LF/CRLF 提示）。

### 提交与推送

- 提交标题：「提交循环012关闭与循环013可达性证伪判决，吞吐诊断与提速文档，白皮书至v1.1.635」。
- 推送沿用既定模板（`GIT_CONFIG_SYSTEM` 指向空 gitconfig、清空代理、`credential.helper=manager`、`http.schannelCheckRevoke=false`），一次成功；推送后按已知现象重建被抹掉的 `.git/refs/remotes/origin/master` 跟踪引用。

> 本条目由 **WorkBuddy** 编写（2026-09-11）。
