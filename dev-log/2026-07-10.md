# 开发日志 2026-07-10

- 清理 AR 页面等位置遗留的 Git 合并冲突标记，并完成主要页面构建与访问验证。
- 审计旧模型 Demo：确认 `localhost` 拒绝连接源于 8080 Python 服务未运行；旧 Demo 未实际调用 `nail_segmenter`，训练与验证数据重合且标注质量不足，因此判定不适合正式使用，并按要求暂停新模型训练。
- 修复 AR 试戴手心/手背提示文字、图标和状态颜色显示相反的问题，不改动现有识别与美甲渲染门控逻辑。
- 新增动态视频展示窗口策略：窗口比例跟随网页可视区域，摄像头画面采用等比 `cover` 缩放和居中裁切，AR 画布复用同一坐标转换，避免拉伸变形与贴图错位。
- 完成横屏 `2560×1440` 与竖屏 `1080×1920` 浏览器验证；视频窗口分别保持 `16:9` 与 `9:16`，视频和画布尺寸一致且页面无控制台错误。ESLint、编码检查、273 项自动测试和 Next.js 生产构建均通过。
- 修复 AR 页面 9 处因历史编码问题被替换成 `??` 的图标文字，恢复标题、摄像头入口、纯色/纹理模式、上传入口及隐私提示的正常显示。
- 完善动态视频居中裁切：显式锁定视频 `object-position: 50% 50%` 与镜像变换中心，横向溢出由左右边界各裁一半，纵向溢出由上下边界各裁一半，并补充双边裁切量相等的回归测试。
- 根据实拍手心画面修复朝向标签：移除 UI 层错误的手心/手背反转，恢复 `palm=手心`、`dorsum=手背` 的语义映射，并新增独立映射函数与自动测试。
- Fixed orientation being stuck on palm: replaced handedness-coupled 2D scoring with symmetric palm/finger depth evidence and added 3-frame switching stability (5 frames for ambiguous state).
