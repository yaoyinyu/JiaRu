# 2026-07-20 开发日志

## 正式候选训练启动距离复核

- 重跑`npm.cmd run audit:nail-texture-completion`，报告按预期为`HOLD`：323个进度标记、311个PASS，完整发布门2/10通过。
- 区分训练与发布口径：2/10衡量完整发布闭环，不代表训练准备度20%。候选训练三类最低图片数量为train 100/100、val 30/30、hard negative 1/100，合计131/230，约57%。
- train已有100张/521个完整mask，但仍为`prohibited-until-materialization-audit`；val已有30张/144个完整mask并通过规范物化、校准真值终审及train/冻结test来源隔离；hard negative仍缺99张且唯一候选尚未正式物化。
- 正式候选训练还需：补齐并审核99张合格hard negative；建立完整候选训练包物化与来源隔离终审；生成可供`--candidate-mode`消费的新val桥接报告。
- 发现代码治理缺口：当前候选训练入口尚未强制train不少于100张和hard negative不少于100张。该缺口不得用于绕过白皮书训练门，后续工程应补齐规范候选训练数据集物化器和完整训练输入审计。
- 33张代表性发布test、Android手机/平板、iPhone、iPad真机、用户失败案例和100张Beta人工审核继续阻塞正式发布，但不单独阻塞下一轮候选训练。
- 同步白皮书v1.1.190：更新模型发布治理行、纠正§11.1旧的train 53/val 0快照，并追加2026-07-20变更记录。
- 本次未运行模型训练，未修改生产manifest，未执行Git提交或推送。

## 验证

- `npm.cmd run audit:nail-texture-completion`：按预期退出1并输出`decision=hold`；报告已刷新至2026-07-20，323个进度标记/311个PASS、2/10发布门。

## Git分支恢复为master

- 用户明确要求将本地与远端分支名称恢复为`master`；暂停候选训练输入门工程实现，并终止对应并行子任务，避免在分支治理期间产生交叉修改。
- 操作前本地、远端唯一分支和GitHub默认分支均为`JiaRu_让每一次抬手都遇见未来`，提交为`41d5074`。
- 本地分支已重命名为`master`并建立`origin/master`上游；GitHub默认分支经API更新为`master`；远端旧中文分支已删除。
- 复核`git remote show origin`、`git ls-remote --symref origin HEAD`与`git ls-remote --heads origin`：远端HEAD指向`refs/heads/master`，远端仅保留`master`，本地与远端均为`41d5074`且状态同步。
- 本次没有创建新提交；只进行了用户明确要求的分支推送、默认分支切换和旧远端分支删除。分支改名不会单独决定GitHub贡献图，贡献仍依赖提交作者邮箱关联、默认分支可达性及统计延迟。
- 同步白皮书v1.1.191变更记录；模型接口、训练数据、训练HOLD与生产manifest均无变化。

## GitHub 443超时只读复核

- 用户执行`git push`时收到`Failed to connect to github.com port 443 after 21595 ms`。只读复核显示DNS解析正常、TCP 443连通、`https://github.com`返回HTTP 200，`git ls-remote origin`亦可读取远端。
- 报错后的本地`master`、`origin/master`与远端HEAD已包含用户刚提交的`0ff9896`，说明该次操作虽然客户端报告连接超时，但提交实际已经到达GitHub；结论为瞬时网络/连接复位，而非分支名、仓库权限或提交内容失败。
- 收尾只读刷新时，本地HEAD、`origin/master`和`git ls-remote origin refs/heads/master`又一致指向`63eb39c`，进一步证明后续远端连接已经恢复；Codex没有为此执行额外push。
- 发现全局Git配置存在`http.sslbackend=openssl`与`http.sslverify=false`；后者会降低TLS校验安全性，但与本次已成功到达远端的超时不是同一根因。本轮只读诊断未修改用户Git配置，也未代替用户再次提交或推送。

## 规范候选训练数据与输入门

- 新增`materialize-canonical-candidate-training-dataset.py`：绑定训练真值唯一索引、正式hard-negative manifest、规范val-only数据集及终审、冻结test；在创建输出前固定核验train正样本≥100、正式hard negative≥100、val≥30，CLI不能下调。
- 物化契约固定train为“批准正样本+零字节标签hard negative”、val为规范校准真值、test为空，并记录`records`、`datasetFiles`、角色身份、四角色交集和聚合SHA-256；目录采用临时目录完成后原子替换，失败不得留下半成品。
- 新增`audit-candidate-training-input.py`：不信任物化报告外层PASS，独立重算训练annotation→YOLO标签、val逐项等价、polygon合法性/严格零交叠、负样本空标签、孤儿文件及四角色文件名/图片哈希/sourceGroup隔离；公开`verify_approved_report()`供训练入口深度重放。
- `train-yolo-seg.py --candidate-mode`现在只接受`--candidate-input-report`；旧`--candidate-validation-report`明确不能单独授权。正式门在创建输出、加载Ultralytics和占用GPU前执行，训练结束后再次重放；训练摘要绑定dataset SHA、输入报告证据和权重SHA。
- `run-training-release-pipeline.ts`把`candidate-input-preflight`固定为候选模式第一步；dry-run、`--skip-train`或跳过环境检查均不能绕过。恢复已有训练必须由`train-summary.json`证明同一dataset、输入报告和权重哈希，否则在评估/导出前停止。
- 真实输入运行结果：train 100、hard negative 1、val 30，正确输出`hold_canonical_candidate_dataset_materialization`。外部证据为`candidate-training-materialization-hold-v1.json`，SHA-256 `a0cec8aafaf1aef7c69124824cc9c108a8af02df69730bbad725bc88bb151493`；候选数据集输出目录确认不存在，未启动模型训练。
- 新增进度PASS标记`M2-T6-CANONICAL-CANDIDATE-DATASET-GATE`、`M2-T6-CANDIDATE-INPUT-DEEP-AUDIT`和`M2-T6-CANDIDATE-PIPELINE-PREFLIGHT`；实施规范同步两项已完成工程门。当前用户输入阻塞保持`USER-HARD-NEGATIVE-01`：仍需99张清晰、来源隔离、无有效美甲甲面的部署相关图片，经授权和原分辨率审核后汇总为正式manifest。
- 同步白皮书v1.1.192的状态表、§8.1、§11.1及变更记录；正式候选训练仍为HOLD，生产manifest未修改。

## 验证（规范候选训练门）

- 候选数据集物化专项：12/12 PASS。
- 候选输入深审与训练/恢复联调：10/10 PASS。
- `npm.cmd test`：506/506 PASS。
- `npm.cmd run lint`：PASS。
- `npm.cmd run audit:nail-texture-completion`：按预期退出1并输出`decision=hold`；326个进度标记、314个PASS、2/10完整发布门通过。
- `npm.cmd run audit:encoding`：421文件、0失败；`py_compile`与`git diff --check`通过。
- `npm.cmd run build`：Next.js 16.2.9生产构建和TypeScript检查通过。
- 本阶段未由Codex执行Git提交或推送；素材和HOLD报告仍位于Git外部审核工作区。

## 移动真机基准采集与设备证据深审

- 重新核对实施规范后确认：移动设备验收聚合器此前只信任外层`ok=true`和样本数，最终完成度审计也未重放源证据；极简伪造JSON、桌面报告冒充手机或写后漂移存在误判PASS风险。
- 新增`/device-benchmark`真机页面：固定3次预热和20次正式识别，逐次记录session、deviceFamily、模型、WebGPU/WASM后端、输入尺寸、端到端/Worker耗时、候选数、告警、时间戳和可用JS堆诊断；fallback、混模型/后端/输入或少样本不能晋级。
- 新增`build-nail-texture-mobile-memory-raw.ts`和CSV模板，把Android Profiler或iOS Instruments的20次系统级进程内存采样绑定至同一浏览器会话、设备、模型、后端和输入尺寸，并记录会话/CSV SHA-256；浏览器JS堆不得冒充整体峰值内存。
- `build-nail-texture-device-acceptance.ts`升级为`nail-texture-device-acceptance/v2`：重新读取性能报告、内存验证及原始内存，重算P95、阈值超限、内存峰值、窗口增长和连续增长，绑定路径与三层SHA-256并拒绝跨session。
- `audit-nail-texture-local-inference-completion.ts`不再信任设备报告外层布尔值，而是对四个设备槽位调用同一深度复验；任一源文件改变一字节后既有PASS自动失效。
- 新增`docs/mobile-device-acceptance-runbook.md`，明确Android手机/平板、iPhone、iPad四类实际物理设备仍需用户执行；新增PASS标记`M3-T3-MOBILE-BENCH-EVIDENCE`，原`M3-T3-DEVICE`保持PARTIAL。
- 并行只读核查确认现有本地排除项没有隐藏的可用hard negative：正式口径仍为1/100、缺99张；旧2条negative分别为独立甲片和旧test中的AI纯暗背景，均不可回流训练。另确认下一项工程优先缺口为规范val30阈值校准与冻结test发布评估编排断点。
- 同步白皮书v1.1.193的状态表、§9验证门、§11.1阻塞项和变更记录；实施规范保留真实设备任务未勾选，仅新增已完成工程门。

## 验证（移动真机证据工程门）

- 设备采集、性能/内存验证、聚合深审、伪造/跨session/少样本/写后漂移和内存CSV专项：21/21 PASS。
- `npm.cmd test`：513/513 PASS；`npm.cmd run lint`：PASS；`npm.cmd run audit:encoding`：428个文件、0失败；`git diff --check`：PASS。
- 相关文件ESLint：PASS。
- `npm.cmd run build`：Next.js 16.2.9生产构建、TypeScript和`/device-benchmark`静态页面生成PASS。
- `npm.cmd run audit:nail-texture-completion`：按预期输出HOLD；327个进度标记/315个PASS、2/10完整发布门通过，四类移动设备仍无真实报告。
- 未生成任何伪造真机PASS，未修改生产manifest，未启动模型训练，未执行Git提交或推送。

## GitHub 443超时再次只读复核

- 用户执行`git push`时再次收到`Failed to connect to github.com port 443 after 21595 ms`。本轮只读复测确认`github.com`可正常解析，HTTPS首页返回HTTP 200，Git智能HTTPS端点`git ls-remote --heads origin master`在约1.18秒内成功返回。
- 本地`master`、`origin/master`与远端`master`当前均指向`cc1f2638045b78514b4ea2ac0c94262aae9dfa31`，`origin/master...master`为ahead 0、behind 0；当前没有已提交但尚未推送的提交。工作区中的修改仍未提交，因此也不会被`git push`上传。
- GitHub官方状态页当前显示All Systems Operational且Git Operations正常。综合判断为此前的瞬时网络超时，不是分支名称、仓库权限或提交内容问题。
- 全局Git仍存在`http.sslbackend=openssl`和`http.sslverify=false`；本轮未改配置，且该不安全配置不是本次“连接不到443”的直接原因。未执行提交或推送，模型、数据、训练HOLD和生产manifest状态不变。
- 同步白皮书v1.1.194的§9网络诊断说明和变更记录。

## 验证（GitHub 443超时再次复核）

- `curl.exe -I https://github.com`：HTTP 200。
- `git ls-remote --heads origin master`：PASS，返回`cc1f263...`。
- `git rev-list --left-right --count origin/master...master`：`0 0`。
- 未执行任何Git写操作。
