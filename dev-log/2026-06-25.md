# 开发日志 — 2026-06-25 & 2026-06-26

## 2026-06-25（晚间工作 — 实际跨日至 26 日凌晨）

### 一、完成项目全面扫描审计

对 JiaRu 项目进行了全面扫描，生成审计报告 `jiaru-project-scan_2026-06-25_2150.md`（保存到 QClaw workspace）。

**扫描范围**:
- 13 个源文件（.tsx/.ts）
- 17 份 docs/ 技术文档
- 4 份 dev-log/ 开发日志
- 26 个 Git commit 历史
- Build + Lint 验证

**关键发现**:
| 类别 | 问题数 | 严重度分布 |
|------|:------:|:----------:|
| 高优先级 | 3 | 🔴 |
| 中优先级 | 3 | 🟡 |
| 低优先级 | 2 | 🟢 |

**🔴 高优先级问题**:
1. `dev-log/2026-06-24.md` 含 Git 合并冲突标记
2. 8 个 ESLint errors（3 ref-in-render + 2 prefer-const + 2 any + 1 可自动修复）
3. `package.json` 残留 3 个未使用依赖（camera_utils / drawing_utils / ngrok）

**🟡 中优先级问题**:
4. CLAUDE.md 参数表与代码不一致（TIP_OFFSET_RATIO 0.15 vs 0.28）
5. palm-orientation-spec.md 仍标注"待实现"但已实现
6. 根目录残留文件（旧 jpg / https-dev.mjs / backup_ar_working/ / certificates/）

**🟢 低优先级**:
7. ArView.tsx 1033 行过长，建议拆分
8. NailArtPicker.tsx 6 个未使用变量

### 二、修复 dev-log/2026-06-24.md Git 合并冲突

**问题**: 文件含 `<<<<<<< HEAD` / `=======` / `>>>>>>> 23c16d32` 三段冲突标记，两个版本内容分裂。

**分析**:
- HEAD 版本：完整版（6 修复 + 5 功能 + 2 文档优化 + 完整参数表 + 12 条经验教训 + 已修复 Bug 清单）
- 分支版本：早期简略版（仅摄像头管线 + 朝向策略 + 逐指可见性 + 11 commit 列表）

**决策**: 保留 HEAD 版本（信息量更完整），丢弃分支版本。

**结果**: 文件从含冲突标记 → 干净的完整日志。

### 三、修复 8 个 ESLint errors

| # | 文件 | 行号 | 问题类型 | 修复方式 |
|---|------|:----:|----------|----------|
| 1 | `ar-tryon/page.tsx` | 38 | react-hooks/refs（渲染期间更新 ref） | `texturesRef.current = nailTextures` 移到 `useEffect` |
| 2 | `ar-tryon/page.tsx` | 40 | react-hooks/refs（渲染期间更新 ref） | `photoUrlRef.current = uploadedPhotoUrl` 移到 `useEffect` |
| 3 | `TextureCropper.tsx` | 298 | react-hooks/refs（渲染期间访问 ref） | 新增 `imgLoaded` state，用 state 替代 `imgRef.current` 检查 |
| 4 | `NailArtPicker.tsx` | 105 | prefer-const | `let fx, fy` → `const fx, fy`（后续未重新赋值） |
| 5 | `NailArtPicker.tsx` | 156 | no-explicit-any | `(window as any).Hands` → `window.Hands as unknown as HandsCtor` |
| 6 | `NailArtPicker.tsx` | 160 | no-explicit-any | 同上 |

**TypeScript 类型冲突修复**:
- 首次尝试在 NailArtPicker.tsx 中 `declare global { interface Window { Hands?: HandsCtor } }` → 与 ArView.tsx 的 `Window.Hands?: HandsConstructor` 冲突
- 解决方案：不在 NailArtPicker.tsx 重复声明 global，复用 ArView.tsx 的声明，用 `as unknown as HandsCtor` 类型断言

### 四、验证

| 检查项 | 结果 |
|--------|:----:|
| ESLint | ✅ 8 errors → 0 errors（12 warnings 保留，均为未使用变量） |
| Build | ✅ exit 0，1161ms，9 routes |
| TypeScript | ✅ 通过 |

### 五、Git Commit

```
71fb430 fix: resolve dev-log merge conflict + fix 8 ESLint errors
```

4 files changed, 18 insertions(+), 201 deletions(-)

---

## 2026-06-26

### 今日工作

1. **更新 dev-log/2026-06-25.md**（本文件）— 记录 06-25 晚间的扫描审计 + 冲突修复 + ESLint 修复
2. **修复 CLAUDE.md Git 合并冲突** — CLAUDE.md 同样含 `<<<<<<< HEAD` / `>>>>>>> 23c16d32` 冲突标记，需要解决并同步更新项目进度
3. **更新 CLAUDE.md** — 更新当前进度、参数表、文档索引

### 待办

- [ ] 真机测试（手机端摄像头启动 + 帧率 + 阈值调优）
- [ ] 清理未使用依赖（camera_utils / drawing_utils / ngrok）
- [ ] 清理根目录残留文件
- [ ] Phase 2: AI 生成模块
- [ ] Vercel 部署
