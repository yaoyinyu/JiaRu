# 2026-07-31 开发日志

## 美甲纹理局部识别恢复（白皮书 v1.1.271）

### 只读交接与约束

- 任务开始前完整读取`AGENTS.md`、`docs/technical-whitepaper.md` v1.1.270、`dev-log/2026-07-30.md`及相关最新记录，并核对状态表、§4.5、§6、§11.1和§13。
- 起点为`master`、HEAD `3e4561c80abe79b21c317168f652a5088df540ee`、单一worktree；`git status`、工作区diff和暂存区diff均为空。
- 正式manifest所指`nail-texture-seg-v1.onnx`仍不存在；candidate5已因训练后独立困难负样本三变体门失败而拒绝。本任务不运行candidate5推理、训练、导出、登记、部署或freeze，不改变共享阈值及产品HOLD。
- 未启用子智能体或高并发，未扫描Codex会话目录，未执行Git提交或推送。

### 实施前技术方案与复审

- 新建`docs/nail-texture-recognition-recovery-plan-2026-07-31.md`。v0.1先记录现场3/5复现、正式模型阻塞及候选修复方向，明确在直接拒绝原因和无关键点指序尚未机器化前不得修改运行代码。
- 从用户截图取得的临时903×1195现场裁片只用于本地功能回归，不入仓库、训练或正式模型证据。真实页面复现`backend=fallback`、模型ONNX外部数据加载失败、MediaPipe无可用甲面、局部路径仅3/5；控制台0 error。
- 按当前算法逐组件复算，五枚真实甲面均形成独立连通组件。食指与无名指的面积、形状、填充率、灰度方差和触边门全部通过，仅被`directSmoothSkinRatio < 0.24`拒绝；干燥皮肤、甲缘和镜面高光提高拉普拉斯响应是直接根因。同图大背景组件已被触边、面积和长边门拒绝，否定了v0.1关于当前主因是背景桥接的推断。
- 方案v1.0拒绝“直接降低0.24”和“直接取消掌面拒绝”，批准最大手部肤色支持域、原始肤色环、候选—肤色环局部RGB色差、原有几何门和候选弧排序的复合方案。第一阶段达到现场5/5后停止扩大修改面；MediaPipe逐指ROI第二阶段未触发。

### 代码与接口

- `src/lib/nail-partial-closeup-detection.ts`
  - 分析最长边由480提高到640；二值前景先开运算断细桥，再闭运算补甲内高光裂缝。
  - 保留RGB、原始肤色和平滑肤色掩码，从最大连贯肤色组件建立手部支持域与掌心近似。
  - 组件同时检查既有面积、旋转长宽、填充率、方差和触边门，以及原始肤色环≥0.52、手部支持环≥0.38、局部RGB均值距离≥18；平滑肤色只参与评分，不再单点否决。
  - 五候选按掌心近似的极角弧排序，并结合弧端候选尺度和相邻间距确定拇指方向；兼容入口`detectPartialCloseupNailCandidates()`继续只返回候选。
  - 新增`detectPartialCloseupNails()`及`PartialCloseupDetectionDiagnostics`，输出分析尺寸、组件数、合格/选中数和枚举拒绝计数，不输出原图像素。
- `src/components/NailArtPicker.tsx`接入详细局部检测结果并把诊断加入导出摘要；正式模型失败warning、安全人工出口和现有用户文案保持不变。
- `src/lib/nail-texture-debug-sample.ts`把局部组件诊断持久化到本地诊断JSON。
- `tests/nail-partial-closeup-detection.test.ts`新增干燥纹理甲缘五甲回归：旧实现稳定2/5，新实现5/5；同时断言背景大组件继续被拒绝并可解释。`tests/nail-texture-debug-sample.test.ts`新增诊断导出契约断言。

### 浏览器验收

- 使用本地Next.js页面`http://localhost:3000/ar-tryon`重新上传现场临时裁片：区域5、已分配5、来源为“局部近景自动定位”，五个椭圆经截图视觉检查均覆盖实际拇/食/中/无名/小指甲面，背景0候选。
- 点击一次“提取所选纹理”后逐指切换，确认拇指、食指、中指、无名指、小指五份纹理全部存在；控制台0 error、4条预期MediaPipe/正式模型运行warning。
- 导出诊断JSON显示`analysisWidth=484`、`analysisHeight=640`、`componentCount=33`、`acceptedComponentCount=5`、`selectedCandidateCount=5`；其余组件由触边、过小、过大或长轴过大等门拒绝。
- 浏览器截图仅存于`output/playwright/nail-texture-recovery-2026-07-31.png`作为本地验证产物；不作为训练或模型发布质量证据。

### 验证与文档同步

- 新失败回归在旧实现下为2/5，确认测试有效；修改后局部检测、正常/镜像/歧义指序与诊断专项10/10通过。
- `npm.cmd run lint -- --quiet`通过。
- 显式将Python 3.13置于`PATH`首位后，最终`npm.cmd test`以单进程串行完成727/727，0失败。
- 停止本项目3000端口开发进程后串行执行`npm.cmd run build`，Next.js 16.2.9生产构建、TypeScript及12个路由生成通过；随后恢复开发服务，实验性HTTPS证书生成失败时按Next.js既有行为回退到`http://localhost:3000`并Ready。
- 技术白皮书更新到v1.1.271，同步状态表、§4.5、§6.2、§11.1和§13变更记录；正式ONNX阻塞、candidate5拒绝、生产manifest、共享阈值和产品HOLD均不变。
- 收口时检测到HEAD由任务起点`3e4561c`外部前进到`7e9c207`，提交者/推送者为YaoYinyu，提交包含本任务源码、测试和当时文档版本，且`origin/master`同步前进；本代理未执行`git add`、`git commit`或`git push`，未重置或改写该提交。最终保留其后的白皮书计数、方案实施结果和本日志补记为未提交文档差异。

## 低对比裸色甲面识别补充修复（白皮书 v1.1.272）

### 交接与实施前证据

- 本次起点重新读取`AGENTS.md`、白皮书v1.1.271状态表、§4.5、§6.2、§11.1、§13、恢复方案及当天唯一日志；`master`、HEAD `0fe8a208365a1a92890120b2853f9bb6c8465d54`、单一worktree，任务开始时工作区与暂存区均干净。未启用子智能体/高并发，未扫描Codex会话目录，未提交或推送Git。
- 用户更换为裸色/半透明五甲图后，真实页面“多纹理提取”稳定复现0区域/0分配，约4.9秒，控制台0 error；MediaPipe返回无完整手部，排除上传或脚本崩溃。
- 临时1201×1195裁片SHA-256为`b338357bb7c87a8e8b69aaf33753482b5f5b830ca13ecca4a6a33bef9deb1830`，来自用户2549×1403界面截图且含顶部提示条；只用于本地功能回归，不入Git、训练或发布证据。
- 640像素逐像素复算确认四枚无遮挡可见甲面96.5%—100%像素被肤色分类器判为皮肤，蝴蝶结甲和小指甲的甲色候选种子为0；甲色路径43个组件中仅1个指缝暗阴影通过门，因不足2枚整体返回0。新故障发生在候选种子阶段，与上一轮“组件存在但后置平滑肤色门误杀”不同。

### 技术方案与代码

- 在修改运行代码前将`docs/nail-texture-recognition-recovery-plan-2026-07-31.md`升级至v1.2并完成复审：拒绝放宽全图肤色/色差、降低后置门或取消MediaPipe掌面安全门；批准仅在甲色路径不足2枚时启用的低对比闭合边界路径，并要求单一4—5枚连续链。
- `src/lib/nail-partial-closeup-detection.ts`新增固定5×5平滑、Sobel阈值22/24、半径2闭运算、边缘补集连通域、甲板内部与肤色邻环/手部支持、内部非暗平滑、边界梯度、跨阈值去重和最长边21%连续链判定。少于4枚、超过5枚或存在第二个4枚竞争簇时整批返回0；旧甲色路径继续优先且阈值未改。
- 低对比候选保持`source: partial-closeup`、固定`confidence: low`并带`partial_closeup_low_contrast_boundary`；中文质量提示明确要求检查裸色/半透明甲面边界。
- 诊断契约新增`strategy`、低对比组件/合格/选中和逐原因拒绝统计；本地导出继续不含图像像素。
- 测试先行：合成肤色闭合五甲在旧实现为0/5，新实现5/5；同类区域不足4枚整批隐藏。诊断持久化测试同步覆盖新增字段。

### 浏览器与工程验收

- 第二现场临时裁片在真实`/ar-tryon`由0/5恢复为5/5区域和唯一分配；截图视觉复核确认五框分别落在顶部横向甲、上方裸色甲、蝴蝶结甲、圆点甲和小指甲，背景/掌纹0候选。
- 一次点击“提取所选纹理”后逐指切换，拇/食/中/无名/小指五份纹理均存在；控制台0 error，MediaPipe WebGL信息为预期warning。
- 诊断JSON为640×637、甲色44组件/1合格、低对比140组件/6合格/5选中，`strategy=low-contrast-boundary`。上一轮深色/银色现场裁片回测仍走`painted-color`且保持5/5区域、5/5分配，未发生路径抢占。
- 局部检测、中文质量提示和诊断专项20/20通过；ESLint通过；显式Python 3.13置于`PATH`首位后全量单进程729/729、0失败；Next.js 16.2.9生产构建、TypeScript和12个路由生成通过；477文件严格UTF-8审计0失败，`git diff --check`通过（仅提示既有Windows行尾转换）。
- 白皮书更新至v1.1.272，同步状态表、§4.5、§6.2、§11.1与§13；恢复方案追加v1.2实施结果。正式ONNX仍缺失、candidate5继续拒绝，生产manifest、共享阈值和产品HOLD不变。
- 收口时检测到HEAD由本次任务起点`0fe8a208365a1a92890120b2853f9bb6c8465d54`被外部进程前进到`b39b46b2b2a6948af2dd0df28819b1e747846cf3`，且`origin/master`同步；外部提交依次为`bb9e758`（方案v1.2）、`7bf89b3`（低对比检测实现）和`b39b46b`（测试覆盖），作者/提交者均为YaoYinyu。本代理未执行`git add`、`git commit`、`git push`或重置，保留当前未提交文档、诊断测试补充及源码lint收口差异。

## 复杂拼图现场与正式模型装载核验（白皮书 v1.1.273）

### 只读核验与直接结论

- 重新完整读取`AGENTS.md`、白皮书v1.1.272状态表、§4.5、§6.2、§11.1、§13、恢复方案和当天唯一日志。任务起点为`master`、HEAD/`origin/master`均为`099b207778a286d042948094623bef98da96db3d`、单一worktree，工作区和暂存区干净；未启用子智能体或高并发。
- 用户新图包含中文标题、说明文字、主手照片和右下重复缩略图；页面仍显示区域0/已分配0及“模型会话初始化失败，已回退到规则识别”。
- 磁盘复核确认`public/models/nail-texture-seg/manifest.json`存在并声明`nail-texture-seg-v1.onnx`，但相邻ONNX文件实际不存在。因此页面没有运行此前训练模型，当前0候选来自MediaPipe/局部规则辅助路径；上一轮不能把规则裁片5/5解释成正式模型质量。

### candidate5离线诊断

- 复验正式交接权重`E:\AI Project\Codex\JiaRu_image\训练输出\nail-texture-candidate-2026-07-29-v5\candidate-hard-negative-v5\weights\best.pt`为6,016,533字节，SHA-256=`26daf742ce83a9d740a786114cfec481051e6f86574ba1c43428f75e5db4ca56`。
- 从用户2549×1403界面截图裁得725×897临时诊断图，SHA-256=`58b0fc255664089e5ffba3628aa0a8a4735986b2c1e71595d061596fd5ae2ea2`；只用于本地诊断，不入Git、训练或发布证据。
- 使用Ultralytics 8.4.92、部署`imgsz=512`、规范阈值0.50和CUDA设备执行candidate5推理，输出6个mask。五枚主图甲面置信度约0.84—0.90且预测叠加视觉覆盖完整；第六个0.68候选落在中文标题文字上。该结果证明页面0枚主要是模型资产未加载，也再次证明candidate5仍有发布级误检，不能复制到生产目录。
- 未按本图把阈值提高到0.68以上，未导出、登记或部署candidate5，未修改生产manifest、共享阈值或运行代码。candidate5独立困难负样本3/4/3张误检、产品HOLD与trainingUse边界均保持不变。

### 方案与文档同步

- 恢复方案升级v1.3：拒绝继续按单图扩张规则、用本图调阈值或部署已拒绝权重；下一步固定为新训练授权/来源隔离数据、下一候选训练、训练前冻结主手一致性后处理、val30与冻结test100、训练后全新未见独立困难负样本、生产ONNX、真机和Beta门。
- 白皮书更新至v1.1.273，同步§4.5、§6.2、§11.1及§13变更记录。本轮是诊断与方案纠偏，没有运行代码接口变化；正式自动识别继续阻塞，辅助路径和candidate5都不再被表述为已解决产品质量。

## 模型落地Goal技术文档回查（白皮书 v1.1.274）

- 重新读取白皮书v1.1.273状态表、§9.2实施规范复核、§11.1阻塞项和§13最新记录；任务起点为`master`、HEAD/`origin/master`=`a17f8ba43a009072a51752d7642091da1196b2d8`、单一worktree，工作区和暂存区干净。
- 当前Goal接口返回`null`，内存索引无JiaRu/模型落地历史Goal条目；遵守既有约束，未扫描`.codex/sessions`。
- 仓库证据确认`docs/nail-texture-recognition-model-plan.md`是2026-06-30原始Phase 0—5/MVP路线图；`dev-log/2026-07-04.md`第468、490行两次写明真实授权数据和真实ONNX是“goal can be marked complete”前的剩余阻塞，因此它是早期Goal最直接的文档入口。
- 后续真正持续推进落地的权威主文档为`docs/nail-texture-local-inference-implementation-spec.md`，其配套实时账本为`docs/nail-texture-local-inference-implementation-progress.md`；`docs/nail-texture-completion-audit.md`和机器JSON负责最终Goal完成判定。早期逐阶段审计为`docs/nail-texture-progress-audit-2026-07-01.md`。
- 白皮书升级至v1.1.274，新增§9.3并追加§13变更记录，固定“原始Goal路线图→实施规范→进度账本→完成度审计→白皮书当前状态”的读取顺序。本轮只做只读查找与文档导航同步，无运行代码、模型、manifest、阈值或产品状态变化。

## 正式模型落地与前端接入Goal建议（白皮书 v1.1.275）

- 复核白皮书v1.1.274状态表、§9.3文档谱系、§11.1阻塞项，以及实施规范§16—19和完成度审计；当前Goal接口仍返回`null`。正式ONNX缺失、candidate5已拒绝、生产manifest和产品状态继续HOLD。
- 推荐Goal命名为“完成甲如正式美甲纹理模型、前端接入与发布验收”，并在白皮书§9.4给出可直接复制的完整Goal正文。
- Goal把新来源且精确授权的训练数据、下一候选训练、来源隔离val30、冻结test100、训练后全新未见困难负样本三变体零误检、生产ONNX大小/SHA-256完整性、浏览器Worker WebGPU/WASM、`/ar-tryon`像素mask自动提取、真实浏览器、Android手机/平板、iPhone/iPad、至少100张Beta审核、产品质量报告和两个正式版本回滚统一纳入完成条件；最终仅以完成度审计`ok=true`、`decision=complete`收口。
- 固定不复用candidate5或已消费holdout、不使用test/发布留出/用户单图调阈值、不在精确训练授权前启动训练、不把候选训练、ONNX导出或网页加载等中间里程碑误报为完成。建议该长周期Goal不设置固定token预算。
- 本轮只形成Goal建议并同步文档，没有调用Goal创建接口，没有运行生成、训练、推理、导出、部署或修改前端；未执行git commit或git push。
