# 2026-07-22 开发日志

## GitHub 443 连接超时再次只读复核

- 当前分支为 `master`，远端地址为 `https://github.com/yaoyinyu/JiaRu.git`；Git 配置及环境变量未发现代理。
- DNS 解析正常，GitHub 首页 HTTPS 返回 200；Git 自身的只读命令 `git ls-remote --heads origin master` 成功返回。
- 本地 `HEAD`、缓存的 `origin/master` 与 GitHub 远端 `master` 三者均为 `2be103b19e258a20f0e93986012b5df9d46376f8`，对应提交 `Validate hard-negative image integrity and approval evidence`。
- 当前没有待推送提交。报错属于执行 push 时的瞬时网络连接失败，或者连接恢复后用户已经重试成功；与分支名称、提交信息或仓库内容无关。
- 发现用户级 Git 配置包含 `http.sslverify=false`，本次未修改；该设置会降低 HTTPS 证书校验安全性，建议另行确认后再恢复为默认校验。
- 未执行 `git commit`、`git push`、`git fetch` 或 Git 配置修改。
- 技术白皮书已同步为 v1.1.200，在“版本与变更记录”登记本次只读复核；模型接口、数据集、训练 HOLD 与生产 manifest 均无变化。

### 验证

- `Invoke-WebRequest -Method Head https://github.com`：HTTP 200。
- `git ls-remote --heads origin master`：成功，远端 `master` 为 `2be103b19e258a20f0e93986012b5df9d46376f8`。

## GitHub 443 连接超时只读复核

- 复核远端地址为 `https://github.com/yaoyinyu/JiaRu.git`，当前分支为 `master`。
- DNS 解析正常，GitHub 首页 HTTPS 返回 200；`git ls-remote --exit-code origin HEAD refs/heads/master` 成功返回。
- 本地 `HEAD`、`origin/master` 与远端 `master` 均为 `2a22715b143b3272168cf225c75ed6114ea1fe83`，当前没有待推送提交。
- Git 仓库、全局配置及环境变量均未发现代理设置；本次错误判定为命令执行当时的瞬时网络超时，与分支名、提交信息或仓库内容无关。
- 未执行 `git commit`、`git push`、`git fetch` 或 Git 配置修改。
- 已同步技术白皮书 v1.1.198 的“版本与变更记录”；模型接口、数据集、训练 HOLD 与生产 manifest 均无变化。

### 验证

- `curl.exe -I --connect-timeout 15 --max-time 25 https://github.com/`：HTTP 200。
- `git ls-remote --exit-code origin HEAD refs/heads/master`：通过，远端 `HEAD` 与 `master` 均为 `2a22715b143b3272168cf225c75ed6114ea1fe83`。

## Hard negative schema v2正式终结与深度重放

- 新增`model/training/finalize-reviewed-hard-negative-manifest.py`：支持合并一个或多个逐图审核候选批次，绑定候选清单、审核决定、A授权、来源筛选、当前图片哈希和既有隔离证据。
- 固定正式hard negative下限为100张，CLI不能降低；不足时只输出`HOLD`、`trainingUse=prohibited`和`candidateItems`，不输出正式消费者可用的`items`。
- 正式PASS报告升级为schema v2并提供`--verify-report`/`verify_approved_report()`；`audit-validation-role-isolation.py`、规范候选物化器和候选输入深审都会从当前审核、授权及图片字节重放，不再信任外层`ok/decision/itemsSha256`。
- Pillow在批准前执行`verify()`和完整`load()`，复核最短边320像素、审核宽高及支持格式；GPU前输入审计再次解码源/物化负样本。文本伪装、损坏、尺寸漂移、审核/授权漂移和输出覆盖均会拒绝。
- 可解码图片若真实格式与扩展名不一致，保留源文件与原审核身份，正式物化文件名按真实格式纠正。真实唯一候选源名为`.jpg`但字节是1078×1418 WEBP，schema v2 HOLD中物化名为`.webp`，未修改原图。
- 真实候选复跑：1/100、缺99、训练用途继续禁止；外部报告`hard-negative-formalization-hold-v2.json` SHA-256为`c35214e92abdfcb57b6e3005afa67230b1aab2aaea21107c71e1afd8c1f365a4`，未创建候选训练数据集。
- 并行复核既有300张已授权AI美甲图：两批100+200张及重命名清单哈希均稳定，但缺当前严格逐图审核；历史9个低标注候选9/9仍含清晰美甲，不能作为无甲hard negative，不用于凑数。
- 新增进度标记`M2-T3-HARD-NEGATIVE-FINALIZER-002`，状态为PASS（工程门；真实池HOLD）。

### 审核与验证

- hard negative终结、角色隔离、规范物化、GPU前输入审计联合专项：63/63通过。
- Python编译：通过。
- `npm.cmd run lint`：通过。
- `npm.cmd test`全量回归：550/550通过。
- `npm.cmd run build`：Next.js生产构建通过。
- 编码审计：431个文件全部通过；`git diff --check`通过。
- 完成度审计：330个进度标记、318个PASS、2/10发布门通过，并按预期保持HOLD。
- 训练、评估、导出、生产manifest、正式409图/2142 mask数据集均未修改；未执行Git提交或推送。
- 技术白皮书同步为v1.1.200，更新功能状态表、§8.1数据集契约及版本变更记录，并补记本阶段最终全量验收结果。

## 训练启动与发布测试角色状态复核（v1.1.201）

- 当前规范训练输入为train 100张、val 30张、hard negative 1张，仍缺99张合格hard negative；按三类角色最低图片数量计算为131/230（约57%）。当前训练输入继续HOLD的原因是hard negative未达到100张正式门槛。
- 另缺的33张代表性发布测试属于训练后的模型质量评估与正式发布门，不阻止候选训练输入门满足后启动训练；不得把该发布规模缺口误记为训练启动前置数量缺口。
- 原规划33张独立发布测试候选完成原分辨率审核：25张保留、8张因甲面裁断、遮挡或仅局部露出而排除。已确认4个完整train来源组共8张替补，替补来源组成员均须整体迁出train并继续保持`trainingUse=prohibited`、`annotationTruthStatus=not-started`。
- 新增发布测试角色替换工具，重放原33逐项审核、A授权、screening keep、完整甲数、当前图片哈希以及train100、val30、冻结67和保留原候选的身份隔离；不足、重复、部分来源组、视觉未审、哈希漂移或输出覆盖均拒绝且不产生新输出。专项测试8/8通过。
- 产品质量原始证据链专项16/16通过，回滚深验专项26/26通过。真实冻结67图的质量结论仍为HOLD；主completion集成与全量回归尚未完成，本轮不得据此晋升生产发布状态。
- 本次仅同步技术白皮书和当日开发日志，未修改completion文档、实施规范、实施进度、运行时接口、生产manifest或真实外部素材，也未执行Git提交或推送。

### 验证

- 发布测试角色替换工具专项：8/8通过。
- 产品质量原始证据链专项：16/16通过。
- 回滚深验专项：26/26通过。
- 主completion集成与全量回归：尚未执行，保持待完成状态。
- 技术白皮书版本已升至v1.1.201，并在“版本与变更记录”首行登记本次状态复核。

## 正式训练与发布测试进度刷新（v1.1.202）

- 按当前权威输入重新核对：train正样本100/100、val30/30已经满足；hard negative仍为1/100、缺99张。按三类最低图片数量为131/230（约57%），但门禁是二元HOLD，hard negative未满100前不会物化规范候选数据集或占用GPU启动正式候选训练。
- 补足发布测试代表性下限所需的33张候选已完成角色替换：原33张保留25、排除8，并从4个完整train来源组迁出8张替补，最终33张/170个预期甲面/11个来源组；与train100、val30、冻结test67按文件名、图片SHA-256和来源组均零重叠，训练用途固定prohibited。
- 外部标注工作区已物化33张、4个来源组原子分片，33个图片硬链接逐字节一致。v6在1024输入下生成204个YOLO候选，SAM2完成204/204 mask、0 fallback、0错误；几何审计163 pass/41 suspect。
- 33/33原分辨率视觉首审已覆盖完成，严格结论为7张可直接通过、26张需要删除误检/重复/局部mask或补建完整甲面、0张因源图质量排除；其中一张视觉轮廓可用但5个polygon拓扑无效，仍按返修处理。返修和逐甲终审完成前，代表性冻结test仍按67/100计数。
- 完成度审计v2现场复跑：新增两项工程PASS标记后为333个进度标记、321个PASS；13个正式发布门中2个通过、11个未通过，决定保持HOLD。该2/13衡量完整发布闭环，不代表候选训练数据只完成15%。
- 本轮为状态核验与文档同步，没有修改运行时接口、生产manifest或正式模型资产；未执行Git提交或推送。

### 验证

- `npm.cmd run audit:nail-texture-completion`：按预期输出HOLD，2/13门通过；失败项包括模型部署512质量退化、正式快照仍为67/100、四类移动真机、失败案例、Beta人工质量、产品质量报告、生产ONNX和回滚证据。
- 技术白皮书同步为v1.1.202，更新功能状态表中的正式模型、数据集治理、模型发布治理及版本变更记录；实施进度同步新增发布测试角色替换/标注工作区PASS标记，并刷新发布测试标注状态。

## 正式训练启动距离与发布测试返修增量（v1.1.203）

- 现场复核规范训练输入仍为train正样本100/100、val 30/30、hard negative 1/100；按最低图片数为131/230（约57%）。训练启动前仍缺99张合格hard negative，以及补齐后的正式manifest终结、规范候选数据集物化和GPU前输入深审。
- 该99张hard negative是当前开训HOLD的直接素材缺口；补足发布测试的33张属于训练后的冻结质量评估，不阻止hard negative达标后启动候选训练。
- 33张发布测试候选已有7张直通项和8张仅删除误检项形成15张/76个完整mask候选真值索引；另1张拓扑修复候选完成，剩余17张已准备89个SAM返修提示，GPU推理尚未执行。
- 发布角色终结器与唯一索引正在增加视觉证据深度重放、角色manifest复验、原始证据防覆盖和原子写入保护。15张既有候选是在加固完成前生成，必须重新终结并复验后才能计入冻结test，因此当前正式冻结规模仍是67/100。
- 修正一项安全专项测试的错误文案断言；实现正确更早拒绝了“没有当前decisions文件”的伪造证据，未放宽安全门。
- 本轮未启动训练、未修改生产manifest、未执行Git提交或推送。

### 验证

- 发布测试真值终结与唯一索引安全专项：8/8通过。
- `python -m py_compile model/training/finalize-first-annotation-training-truth.py model/training/audit-first-annotation-training-truths.py`：通过。
- 技术白皮书同步为v1.1.203，更新功能状态表的数据集治理状态及版本变更记录。

## 正式训练距离与发布测试终审进度刷新（v1.1.204）

- 现场复核三类正式训练输入：train正样本100/100、val 30/30、hard negative 1/100，合计131/230（约57%）。当前阻止GPU正式候选训练的唯一素材缺口仍是99张合格hard negative；补足后再执行正式manifest终结、规范候选数据集物化和GPU前输入深审。
- 发布测试真值终结器和唯一索引已完成加固。原15张候选全部用当前证据重放通过；拓扑项仅修复第3个自交polygon，其余4个源polygon保持不变。当前候选真值唯一索引为16张/81个完整mask、0拒绝、0冲突，SHA-256为`2c40aa2fd71a3d92a1927b15c13de8ddb713912eb467a0e3644738f37d23711b`。
- 剩余17张发布测试返修已由SAM2.1 Large在RTX 4060上完成89/89个mask，0 fallback、0错误；SAM报告SHA-256为`2a2bfb934a63d1f1028294f2a9faca240d882c0f7c2e816b87e3a41c56ff58f3`。几何审计为86 pass/3 suspect，并已生成17张整图叠加及89组原图/叠加2×裁剪证据，当前正在进行原分辨率逐甲终审。
- 上述17张尚未全部形成终结报告，16张候选真值也尚未与原67张合并冻结，因此正式冻结release-test仍按67/100计数。该发布测试工作属于训练后的质量门，不改变“补足99张hard negative即可进入候选训练输入终结”的开训口径。
- 本轮为状态核验和文档同步；未启动训练、未修改生产manifest、未执行Git提交或推送。

### 验证

- `release-test-truth-index-v1.json`：`approved_unique_release_test_truth_index`，16张/81 mask、0冲突，当前文件SHA-256为`2c40aa2f…711b`。
- `sam-repair-run-v1/report.json`：17张、89个提示全部完成，0 fallback、0错误；几何审计86 pass/3 suspect。
- `hard-negative-formalization-hold-v2.json`：`HOLD`，1/100，`gapToMinimum=99`。
- 技术白皮书同步为v1.1.204，更新正式纹理模型、数据集治理及版本变更记录。

## 正式训练距离与发布测试第二轮返修进度（v1.1.205）

- 现场复核正式候选训练输入仍为train正样本100/100、val 30/30、hard negative 1/100，即131/230（约57%）；开训采用硬门而非平均百分比，当前唯一直接素材缺口仍为99张合格hard negative。
- 对现有排除池115项及派生区域再次只读复筛，新增安全hard negative为0；既有唯一候选保持1张，不能用拼图、模板、低清、裁断图或实际含美甲的图片凑数。
- 剩余17张发布测试返修图完成严格逐甲视觉审核，仅4张可直接通过。schema v2终结器新增对提示、SAM、几何、annotation、overlay及逐甲原图/叠加裁剪证据的路径和SHA-256深度绑定；4张通过项完成终结后，候选真值唯一索引由16张/81 mask推进至20张/101 mask、0拒绝、0冲突，当前SHA-256为`a19154558d6a113dc1baaf90bab621f2413eb9c854dd39dd65a978ec2092ba91`。
- 其余13张已执行第二轮SAM2.1 Large返修：69/69 mask、0 fallback、0错误；几何审计59 pass/10 suspect。整图叠加和逐甲裁剪证据已生成，仍须完成原分辨率视觉审核；失败项转人工多边形，不提前计入冻结test。
- 正式冻结release-test仍为67/100；发布测试补齐是训练后的质量评估门，不改变“补足99张hard negative后进入训练输入终结”的启动口径。
- 未启动训练、未修改生产manifest、未执行Git提交或推送。

### 验证

- schema v2返修终结与训练真值深度重放专项：4/4通过。
- 第二轮SAM报告：13张、69个提示全部完成，0 fallback、0错误；几何审计59 pass/10 suspect。
- `release-test-truth-index-v1.json`：20张/101 mask、0拒绝、0冲突，SHA-256为`a1915455…a91`。
- 技术白皮书同步为v1.1.205；实施进度新增`M2-T3-RELEASE-TEST-VISUAL-EVIDENCE-FINALIZER-002` PASS并刷新发布测试标注状态。

## 补充发布测试终结与100张冻结快照（v1.1.206）

- 完成13张第二轮SAM返修的原分辨率逐甲审核：8张整图直接通过，5张转混合人工polygon。人工批次保留22个已审polygon并重画7个问题polygon；独立复核拦截`00436 n5`白色法式延长段、`00705 n1`透明浅色甲尖和`00743 n4`透明银色甲尖/附饰遗漏，迭代后5张/29 polygon全部视觉通过、合法且同图零交叠，几何29 pass/0 suspect/0 missing。
- 8张直接通过项和5张人工项均完成哈希绑定返修终结与release-test角色真值终审。补充唯一索引最终为33个批准报告、33张唯一图片、170个完整mask、0拒绝/冗余/冲突，SHA-256为`93588fb6d0389276faff9726207d9bd0cf1715e8d1204e04edcd0892bff7e04c`。
- 新增`freeze-extended-release-test-snapshot.py`，深验旧67张冻结manifest、新33张真值报告及train100/val30身份，重算图片、annotation、联合哈希、Pillow解码、尺寸、polygon合法性和严格零交叠，并按文件名、图片SHA-256、`sourceGroup`及`parentSourceGroup`执行四角色隔离；失败时使用事务式临时目录，不复用输出。
- 外部冻结快照形成100图/554 mask、core78/stress22、29父来源组，代表性门100/100通过，manifest SHA-256为`b3baa41c913548ed1513e7e22457fcc2bf4f1f99826ab9db1033e41e521823c6`，`trainingUse=prohibited`且决定仍为`frozen_reviewed_candidate_not_release_ready`。
- 评估专用物化形成train0/val0/test100、554 mask、29父来源组，与正式409图训练集按来源组、图片SHA和文件名零重叠；物化报告SHA-256为`cf45ad81437fa85e612551f65c1f5ee9bdd5814c30fab529bcfc29b27abc1d0d`，只读深度重放PASS。
- 发布测试代表性规模缺口已解除，但正式候选训练仍为train100/100、val30/30、hard negative1/100；唯一直接素材缺口仍是99张合格hard negative，补齐后还需正式manifest终结、规范候选数据集物化和GPU前输入深审。未启动训练、未修改生产manifest、未执行Git提交或推送。

### 验证

- `node --test tests/freeze-extended-release-test-snapshot.test.ts tests/finalize-first-annotation-mask-repair.test.ts`：4/4 PASS。
- `python -m py_compile model/training/freeze-extended-release-test-snapshot.py`：PASS。
- `release-test-truth-index-v1.json`：33张/170 mask、0拒绝/冗余/冲突。
- 冻结合并器真实运行：100图/554 mask、core78/stress22、29父来源组，代表性门PASS，四角色身份零泄漏。
- `materialize-frozen-release-test-evaluation.py --verify-report ... --expected-dataset ...`：PASS，`outputWritten=false`。
- 技术白皮书同步为v1.1.206，更新功能状态总表、训练启动距离和版本变更记录；实施规范及进度表新增补充真值、100张冻结与评估物化PASS标记。
- 完成度审计改用100张冻结快照现场复跑：337个进度标记、327个PASS，13个正式发布门中4个通过；`USER-TESTSET-01`已由进行中改为PASS，决定按预期保持HOLD。
- README已把旧67/100与仍缺33张的状态改为100张/554 mask冻结完成，并新增正式训练仍缺99张hard negative；严格UTF-8、0 NUL、12个代码围栏成对、31个标题、6个表格分隔行及末尾换行全部正常。
- Python四个改动脚本编译、ESLint、443文件编码审计、Next.js 16.2.9生产构建和`git diff --check`通过；全量串行测试为613/623，10个既有发布治理回滚完整性用例失败，单独串行复验`run-release-governance-pipeline.test.ts`仍为6/9通过，失败原因是旧测试fixture的manifest未包含当前回滚深审要求的`modelSizeBytes`与`sha256`，与本轮发布测试冻结代码无直接关联，暂不把全量回归记录为通过。
- 用户已安装PowerShell 7，后续命令可显式使用`C:\Program Files\PowerShell\7\pwsh.exe`；npm脚本继续使用`npm.cmd`。本轮生成于仓库内的临时发布测试媒体快照已加入精确`.gitignore`规则，规范冻结快照仍保存在`JiaRu_image`外部审核工作区；未执行Git提交或推送。

## 冻结质量报告泛化与发布治理回归清零（v1.1.207）

- 后续项目命令已显式切换到`C:\Program Files\PowerShell\7\pwsh.exe`，npm脚本继续固定调用`npm.cmd`；该约束已提炼到`AGENTS.md`。
- `build-frozen-release-test-quality-report.py`升级为schema v2：从冻结manifest动态复算图片、mask、core/stress和父来源组，兼容历史67图/384 mask与当前100图/554 mask；深验schema v2物化证据、assessment动态标签、预测制品覆盖、baseline图片数，以及baseline、全量512/640、core512、stress512使用同一候选权重和权重SHA-256。
- 正式质量门固定使用部署512输入；全量、core、stress三组必须同时满足绝对box/mask mAP50与相对不退化门，640仅作诊断。少于100图的冻结快照始终拒绝；output与任一输入直接同路径、Windows别名或硬链接同文件时在写前拒绝。
- 真实旧67图全套证据重放为通用`reject_candidate_release_at_deployment_resolution`，保留67/100代表性不足及三组512失败原因；新100图manifest与schema v2评估物化证据深验通过，但没有新候选模型指标，因此未生成或伪造质量PASS。
- 发布治理与训练发布编排测试fixture在写入候选模型字节后，动态写入真实`modelSizeBytes`和SHA-256，满足当前回滚完整性深审；生产逻辑未放宽。原10项夹具型失败全部清零。
- 实施规范新增两项完成清单，进度表新增`M3-T4-FROZEN-QUALITY-REPORT-V2`与`REL-T1-ROLLBACK-FIXTURE-INTEGRITY-002`工程PASS；技术白皮书同步为v1.1.207并纠正旧67图段落，明确当前规范冻结快照为100图/554 mask。
- 当前正式候选训练输入仍为train正样本100/100、val30/30、hard negative1/100；唯一直接素材缺口仍是99张合格hard negative。未启动训练、未修改生产manifest、未执行Git提交或推送。

### 验证

- 质量报告专项：8/8 PASS；真实旧67证据重放正确拒绝，真实新100快照与物化证据契约深验PASS。
- 发布治理与训练发布编排专项：27/27 PASS；包含冻结、返修和质量报告的联合专项：39/39 PASS。
- `npm.cmd test`：630/630 PASS，串行执行。
- `npm.cmd run lint`：PASS。
- `npm.cmd run audit:encoding`：443文件、0失败。
- 五个改动Python脚本`py_compile`：PASS。
- `npm.cmd run build`：Next.js 16.2.9生产构建PASS。
- `npm.cmd run audit:nail-texture-completion -- --release-test-snapshot <100图manifest>`：339个进度标记、329个PASS，13个正式发布门中4个通过，按预期输出HOLD；新质量报告工程PASS没有绕过新候选指标、移动真机、失败案例、Beta、生产资产和回滚证据缺口。

## 冻结质量报告深重放与完成度审计收紧（v1.1.208）

- 后续命令已现场确认使用`C:\Program Files\PowerShell\7\pwsh.exe`，npm脚本继续显式调用`npm.cmd`。
- `build-frozen-release-test-quality-report.py`新增`--verify-report`：从现有schema v2报告读取全部输入，在临时目录重建报告并逐字段比较；固定复验训练禁用、部署512全量/core/stress门、输入集合、外层决定和全部传递证据，拒绝手写决定或输入写后漂移。
- 最终完成度审计直接调用质量报告深重放器，并把报告声明的传递输入纳入统一输出覆盖保护。当代表性100图冻结快照存在时，候选质量门必须取得与该快照绑定且可重放的部署512报告，不能退回历史13图指标或旧67图报告。
- 真实100图快照复跑后，完成度从此前偏松的4/13纠正为3/13并继续HOLD；这是证据门收紧，不是训练数据倒退。新增`M3-T4-FROZEN-QUALITY-DEEP-REPLAY-003`工程PASS后为340个进度标记/330个PASS。
- 复核历史返修口径：旧2026_7_13批次28张返修未计入当前规范train100，属于后续可选扩充；当前开训直接素材阻断仍只有hard negative 1/100、缺99张。未启动训练、未修改生产manifest、未执行Git提交或推送。

### 验证

- Python编译：PASS。
- 冻结质量报告与完成度审计联合专项：41/41 PASS；覆盖手写外层决定、源指标漂移、传递输入覆盖和代表性快照存在时禁止历史指标回退。
- `npm.cmd test`：634/634 PASS，按仓库规则串行执行。
- `npm.cmd run lint`：PASS。
- `npm.cmd run audit:encoding`：443文件、0失败。
- `npm.cmd run build`：Next.js 16.2.9生产构建PASS。
- `git diff --check`：PASS；仅显示Git预期的LF/CRLF转换提示，无空白错误。
- `npm.cmd run audit:nail-texture-completion -- --release-test-snapshot <100图manifest>`：340个进度标记、330个PASS，13个正式发布门中3个通过，决定按预期为HOLD。
- 技术白皮书同步为v1.1.208；实施规范、进度标记和当日日志已同步。
