# Dev Log 2026-07-08

- Added MVP readiness coverage for the baseline green-circle fixture / fallback overlay toolchain so the 5188 reference fixture, batch overlay verifier, and fallback annotation export cannot silently disappear while real training data is deferred.
- Validation passed: targeted readiness test, related fixture regressions, encoding audit, lint, build, full test, whitespace check, and image/model export git-status check.
- Added recognition mask overlay artifact export to single-image and batch nail detection verification; validation passed for targeted, related, and full gates.
- Carried recognition mask overlay paths into first-run/final-audit evidence records; validation passed for targeted, related, and full gates.
- Preserved first-run visual evidence paths, including recognitionMaskPath, in release trace indexes; validation passed for targeted, related, and full gates.
- Preserved first-run visual evidence paths in release history manifests and added aggregate visual-evidence counters; validation passed for targeted, related, and full gates.
- Added release-comparison visual evidence deltas so baseline/candidate trace indexes report first-run output coverage and warn when candidate recognition-mask overlay evidence is missing.
- Connected release-comparison visual evidence deltas to release decisions so candidates that lose first-run or recognition-mask overlay evidence require manual review before promotion.
- Added release-governance pipeline acceptance coverage proving visual-evidence manual reviews skip automatic promotion by default and preserve manual_review status when explicitly allowed.
- Added training-release pipeline end-to-end acceptance for visual-evidence manual reviews so the one-command release entrypoint can explicitly promote while preserving manual_review status.
- Added MVP readiness audit coverage for release visual-evidence governance, ensuring compare, decision, governance, training-release tests, and docs keep the recognition-mask manual-review path intact.
- Added MVP readiness audit coverage for the release-history evidence ledger so performance, quality, failure taxonomy, active-learning, and visual-evidence history coverage cannot silently regress.
- Added explicit deferred MVP readiness validation for non-real-training milestones, keeping strict real data/model gates intact while allowing code/toolchain acceptance to pass when those assets are intentionally out of scope.
