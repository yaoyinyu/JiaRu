# 2026-07-05 Dev Log

## Phase 1 review candidate planning

- Added `model/training/plan-phase1-review-candidates.ts` to generate review candidates for the remaining Phase 1 coverage gates.
- The report ranks complex-background samples and possible negative samples without automatically relabeling data.
- Added tests for candidate ranking, report persistence, CSV output, and safe negative-sample command generation.
- Generated `model/datasets/nail-texture-v1/metadata/phase1-review-candidates.json` and `.csv` for the current 300-image AI nail batch.
- Current report found 28 complex-background candidates and 9 possible negative candidates; negative candidates remain high-risk and require visual confirmation or dedicated negative-image collection.

## Git image cleanup

- Moved the AI nail source image workflow to the external `E:/AI Project/Codex/JiaRu_image` folder.
- Cleaned the unpushed Git history so generated images are not included in the push payload.
- Added ignore rules for `/image/` and generated `model/debug` image artifacts.
## Phase 1 split readiness

- Regenerated the dataset split to 210 train, 45 validation, and 46 test samples.
- Added a readiness gate that rejects empty or materially imbalanced train/validation/test splits.
- Phase 1 readiness now passes with 301 images, 1411 valid masks, one negative test sample, and one complex-background test sample.
- Acceptance: 240 tests passed, ESLint passed, and generated YOLO labels match the split counts.
## Phase 2 training dataset materialization

- Added `materialize-training-dataset.ts` to build Ultralytics-compatible image and label split directories after annotation conversion.
- The materializer preflights every raw image and YOLO label before replacing generated outputs and preserves image split `.gitkeep` files.
- Real dataset acceptance produced 210/45/46 image and label pairs; the Python training dry-run reports the same counts.
- Acceptance: 242 tests passed and ESLint passed.

## Phase 2 training environment preflight

- Added `check-training-environment.py` so real YOLO segmentation training has a no-network preflight before non-dry-run execution.
- The preflight reports materialized train/val/test image counts, Python version, Ultralytics/Torch availability, local checkpoint presence, and whether the first real training run may download the named checkpoint.
- Current machine status: dataset is ready at 210/45/46 images, but `ultralytics`, `torch`, and a local `yolo11n-seg.pt` checkpoint are not installed/present yet.
- Acceptance: training script tests passed, full `npm.cmd test` passed with 244 tests, and ESLint passed.

## Phase 2 non-training safeguards

- Wired `check-training-environment.py` into the release pipeline so real training is blocked before YOLO execution when local dependencies or checkpoints are not ready.
- Added pipeline flags to skip the environment gate for controlled dry runs and to require a local checkpoint when avoiding implicit downloads.
- Added browser runtime manifest validation so invalid, unsafe, or incompatible model manifests fall back cleanly instead of loading unsafe ONNX paths.
- Acceptance: targeted runtime and release-pipeline tests passed, full `npm.cmd test` passed with 248 tests, ESLint passed, and production build passed.

## NailArtPicker text regression guard

- Added a focused regression test for the picker progress labels and selected-finger separator.
- The guard blocks visible mojibake fragments and Unicode replacement characters from returning to `NailArtPicker.tsx`.
- Acceptance: targeted picker text test and text encoding audit passed.

## Model fallback warning classification

- Split model manifest loading errors from model inference/runtime errors in `recognizeNailTextures`.
- Runtime/session/postprocess failures now fall back with `model_inference_error:*` instead of being mislabeled as manifest failures.
- Added `verify-browser-integration.ts --skip-model-artifact` for non-training browser contract checks while the real ONNX artifact is intentionally deferred.
- Acceptance: targeted model runtime/browser integration tests passed; contract-only browser integration passed; strict browser integration still correctly blocks on the missing real ONNX artifact.

## Debug priority runtime warning coverage

- Added new model manifest/inference fallback warning prefixes to debug sample priority scoring.
- Active-learning prioritization now treats `model_manifest_error:*`, `model_inference_error:*`, missing session/tensor, and empty model-output fallback as model runtime warning evidence.
- Acceptance: targeted debug priority test and text encoding audit passed.

## NailArtPicker runtime warning presentation

- Added user-facing prefix handling for model manifest, inference, session/tensor, and empty-output fallback warnings.
- Runtime warnings with details such as `model_inference_error:*` now display as stable fallback guidance instead of raw internal error strings.
- Acceptance: targeted picker quality/debug priority tests and text encoding audit passed.
