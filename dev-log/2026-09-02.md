# 2026-09-02 开发日志

## candidate50严格旧真值复核、训练与val选择

- 续接正式模型Goal前复核白皮书v1.1.555的状态表、正式纹理模型、§11.1、candidate49及最新变更；Git为master、HEAD `a5aef7b`、单一worktree，保留全部既有未提交文档和脚本差异，未清理、重置、提交或推送。
- 重算candidate50隔离库存为44张/14来源组；其中15张存在历史批准真值，但当前逐图原图/overlay复核只接受`00293/00301_11/00301_8/01241`四张/25 mask。其余11张按饰品外扩、锯齿、皮肤污染、低清或边界不确定返修/排除，历史PASS未自动继承。
- 与candidate49合并索引为321张正图/1935 mask；规范物化train481（321正图+160空标签训练负图）、val30/144、test0，记录摘要`a558731c…43cf`、数据树`120013b0…b6e8`、输入审计`b84d4ff5…070e`PASS。
- candidate50从candidate29权重`1524ba9f…6f02`重新初始化，固定640、AdamW 1e-6、freeze10、mosaic0、全分辨率独立mask、CUDA0。训练24轮早停、最佳第12轮，最佳权重45168809 bytes、SHA-256 `a0f603a681ac0553f9263d00ed9536be5ed2abb3aa2b5f17a535e6ee8a64aeec`。
- 直接权重部署512 val30最佳联合点为阈值0.50、125匹配/19漏检/16误检，识别非退化失败。按训练前登记方案评估1%—20%七个candidate29→candidate50融合点；alpha0.01/0.02/0.03在0.45均保持128/16/16，边界F1分别为0.6017576/0.6023045/0.6025056。选择最高的alpha0.03，权重SHA-256 `58e5006c5635f9ebf57a1885791daba377f806ecdbb6187f53d1f958b5540106`，较基线严格提高0.0013767。
- 已生成candidate50 val决定与运行时选择锁；candidate50尚未读取受保护test100。下一步只允许对锁定权重、512、阈值0.45执行一次回归，禁止据test反向调权重、alpha、阈值、样本或后处理。
- 技术白皮书升级v1.1.556并同步最高优先级、正式纹理模型、candidate50状态、§11.1和变更记录；实施规范新增§40，实施进度新增candidate50五项标记，完成度审计新增candidate50结论。产品继续HOLD。
- 完成度机器重放读取494个进度标记、454个PASS；14个正式门仍为4通过/10失败，返回`ok=false`、`decision=hold`，报告SHA-256 `638e352b5f28ec8f9bc41d4a8d7d4c23fd12860dce33993a571b55042671f94d`。candidate49训练等待标记已由candidate50真实训练承接为PASS，candidate50 test100仍明确为READY而非PASS。

## candidate50受保护test100与蒸馏路线复核

- 按读取test前锁定的candidate50-alpha-003权重`58e5006c…0106`、部署512、阈值0.45执行唯一一次受保护test100；总体mask mAP50=0.974，GPU推理约11.7ms/图。
- schema v2逐实例报告为519/554匹配、467完整mask、35漏甲、13重复、16额外、12无效，20图漏甲、55图可直接提取；实例召回0.93682通过，但完整mask比例0.84296、漏甲图片率0.20和加权杂散率0.11372失败。报告`c65e8b35…eb22`构建与只读重放一致，candidate50固定TEST HOLD。
- 相对candidate46只增加1枚完整mask和1张可直接提取图，其余逐实例错误不变。已新增`candidate50-frozen-test100-decision-v1.json`，终止candidate50及其插值轨迹；未导出、登记、接入前端、部署或据test反调。
- 复核OpenAI强教师路线：官方`gpt-image-2`只可生成来源隔离训练候选，接口不提供YOLO蒸馏所需logit、特征层、软mask或边界张量；GPT-5.6 Sol只做语义审核与返修路由。真实本地YOLO11m→YOLO11n多信号蒸馏candidate16已在val30否决，当前不重复该实验。
- 加速主线改为只补充来源隔离的透明低对比、侧视、复杂多手和相邻甲面真实难例完整polygon，达到明确有效增量后训练一个新候选并由val30筛选；禁止使用test100逐图结果选样或调参。
- 技术白皮书升级v1.1.557，同步当前优先级、正式纹理模型、candidate50状态、§11.1和变更记录；实施规范新增§41，实施进度和完成度审计同步candidate50 TEST HOLD与蒸馏边界。产品继续HOLD。
- 完成度深验按预期以退出码1保持HOLD：494个标记/454个PASS，14门仍4通过/10失败，`ok=false`、`decision=hold`，报告SHA-256 `a3f27779dc17586f99b9d5d257b778d1a83b001f72f50c928134f859fa7f42b9`；candidate50 test失败没有被总体mAP误晋升。
- 收尾检查：candidate50决定、运行时锁和完成度报告JSON 3/3解析通过；编码审计817文件/0失败；`git diff --check`与cached差异检查通过，仅有既存LF→CRLF提示。当前仍为master、HEAD `783ef640`、单一worktree；保留全部未提交差异，未commit或push。

## candidate51新来源冻结与上线加速路线复核

- 复核白皮书v1.1.557状态表、正式纹理模型、§11.1、candidate50/test100与最新变更，并核对实施规范§6.4/§41、完成度报告和OpenAI官方图像生成文档；Git仍为master、HEAD `783ef640`、单一worktree，保留全部既有未提交差异。
- 从candidate50训练后44张/14来源组模型无关库存中，在模型或旧mask辅助前冻结5张/4来源组/预计40枚完整甲面；文件名清单SHA-256 `d1fe39422ca54deb12b435581a7beed433beaec2c21826e6091725b1135351a1`。清单只冻结源图，全部保持`trainingUse=prohibited`。
- 复用4张旧候选共31 polygon作返修起点，机器审计0非法、0越界、0贴边，但7对交叠，仅1张机器清洁、3张需返修；报告SHA-256 `880a138f56f31d04667b7b738fe892dfb8d8a39f541583fb1679b19f9eb9828b`。第5张只有1个旧候选，必须新建完整10甲标注。
- 确认OpenAI官方`gpt-image-2`是图像生成/编辑接口，本项目没有其logit、特征层、软mask、边界张量或端到端蒸馏运行报告；唯一真实可微蒸馏仍是已在val30否决的candidate16本地YOLO11m→YOLO11n实验。
- 加速路径调整为双轨：模型轨先返修`00710/00225`，再处理`00624/00625/00846`，达到有效新真值增量后只训练一个candidate51直接候选；工程轨提前用非生产fixture验证ONNX/Worker/`/ar-tryon`替换点，但不修改生产manifest或降低发布门。
- 技术白皮书升级v1.1.558并同步最高优先级、正式模型状态、§11.1和变更记录；实施规范新增§42，实施进度新增candidate51三项标记。产品继续HOLD。
- 完成度机器重放读取497标记/455 PASS，14门仍4通过/10失败，`ok=false`、`decision=hold`，报告SHA-256 `abf42ec17b9f72bfba8c9420b07bb129fec0ceb90d2bfa1fd21cc0fc9ac4489e`；4份相关JSON解析通过、819文件编码0失败、工作区与cached差异检查通过，仅保留既存LF→CRLF提示。未commit或push。

## candidate51首批真值终结与加速路线更新

> 后续更正：见下方“candidate51计数纠正与上线加速复核”。

- 复核白皮书v1.1.558的当前优先级、正式模型状态、§11.1、最新变更，以及实施规范§42、进度与完成度审计；Git为`master`、HEAD `e327f78`、单一worktree，保留全部既有未提交差异。
- 原图事实校正candidate51为5张/4来源组/39枚完整可见甲面；`00710`实为4甲，`00225`为5甲。两张已完成原分辨率整图及逐甲终审、polygon合法性、零交叠与上游哈希重放，终结2张/9 mask训练真值候选。
- 新增`finalize-candidate51-reviewed-training-truth.py`和`build-candidate51-combined-training-truth-index.py`的严格终结/合并合同；Python 3.13编译通过，将首批2张/9 mask与candidate50基线原子合并为323张/1944 mask/107来源组。输出索引SHA-256 `86997f01f16b66bc8e174fb870f960f8c5d818dd109d10a5bb4dc909198011c2`，规范条目SHA-256 `8fdaa682546082bacb1fb93032b042f10dfa96a13773a31ee00913a298ae7e66`，零冲突/冗余，仍为`trainingUse=prohibited-until-materialization-audit`。
- OpenAI `gpt-image-2`端到端蒸馏未实际启动，没有logit、特征层、软mask、边界置信度或学生权重产物；已完成的本地YOLO11m→YOLO11n多信号蒸馏candidate16在val30无合格阈值。不重复失败实验，最短模型路径为完成`00624/00625/00846`剩余3张/30 mask、整批物化后只训练一个candidate51直接候选；非生产ONNX/Worker/`/ar-tryon`替换验证可前置，生产manifest仍HOLD。
- 技术白皮书升级v1.1.559，同步当前优先级、§11.1、变更记录；实施规范§42、进度表和完成度审计已同步。未训练、未读取新测试集、未导出/登记/部署，未commit或push。
- 完成度深验读取498标记/456 PASS，14个正式门仍为4通过/10失败，`ok=false`、`decision=hold`，报告SHA-256 `e2a3da527b80309808b164c56d150a5356d6b6ff7989718d223b307e1a32e53d`；中间真值PASS没有被误报为开训或发布PASS。

## candidate51计数纠正与上线加速复核

- 复核白皮书v1.1.559的状态表、正式纹理模型、§11.1、蒸馏结论和最新变更；Git为`master`、HEAD `e327f78`、单一worktree，保留全部既有未提交差异，未清理、重置、提交或推送。
- `00624`原图与旧候选逐甲复核确认实际为9枚完整可见甲面而非10枚：旧候选含同甲重复、裸指/皮肤误标及相邻甲合并。candidate51源冻结总数从39校正为38，修正后的源冻结文件SHA-256为`025655c9f31bb04259b1d0b87ebe2a239d5d87195242b85d60d3f536a9d93a2e`。
- `00624`最新人工候选已达到9 polygon合法、零同图交叠，但逐甲2倍放大仍发现透明延长甲与装饰外缘边界风险；按完整甲面门保持返修，不写正式PASS、不计入训练输入。
- 既有`00710/00225`审核决定重新绑定修正后的源冻结SHA，并无覆盖地生成`training-truth-00710-v2.json`和`training-truth-00225-v2.json`。与candidate50基线重建的candidate51索引v2仍为323张/1944 mask/107来源组，索引SHA-256 `2b17925685c2ba2e8735292b8f802dd3b5b1c1ae87ff83557da27093bff438d2`、规范条目SHA-256 `33580c22e4a21bbb839335fe68e596954015a2eed98cca8034e453064e7ca993`，零冲突/冗余，继续`trainingUse=prohibited-until-materialization-audit`。
- 上线加速结论不变：OpenAI `gpt-image-2`从未输出可供本地YOLO训练的logit、特征层、软mask或边界置信度，也没有对应学生权重；已完成的candidate16本地多信号蒸馏在val30否决。最短路径是完成`00624/00625/00846`剩余3张/29 mask、物化审计后只训练一个candidate51直接候选，同时前置非生产ONNX/Worker/`/ar-tryon`替换验证，门禁失败即停。
- 技术白皮书升级v1.1.560并同步当前优先级、§11.1及变更记录；实施规范§42、实施进度和完成度审计叙述同步。生产manifest和产品状态继续HOLD。
- 完成度审计重放为498标记/456 PASS、14门4通过/10失败，`ok=false`、`decision=hold`，报告SHA-256 `0864213438603f6d9997863e773ccd2fd18e0729ed284d409bfa48e21c393035`；修正计数与哈希绑定没有误晋升开训或发布门。
- 收尾检查：7份当前/外部JSON解析通过，candidate51终结器与合并索引器通过Python 3.13编译，文本编码827文件/0失败，工作区及cached `git diff --check`通过；仅保留既存LF→CRLF提示，未commit或push。

## 正式模型上线加速与OpenAI gpt-image-2蒸馏状态复核

- 复核白皮书状态表、正式纹理模型、candidate51、蒸馏边界、已知限制和最新变更，并对照当前选择清单、原分辨率决定、candidate51索引v4及完成度报告。
- 纠正文档漂移：`00624`已终审通过并形成9 mask，`00625`因必要甲面相互遮挡整图排除；candidate51中间索引v4为324张/1953 mask/108来源组，SHA-256 `23391490…e86e`，规范条目`0c4289d3…1f9`。`00846`实际8枚可见甲面，8-mask候选已过合法性和零交叠机器门，但仍待逐甲原分辨率终审，未计入正式真值。
- 确认OpenAI `gpt-image-2`蒸馏从未实际启动：不存在logit、特征层、软mask、边界置信度、端到端运行报告或学生权重；唯一真实运行的本地YOLO11m→YOLO11n多信号蒸馏candidate16已被来源隔离val30否决。
- 上线最短路径保持为完成`00846`终审、最终索引和物化审计，只训练一个candidate51直接候选；同时前置非生产ONNX导出、512预后处理、Worker WebGPU/WASM加载和`/ar-tryon`替换点验证。禁止重复失败蒸馏、用test100选样、多候选无依据并跑或降低发布门。
- 白皮书升级v1.1.565，并同步实施规范§42、实施进度candidate51标记和完成度审计叙述。无运行时接口、生产manifest或产品状态变化，产品继续HOLD；未执行commit或push。
- 完成度审计以Python 3.13环境重放：498标记/456 PASS、14门4通过/10失败，`ok=false`、`decision=hold`，报告SHA-256 `b13035d477c29c3cc840afd3f258cbe423047e63659a7afaef239e0ea55f6a87`；中间真值进展未被误晋升为可训练或可发布。

## 外网临时演示访问通道（Cloudflare Tunnel）

- 复核白皮书v1.1.560的状态表、§2.1技术栈、§11已知限制与最新变更；本次仅新增外网访问通道，**未修改任何源码**，`next.config.ts`保持原样。
- 需求为外网全功能演示（含AR摄像头）。清点确认本机未安装任何穿透工具；GitHub release首次HEAD探测返回000属偶发，改用`--http1.1`后正常，下载 cloudflared 2026.8.3（52.29MB）至隔离目录`C:\Users\YaoYinyu\.workbuddy\binaries\cloudflared\`，未写入系统目录、未使用全局npm安装。
- 探测发现3000端口已被PID 34600占用；取其命令行确认为`node …\next\dist\server\lib\start-server.js`，即`next start`生产服务（HTTP），并非`package.json`中带`--experimental-https`的dev脚本。`/`、`/ar-tryon`、`/ar-demo`均200，故选择直接复用、不重启也不接管该进程。
- 关键源码结论：`node_modules/next/dist/server/lib/router-server.js`第295—296行显示`blockCrossSiteDEV`只在`if (development)`分支被调用，生产模式不执行跨站拦截。因此`allowedDevOrigins`无需追加`*.trycloudflare.com`（该选项虽支持通配，但生产模式不读取），避免了一次不必要的配置改动与dev服务重启。
- 以`cloudflared tunnel --no-autoupdate --url http://localhost:3000`建立quick tunnel，分配到`https://<随机子域>.trycloudflare.com`，连入kix04节点；连通性预检DNS/UDP/TCP/API全部PASS，首页响应约1.57s。
- 端到端验证：三个页面经公网均200，`ssl_verify_result=0`，首页标题「甲如 — 美甲试色」与本地一致；`/ar-tryon`响应头`permissions-policy: camera=(self), microphone=()`，隧道域名下`self`与页面同源匹配，摄像头满足安全上下文要求。
- 资源一致性：`/vendor/mediapipe/hands/hand_landmark_full.tflite`经隧道下载5,478,688字节，SHA-256与本地文件逐位一致；JS chunk返回200且内容完整，排除白屏风险。
- 排查教训：曾因`curl -w %{size_download}`配合`-o /dev/null`读到0字节而误判MediaPipe资源缺失，改用落盘后`wc -c`加SHA比对才确认正常——该统计口径不可作为静态资源存在性依据，后续复核静态资源应落盘比对哈希。
- 环境事实记录：`npm.cmd run build`被本工作区`safe-delete`护栏以`SAFE_DELETE_BULK_CONFIRM_REQUIRED`（目标`.next/trace`，count=50达阈值）中止，属环境护栏而非代码缺陷；其在删除阶段即中止、未写入新产物，事后复核3000服务三个页面仍全200，现有服务未受影响。后续在本工作区执行生产构建需先处理`.next/trace`的批量删除确认。
- 技术白皮书升级v1.1.561并新增变更记录；未执行commit或push，保留全部未提交差异。产品继续HOLD。

## 外网摄像头实测（CDP + 合成设备）

- 上一节仅依据响应头推断摄像头可用，缺少真实浏览器执行证据；本节补上实证，**未修改任何源码**。
- 复用本机已安装的 Chrome（`C:\Program Files\Google\Chrome\Application\chrome.exe`）。项目内无 playwright，故未安装 agent-browser 或额外下载 Chromium（约500MB），改用 CDP 直接驱动。
- 方法：headless Chrome 配合 `--use-fake-ui-for-media-stream`（自动授予权限）与 `--use-fake-device-for-media-stream`（合成视频流），经 CDP `Runtime.evaluate` 在真实页面上下文执行 `navigator.mediaDevices.getUserMedia({video:true})`；验证脚本仅依赖 Node 22 内置 WebSocket，无需额外依赖。
- `/ar-tryon` 与 `/ar-demo` 两页结果一致：`window.isSecureContext=true`、`getUserMedia=GRANTED`、视频轨 `fake_device_0`、`readyState=live`、权限查询状态 `granted`。证明两处 `camera=(self …)` 在外网域名下均实际放行摄像头。
- 结论：HTTPS 隧道域名构成安全上下文，AR 全功能演示可正常调用摄像头；`next.config.ts` 的 Permissions-Policy 无需为外网访问改动，包括 `/ar-demo` 中额外的 `http://localhost:8080` 与 `http://127.0.0.1:8080` 条目也不影响 `self` 生效。
- 清理：临时 Chrome profile 已删除，调试端口仅剩 TIME_WAIT、无残留监听进程；隧道服务复核仍为 200。
- 技术白皮书升级v1.1.562并新增变更记录；未执行commit或push，保留全部未提交差异。产品继续HOLD。

## 外网演示功能失效诊断与隧道恢复

- 用户报告外网演示「有些功能无法正常使用」。排查确认根因分两层，均已定位且未修改任何源码。
- 第一层（阻断性）：原 Cloudflare quick tunnel 进程虽仍存活（PID 26812），但到 Cloudflare 边缘的连接已失效，外网访问全部返回 530；本地 3000 源站正常（200）。已 kill 失效进程并以新 quick tunnel 恢复，新随机域名连入 sjc11 节点（比首轮 kix04 更偏境外，国内延迟更高），首页/ar-tryon/ar-demo/ai-generate 均复测 200。
- 第二层（设计性）：部分功能依赖本地环境，与穿透无关，已在白皮书 §11.4 固化：① `/ar-demo` 经 iframe 加载 `http://localhost:8080/?embedded=jiaru-main`（访问者本机的 Python demo），HTTPS 页加载 HTTP 子资源被混合内容策略拦截且对方电脑通常无此服务，外网必然不可用；② AI 生图 `/ai-generate` 的 `AGNES_API_KEY` 已在 `.env.local` 配置、由服务端 `/api/generate-ai` 调 `api.agnes-ai.cn`，外网可用（前提是运行服务的本机可访问该外部 API）；③ 纹理/AR 合成依赖 onnxruntime-web（WebGPU 优先、WASM 回退），远程手机/老旧浏览器不支持 WebGPU 且未回退时识别失败，属设备能力问题；④ 摄像头外网已实测 GRANTED。
- 用户要求改用国内节点内网穿透；下载 cpolar 客户端至 `C:\Users\YaoYinyu\.workbuddy\binaries\cpolar\`（15MB），实测其免费版启动 `http 3000` 报 `user authToken auth failed`（StatusCode 4003），必须有 authtoken 才能建隧道。花生壳同理需账号。待用户提供 cpolar.cn 注册 token 后再建立国内稳定隧道；届时 `/ar-demo` 的本地 8080 依赖仍需单独处理（同隧道路径或改同源）。
- 技术白皮书升级v1.1.563：新增变更记录并补充 §11.4「外网演示与内网穿透已知限制」；未执行commit或push。产品继续HOLD。

## 一键隧道脚本（脱离会话常驻）

- 用户希望推进但不愿反复依赖后台进程启动隧道；新增两个双击即用脚本至 `C:\Users\YaoYinyu\.workbuddy\tmp\`：`start-jiaru-cloudflare.cmd`（调 `cloudflared.exe tunnel --url http://localhost:3000`，把随机域名实时写入 `jiaru-tunnel-url.txt`）与 `start-jiaru-cpolar.cmd`（含 `TOKEN` 占位，需用户填入 cpolar.cn 的 authtoken 后双击即可建国内节点隧道）。
- 当前 cloudflare 隧道（sjc11 节点）复测仍 200，地址 `https://blanket-games-ross-sperm.trycloudflare.com`。
- 待用户注册 cpolar/花生壳并提供 authtoken 后，即可切换到国内节点稳定隧道；届时仍需处理 `/ar-demo` 的本地 8080 依赖（同隧道路径或改同源）才能真正全功能演示。未执行commit或push。

## Seedream 5 系列接入实施与验证（火山方舟）

- 已读取白皮书（v1.1.563）§2 技术栈、§3 状态总表 AI 美甲生图行、§4.4 `/ai-generate`、§5.1 `POST /api/generate-ai` 及最新变更记录后实施。用户拍板范围：pro + lite 都接；v1 仅单图文生图 + 图生图；前端可选 Seedream；为 Seedream 另写精简系统提示词；不做图片长期留存。
- 半分离实施（Agnes 链路零改动）：新增 `src/lib/seedream-image-size.ts`（pro=1K/1.5K/2K、lite=2K/3K/4K × 8 比例 → 显式 WxH 表，Ark 无 `ratio` 参数）、`src/lib/seedream-prompt.ts`（约 100 字专用系统词，独立于 Agnes 两套长提示词，用户提示词上限 300 字）、`src/lib/seedream-image-api.ts`（扁平 body、顶层 `error{code,message}` 与 `data[0].error` 归一化、429/500/503 退避重试、240s 超时、`watermark` 默认关）、`src/app/api/generate-seedream/route.ts`（`maxDuration=300`，非法组合调用外部前 400）；`/ai-generate` 前端新增「生成引擎」切换（Agnes 默认），切引擎联动尺寸档位与提示词上限。
- `.env.local.example`、`.env.local`（gitignored，写入用户临时 Key 与两个 Model ID）、README 已同步。
- 实测（用户提供的临时 Key）：`GET /api/v3/models` 发现真实 Model ID 必须带日期后缀，pro=`doubao-seedream-5-0-pro-260628`（端到端文生图 19.7s 成功，产物 186KB 有效 JPEG，已下载至 `.workbuddy/tmp/seedream-sample-pro.jpg`）；lite 档=`doubao-seedream-5-0-260128` 返回 "has not activated the model"，需方舟控制台开通或改映射 4.5/4.0。
- 验证：新增 3 个测试文件 17 用例全过；相关域测试（agnes/ai-image-size/ai-hand-anatomy-prompt/seedream）35/35；路由校验冒烟 6/6（Node loader hook 直接调用真实 `POST` handler，零外部调用）；ESLint 0 告警；tsc 对 src 与新测试零报错。
- 全量回归：`npm.cmd test` 761 项，首跑 519/240 失败、复跑 183 失败（不稳定）。经干净 HEAD worktree（`git worktree add --detach`）复现证明失败全部为既有的甲面纹理训练/审计管线问题（如 `tests/model-training-python-scripts.test.ts`、`tests/protected-hard-negative-registry-update.test.ts` 在无本次改动的 HEAD 上同样 7 过/19 败），与 Seedream 改动无关；worktree 已清理。
- git 事故与恢复：跑全量测试期间尝试 `git stash` 触发 `BUG: diff-lib.c` 与 `fatal: bad object HEAD`；`git fsck` 显示大量 commit/tree 对象丢失（仅剩 12 个松散对象，HEAD `a26b825` 不可解析，reflog 含无效条目）。因远端持有同一提交（`git ls-remote origin master` = `a26b825…`），执行 `git fetch origin master` 非破坏性恢复全部缺失对象；`git cat-file -t`、`git status`、`git log`、`git diff --stat HEAD` 均恢复正常，全部未提交改动（README、page.tsx 等）完好，`git fsck` 仅剩无害 dangling 提示。根因未明（时间窗 12:24—12:37 与重负载测试套件重合，疑似测试子进程误删 `.git/objects` 松散对象，另当日 01:34 有 multi-pack-index 写入记录）。
- 技术白皮书升级 v1.1.564：同步 §2.1 技术栈（新增 Ark 行）、§2.2 数据流、§3 状态总表（新增 Seedream 行，状态"待验证（pro 实测通过；lite 待账号开通）"）、§4.4 引擎切换说明、新增 §5.2 `POST /api/generate-seedream` 契约及变更记录。未执行 commit/push。
- 待办：①lite 模型开通（或改映射 4.5/4.0）；②重启服务加载 `.env.local`；③前端浏览器验收引擎切换；④建议尽早人工 commit 保护现场，并排查测试管线删除 `.git` 松散对象的原因。

## candidate51最终物化、OpenAI蒸馏复核与单候选开训

- 读取技术白皮书v1.1.565的状态总表、candidate51正式纹理模型主线、candidate16蒸馏结论、§11已知限制及最新变更；Git为`master`、HEAD `a26b825`、单一worktree，保留全部既有未提交差异，未清理、重置、提交或推送。
- OpenAI `gpt-image-2`蒸馏从未实际运行：无logit、特征层、软mask、边界置信度、端到端报告或学生权重；唯一实际多信号蒸馏为本地YOLO11m→YOLO11n candidate16，已被来源隔离val30否决。因此不再重复教师支线。
- 完成`00846`的8枚完整可见甲面原分辨率终审；与`00710/00225/00624`合计4张/26 mask，`00625`继续因遮挡整图排除。最终合并索引v5为325张/1961 mask/109来源组，索引`d040d11f…c8f8`、规范条目`2b4c7d54…b88`。
- 无覆盖物化为485张训练图（325正样本+160困难负样本）、val30/144 mask、test=0、孤儿文件=0；物化报告SHA-256 `cb9c67795c26d0faafd5f1e5ddd98f027fefa7dcc0e6b8386d3607f455cfab77`，数据文件树`de5387b8…2800`。
- 审计v1/v2使用自定义最低正样本325，训练器按正式最低合同100深重放时因报告字节不同而拒绝dry-run；数据树与角色计数没有变化。按正式合同无覆盖生成审计v3（SHA-256 `4301d117…1892`），深重放与dry-run通过。
- 13:58启动唯一candidate51直接监督训练：candidate29-alpha-015初始化、640、40 epochs、patience 12、自动batch（实测选择2）、CUDA 0、workers 0、冻结10层、AdamW `1e-6`、mosaic=0、mask-ratio=1、overlap-mask=false、无蒸馏。训练启动不构成val/test/发布PASS，产品继续HOLD。
- 白皮书升级v1.1.566，并同步实施规范§42、实施进度和完成度审计叙述；运行时接口与生产manifest未改变。
- 完成度深验读取498个进度标记、457个PASS，14个正式门仍为4通过/10失败，`ok=false`、`decision=hold`，报告SHA-256 `bcf2888651f263ab7c72d1b1c7b1a69a41d099f142b6cdfb52718efd88a5a7cf`；输入PASS和训练启动未被误报为可发布。

## Git 当前状态只读诊断

- 已读取并复核技术白皮书状态总表、§11 已知限制及最新变更记录；本次只判断 Git 当前健康状态，不修复、不清理工作区。
- `HEAD` 为 `a26b82502f5078b4443323f2d57725331bef7460`，`git cat-file -t HEAD` 返回 `commit`；`git fsck --full --no-reflogs` 退出0，无 missing/bad object，仅报告不影响当前提交图完整性的 dangling commit/tree。
- `.git/index.lock`、`MERGE_HEAD`、`CHERRY_PICK_HEAD`、`REVERT_HEAD`、`rebase-merge` 与 `rebase-apply` 均不存在，排除锁文件、合并、拣选、回滚或变基中断。
- `git status` 的 `master...origin/master [gone]` 仅表示本地 `refs/remotes/origin/master` 缺失/过期；`git ls-remote --symref origin HEAD` 已确认远端默认分支与 `refs/heads/master` 均存在，且远端提交与本地 `HEAD` 同为 `a26b825…`，因此不是远端分支丢失或提交未推送。
- 工作区保留15个已修改路径和11个未跟踪条目；`git diff --check` 退出0，仅有既存 LF→CRLF 提示。未执行 fetch、跟踪配置修改、reset、clean、commit 或 push。
- 技术白皮书新增 v1.1.567 变更记录；无运行时接口、模型、数据集或产品HOLD状态变化。

## Git 恢复一致性复核

- 重新读取白皮书状态总表、§11 已知限制及最新v1.1.568/v1.1.567记录，并再次执行当前仓库只读检查。
- 数据层面已恢复到损坏前：本地与远端`master`均指向`a26b82502f5078b4443323f2d57725331bef7460`，`HEAD`可解析为commit；`git fsck --full --no-reflogs`退出0，无missing/bad object，仅有无害dangling commit/tree。
- 损坏前记录的15个已修改路径和11个未跟踪条目全部仍在。当前为15个已修改路径、12个未跟踪条目；多出的`model/training/candidate51-validation-decision-v1.json`以及白皮书v1.1.568内容属于恢复后正常推进，不是恢复遗漏。因此当前不是损坏前静止快照的逐字节副本，而是完整恢复后继续工作的状态。
- 唯一残留是本地`refs/remotes/origin/master`尚未刷新，`git status`仍显示`[gone]`；只读远端查询确认远端默认分支和`refs/heads/master`实际存在且与本地同提交。普通`git fetch origin`即可刷新此元数据，本次未执行。
- `git diff --check`退出0，仅报告既存LF→CRLF提示。未执行fetch、reset、clean、commit或push；白皮书新增v1.1.569记录，无运行时接口、模型、数据集或产品HOLD状态变化。

## candidate51训练完成、val30终止与上线提速结论

- 复读白皮书状态总表、正式纹理模型主线、§11.1阻塞项、OpenAI蒸馏结论和最新变更，并以训练汇总、权重、正式512 val30指标及预测工件为机器事实来源。
- candidate51完成16轮直接监督训练，最佳权重45,167,081 bytes，SHA-256 `403e24cac52813e60ee8fd7bf97df1576200af0ea72876eac23cf7ed6dcaa322`；训练汇总SHA-256 `3e0a42dcc8252095a971fcc4eddb272914453a61aeee0d563a92a42a7aa798bb`，`distillation=null`。
- 正式部署512的来源隔离val30为box mAP50 `0.95226`、mask mAP50 `0.87627`、mask mAP50-95 `0.54533`，指标报告SHA-256 `740eee8604e152e125033fec0430641aa570a40a1c895b68bfb17270927b7b3e`。
- 完成粗扫与0.005细阈值扫描；细扫报告SHA-256 `fff51cec8631e48c5db91c4350897c74bfe03b9b3be364162ba0a7f0e9d7cd80`并绑定权重深重放PASS。0.46为129匹配/15漏检/17误检，0.465为127/17/17，0.47为127/17/16；无阈值满足相对candidate29-alpha-015的至少128匹配、至多16漏检、至多16误检合同。
- 新增`model/training/candidate51-validation-decision-v1.json`，正式在val阶段拒绝candidate51。因识别非退化门先失败，不运行边界晋级、test100、插值、导出、登记、前端接入或部署。
- 再次确认OpenAI `gpt-image-2`从未执行端到端YOLO蒸馏，无logit、特征层、软mask、边界置信度或学生权重；唯一实际本地YOLO11m→YOLO11n蒸馏candidate16已被val30否决。下一轮停止重复蒸馏和同输入盲调，只由来源隔离新完整甲面难例与一项预注册可验证训练改动驱动单一候选；非生产ONNX/Worker/浏览器替换验证可前置。
- 技术白皮书升级v1.1.568，同步状态表、§11.1和变更记录；实施规范新增§43，实施进度与完成度审计同步。生产manifest和产品继续HOLD，未commit或push。
- 完成度机器重放读取499标记/458 PASS，14门4通过/10失败，`ok=false`、`decision=hold`，报告SHA-256 `c92ae1944f3d4e7d222518ea77647c5163be68cb5c182724939d42c1e142a5ff`；candidate51训练里程碑没有覆盖val失败或发布HOLD。

## 非生产ONNX/Worker/AR纹理写回浏览器预验

- 复读白皮书v1.1.568状态表、§11.1、实施规范§43和最新变更后，从现有源码核对ONNX导出、manifest、Worker、WebGPU/WASM、像素mask与`/ar-tryon`替换链；未读取或导出candidate51，不修改生产manifest。
- 首轮静态预验发现生成夹具仍要求两候选场景`firstSuggestedFinger=1`，但当前模型后处理在不足完整手指序列时按契约返回`null`，手指由后续UI独立分配；规范夹具和专项测试也已期望`null`。修正`model/fixtures/nail-texture-model-output-sample.generated.json`后，smoke模型SHA-256/大小完整性通过、浏览器集成16项契约全过、Worker/运行时/工件/后处理相关33项测试全绿。
- 按playwright技能在隔离3001端口显式加载`/models/nail-texture-seg-smoke/manifest.json`。真实Chromium上传来源隔离甲图后，UI报告“模型识别 / WebGPU”、模型`nail-texture-seg-smoke-v1`、2个带mask高置信候选、总耗时1652ms、后台线程1421ms；manifest、ONNX及ORT WASM资源均HTTP 200。
- 把两个候选分配为拇指/食指并点击“提取所选纹理”后，弹层关闭且右侧出现“拇指纹理”，证明像素mask提取和AR纹理槽位写回成功；控制台0错误/0警告。浏览器与3001开发进程均已关闭，无残留服务。
- 白皮书升级v1.1.570并新增变更记录；实施规范§43、实施进度和完成度审计同步。该预验仅为非生产工程PASS，不代表识别质量、生产ONNX、真机、Beta或发布PASS，产品继续HOLD；未commit或push。
- 完成度机器重放读取500标记/459 PASS，14门4通过/10失败，`ok=false`、`decision=hold`，报告SHA-256 `537cef2f15d84ab95e2289c10ce66ae7a3e6987726ff73d33afdb0bb37b27395`；非生产浏览器链PASS没有误晋升生产门。

## 上线加速与OpenAI image2蒸馏复核

- 复读白皮书v1.1.570状态总表、candidate16/51、§11.1、实施规范§43、实施进度和最新变更，并核对当前Git为`master`、HEAD `c5e74594c2a24fe52349385d33e28551686c1414`、单一worktree；保留全部既有未提交差异，未清理、重置、commit或push。
- 结论不变：OpenAI `gpt-image-2`从未输出本地YOLO蒸馏所需的logit、特征层、软mask、边界置信度或学生权重；candidate16是本地YOLO11m→YOLO11n蒸馏且已被val30否决，candidate51是无蒸馏直接监督候选并在0.46以129匹配/15漏检/17误检被正式val拒绝。
- 为减少完整训练后才在val失败的循环，锁定下一候选加速合同：只从train角色按稳定`sourceGroup`建立影子训练/开发子集，最多执行两项短程单变量配方比较；胜出配方再用完整train训练一个正式候选，并只执行一次来源隔离val30正式校准。test100和发布留出不参与选择。
- `gpt-image-2`只保留为来源隔离候选源图生成/编辑工具；所有产物仍须通过源图质量、完整甲面mask原分辨率终审和来源隔离门，禁止把生成图片或文字判断登记为张量蒸馏。
- 白皮书升级v1.1.571，实施规范新增§44，实施进度和完成度审计同步；运行时接口、生产manifest及产品HOLD状态未改变。
- 完成度机器重放读取502标记/460 PASS，14门4通过/10失败，`ok=false`、`decision=hold`；报告SHA-256 `deae3faf976c82a2cb7fe633875eb675b80a862c04f01c26ef913d8f9d85f87b`，提速计划没有误晋升任何正式发布门。

## candidate52既有库存止损与OpenAI生成源图冻结

- 复读白皮书v1.1.571状态总表、candidate16/51、§11.1、实施规范§44及最新变更记录；Git保持`master`、HEAD `c5e74594c2a24fe52349385d33e28551686c1414`、单一worktree，未清理、重置、commit或push。
- 以candidate51当前权威训练索引重放全部源筛选库存：1166张筛选记录、565张源图门通过，555张因来源组已被train使用排除，实际仅余10张/8来源组，不再采用旧44张或51张聊天基线。结合历史原分辨率证据和本轮复核，10张全部因裁甲、遮挡、镜像重复、立体装饰遮边、低清过曝、计数冲突或多手交叠排除；形成`candidate52-source-disposition-v1.json`。
- 既有库存耗尽后使用OpenAI内置图像生成创建3张独立正样本候选，复制到`E:\AI Project\Codex\JiaRu_image\真实素材\candidate52_openai_generated_positive_v1`且不覆盖已有文件。三图尺寸分别为1024×1536、1536×1024、1024×1536，SHA-256为`c5751486…d488`、`43c4c4d0…975d`、`913138e3…33ea`。
- 原分辨率源图初筛确认3张/3来源组/预计20枚甲面完整可见，无甲面触边、相互重叠、文字水印或明显结构错误；冻结清单为`candidate52-generated-positive-source-selection-v1.json`。由于内置接口没有返回可核验的精确模型ID，只登记为OpenAI内置生成，不宣称确定由`gpt-image-2`产生，也不称为logit/特征/软mask/边界置信度蒸馏。
- 3张源图仍需逐甲完整polygon和原分辨率终审；完成前`trainingUse=prohibited`，未物化训练输入、未启动candidate52、未读取val30/test100/发布留出，生产manifest及产品继续HOLD。
- 白皮书升级v1.1.572，同步状态总表、正式模型行、§11.1阻塞项和变更记录；实施规范新增§45，实施进度和完成度叙述同步。
- 完成度机器重放读取504标记/462 PASS，14门4通过/10失败，`ok=false`、`decision=hold`；报告SHA-256 `b02de70bd2000112bf9c21c0850b225ad927f7430c85f7273378844e20a52113`。源图冻结与库存止损没有误晋升训练、val或发布门。
