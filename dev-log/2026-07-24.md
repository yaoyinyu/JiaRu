# 2026-07-24 开发日志

## AI困难负样本严格审核与候选训练

- 阅读并按白皮书“数据集治理、困难负样本准入、候选输入门、已知限制”执行。
- 清点626张AI图：0损坏、0完全重复；原分辨率严格通过99张，剔除模糊、裁断、模板、独立甲片及含真实人类美甲甲面的图片。
- 99张与历史1张合并为100张批准困难负样本；100/100/30候选输入物化与深审PASS，权威冻结test100张来源隔离PASS。
- 修复候选输入审计未排除内部物化报告、Ultralytics训练后写入label cache污染数据树、嵌套core/stress评估stem顺序三项问题。
- v6续训50轮完成，权重SHA-256 `bcb145b59fbb3fc305c90194e0eb1d7523fee981964793b60721f4f7fa091a50`；冻结100图/554 mask在512下box/mask mAP50=0.965/0.960。
- 结果仍为候选：AI负样本右下角系统性水印/模糊角标存在捷径学习风险，待完成水印消融、负样本误检、core/stress质量报告和全量串行回归后再判定。

## 文档同步

- 白皮书新增“2026-07-23 AI困难负样本与首个正式候选”状态行，并明确水印消融未完成时禁止生产晋升。
- `AGENTS.md`新增AI困难负样本水印捷径审计的长期规则。

## 冻结质量报告与水印消融收口

- 新增`audit-hard-negative-watermark-shortcut.py`，从批准困难负样本清单深度重放100张图片，使用8线程生成原图、裁右下12%和右下角模糊三类不可变派生；分别在0.35部署阈值和0.20诊断阈值统计误检。
- 部署阈值原图/裁剪/模糊误检图为0/2/0，诊断阈值为3/5/2；裁剪变体比原图增加2个检测，严格零误检、零变体delta门失败，决定`hold_hard_negative_watermark_shortcut_instability`。两张部署误检分别为玩偶模型手和弧面商品，均属于必须压制的目标域困难负样本。
- 水印审计使用的是训练负样本，因此即使原图为0误检也固定`releaseGeneralizationEligible=false`。现有527张严格拒绝项不回收；下一步需不同生成批次/提示族且最好无统一水印的独立留出候选，建议补充150—200张，以同等审核筛出不少于100张，其中至少30张永不参与训练或调参。
- 冻结100图部署512评估完成：full box/mask mAP50=0.9654/0.9601，core78=0.9715/0.9718，stress22=0.9436/0.9240；全量640诊断=0.9657/0.9623。schema v2质量报告决定`accept_candidate_release`并通过只读深度重放，但该结论只覆盖正样本mAP门，不能覆盖困难负样本HOLD。
- 修复`build-frozen-release-test-quality-report.py`在Windows重放子进程中按固定UTF-8解码系统代码页输出造成的后台异常；改为字节捕获，仅失败时以UTF-8容错解码。完成度审计调用Python深验时显式设置`PYTHONIOENCODING=utf-8`，中文外部路径身份核对恢复正常。

## 验证与最终状态

- Python编译通过；全量串行测试640/640通过；水印派生、冻结质量与完成度专项47/47通过；446文件编码审计、ESLint和Next.js 16.2.9生产构建通过。
- 完成度审计刷新为343个标记/332个PASS、13个正式门中4个通过，决定仍为`hold`。已解除“缺99张困难负样本、未训练”和“新100图无候选质量报告”两项历史事实，新增独立困难负样本留出/水印捷径阻断。
- 白皮书升级为v1.1.211，同步功能状态、数据治理、候选训练、冻结质量、已知限制和版本记录；实施规范及进度表同步当前证据。生产manifest、共享阈值和正式ONNX均未修改；未执行Git提交或推送。
