# 2026-08-15 开发日志

## 全面扫描：项目进展盘点（诊断任务）

- 任务启动完整复读`AGENTS.md`并读取白皮书v1.1.427的功能状态总表、正式纹理模型、candidate7训练方案与最新变更记录，以最新开发日志2026-08-14.md为基线。
- Git现场：`master@b21fbce`（2026-08-14 15:46）、单worktree；工作区未提交1处脚本修改（`build-reviewed-manual-polygon-repair.py`，+46/-2，新增`repairInvalidSourcePolygon`自交拓扑修复与`repairInvalidSourcePolygon`选项）及9个`candidate7-secondary-*.json`（8-14 15:50—16:20生成）。未提交、未推送、未执行任何Git操作。
- **关键发现：candidate7严格真值已达成80张固定目标。** 机器可读权威索引为最新事实：
  - 旧真实严格真值索引`candidate7-training-truth-index-v1.json`：`approved_unique_training_truth_index`、33张/237 mask、0拒绝/冗余/冲突（8-14 13:11）。
  - 新正样本严格真值索引`training-truth-index-v1.json`（`2026_8_14_candidate7_new_positive_training_truths_v1`）：`approved_unique_training_truth_index`、47张/250 mask、0拒绝/冗余/冲突（8-14 16:23:35），policy继续`trainingUse=prohibited-until-materialization-audit`。
  - 两索引按`fileName`交叉对比为0重叠；合计**80张/487 mask**，满足“新增不少于80张”的严格真值目标。
  - 增量构成：白皮书v1.1.427时新正样本为27张/150 mask；8-14下午继续晋升20张/100 mask（seq 028—047，含此前旧真实返修项），其中seq 043—047（`nail_00585/01087/01101/01133/01135`）对应工作区未提交的secondary预标注polygon返修决定，truth-043—047于16:22:52—53生成。
  - 结论：80张目标按机器索引已达成，但白皮书v1.1.427、README和dev-log/2026-08-14.md仍记录60张/387 mask“缺20张”，文档滞后于机器事实，需在同一任务内同步（本日志同批完成）。
- 其余核对：生产manifest `public/models/nail-texture-seg/manifest.json`仍为`nail-texture-seg-v1`（inputSize 640），未被任何候选替换；`AGNES_API_KEY`已配置；完成度审计仍`ok=false`、`decision=hold`（符合发布门HOLD预期）。
- 验证尝试：`npm.cmd run lint`因当前沙盒对`辅助材料\real-release-test-2026-07-22\frozen-reviewed-candidate-v2`等目录无读权限而EPERM中断，属环境限制而非代码错误，未作为结论；未运行全量测试或构建。
- 未运行任何训练、候选模型推理、val/test、导出、登记或部署；80张真值仍整体`trainingUse=prohibited-until-materialization-audit`，candidate7精确训练授权、物化输入与GPU启动尚未形成，正式模型继续HOLD。

## 工作区提交与根目录清理（用户授权）

- 用户明确授权提交工作区、清理根目录垃圾、ESLint忽略`辅助材料/**`；根目录大文件不迁移。
- 垃圾盘点：7个已被Git跟踪的根目录文件确认均为调试残留——`({i`为脚本报错日志（SyntaxError输出），6个`_*.ps1`内容均指向已废弃的`E:\AI Project\ClaudeCode\JiaRu`路径的一次性输出重定向脚本。`git rm`删除并同步移除工作区文件。
- 保留核对：辅助材料目录1321个被跟踪文件为审计证据JSON（授权/审核/筛查记录），.archive归档（https-dev.mjs）、.playwright-cli均非垃圾，未动；大模型权重按用户要求未迁移。
- `eslint.config.mjs`在globalIgnores新增`辅助材料/**`，避免全量lint误入外部证据资产目录。
- 分两个提交：`3645eae`（candidate7 secondary预标注polygon返修truth 043—047与`repairInvalidSourcePolygon`拓扑修复）；`507e6f9`（80张/487 mask文档同步、根目录垃圾清理、ESLint忽略项）。工作区干净、暂存区为空、未push。
- `npm.cmd run lint`在忽略辅助材料后通过（exit 0，无输出错误）；白皮书同步至v1.1.429。未运行训练、推理、val/test、导出、登记或部署。

## 隐私静态说明页完成（/privacy 修订）

- 用户要求完成隐私政策静态说明页。任务启动复核白皮书v1.1.429状态表、§10隐私安全与资源管理及源码事实。
- 源码核对结论：项目不使用Cookie/localStorage/IndexedDB/账户/云同步；编辑器`NailCanvas`“保存到本地”、AI页“保存设计到本地”均为用户主动下载；AR页无保存按钮；AI页已有“只发送文字描述”提示；识别在浏览器Worker/主线程本地运行。
- 重写`src/app/privacy/page.tsx`：核心原则三卡（本地优先/不上传照片/不追踪）+各功能数据流向四卡（试色本地处理、AI生图仅文字且经第三方Agnes并提示勿输入个人信息、AR内存处理不录制不存储不提供保存、识别与图库本地）+第三方/权利/政策更新三卡；保留联系区与卡片视觉风格，更新日期改为2026年8月15日。修正原“保存的试戴效果图”领先文案。
- 验证：ESLint通过（exit 0）、无测试引用隐私页、`git diff --check`通过；白皮书同步至v1.1.430（状态表隐私行“待验证”→“已完成（产品文案；法律合规审核仍待外部）”，§10修订说明）。未提交（用户未要求）；未运行训练、推理、val/test、导出、登记或部署。
- 按用户提供把联系邮箱从占位`hello@jiaru.app`替换为`3181484805@qq.com`（`/privacy`页与首页页脚两处）；白皮书同步至v1.1.431，ESLint通过。

## /privacy 分组标题视觉层级调整（用户测试反馈）

- 用户测试反馈：卡片hover上浮遮挡分组标题、分组标题与下方卡片间距不足、"核心原则"等分组标题层级混乱（原为text-xs 12px小字，比正文还小）。
- 调整`GroupHeader`组件：顶部间距`mt-4`→`mt-10`、新增底部间距`mb-6`（解决上浮遮挡与间距问题）；字号`text-xs`→`text-base`（12→16px）、`font-semibold`→`font-bold`、字距`tracking-[.22em]`→`tracking-[.12em]`、颜色由浅粉`#B95F87`改为深色`#4D464A`（与卡片标题同色系，形成 页面标题>分组标题>卡片标题>正文 的清晰层级）。
- dev server热更新已编译（Compiled in 200ms），/privacy返回200。白皮书同步至v1.1.432（简短变更记录，无接口变化）。

## /privacy 视觉重设计（用户要求，当前版本已存档）

- 用户确认分组标题样式“这版可以”并要求保留——已提交存档`97c7c9b`（含隐私页重写、真实邮箱、分组标题间距修复全部改动），工作区随后干净。
- 重设计`src/app/privacy/page.tsx`（文案全部保留，只升级视觉）：
  1. 新增**承诺横幅**：Hero下方渐变玻璃条，三项承诺（🔒本地优先处理 / 🚫照片绝不上传 / 👁️不追踪·不存储）；
  2. 全部10张卡片升级：**emoji图标渐变圆角容器** + 右上角**数据流向徽章**（试色/识别“数据：不出设备”、AI“数据：仅文字→第三方”、AR“数据：仅内存”）+ **大号背景水印数字**（01—10，hover时加深）；
  3. 分组标题保留用户认可的`mt-10/mb-6`+16px加粗深色样式；联系区按钮改渐变与hover变色、圆角加大。
- 验证：/privacy返回200且包含新横幅与徽章内容，dev server热更新生效；ESLint结果待核。白皮书同步至v1.1.433；新设计未提交，待用户确认后再提交。

## /privacy 水印序号放大与承诺横幅删除（用户反馈第二轮）

- 用户要求：卡片水印序号增大并添加“从右上方向左下方逐渐缩小”的渐变效果；删除承诺横幅（🔒本地优先处理/🚫照片绝不上传/👁️不追踪·不存储）。
- 修改`SectionCard`水印：字号`text-[92px]`→`text-[150px]`，改用`bg-gradient-to-bl from-pink-200/90 via-pink-100/45 to-transparent bg-clip-text text-transparent`实现右上实、向左下透明淡出的渐变缩小视觉效果（hover加深）。
- 删除`promises`数组与Hero承诺横幅JSX（页面直接进入核心原则组）。
- 验证：/privacy返回200、横幅内容已移除、渐变水印类名存在；ESLint结果待核。白皮书同步至v1.1.434；未提交。
- 按用户反馈把水印序号位置由`-right-4 -top-12`（只露右下角）改为`right-6 top-3`完整露出并稍向中心；白皮书同步至v1.1.435。
- 用户反馈“移动多了”，水印位置回退为`right-0 top-0`（贴右上角完整露出）；白皮书同步至v1.1.436。
- 用户反馈微调：水印编号`text-[150px]`→`text-[120px]`缩小，位置`top-0`→`-top-3`轻微上移；白皮书同步至v1.1.437。
- 用户反馈：数据流向徽章（如“数据：不出设备”）从卡片右上角移到主标题（如“图片试色”）后，标题行改`flex flex-wrap items-center gap-x-2.5`，徽章缩小为`text-[10px] px-2.5 py-0.5`，右上角恢复为小圆点；白皮书同步至v1.1.438。
- 用户反馈：删除与水印编号叠加的右上角小圆点；白皮书同步至v1.1.439。

## README 同步与本次提交（用户要求）

- README功能总表隐私行由`📌 静态说明页`更新为`✅ 完整静态说明页：核心原则+各功能数据流向+第三方与用户权利；法律合规审核仍待外部`；隐私与数据节补充完整说明链接、Agnes第三方文字转发与勿输入个人信息提示、无Cookie/账户/云同步及联系邮箱。
- 白皮书同步至v1.1.440；按用户要求提交本次git更新（隐私页视觉重设计多轮微调+README同步+白皮书v1.1.430—440+dev-log）。

## /privacy 图标升级为SVG矢量图标（用户要求）

- 用户要求把emoji换成更有设计感的矢量图形；沙盒禁止直连CDN（unpkg/jsdelivr/GitHub raw均SSL失败），改用web_search查证[Lucide](https://github.com/lucide-icons/lucide)（ISC许可、Feather风格24×24描边图标）作为设计参考。
- 手写内联SVG（viewBox 24、strokeWidth 2、round端点、无填充、`stroke:currentColor`随容器粉色）：01盾牌、02云端禁止、03眼睛禁止、04画笔、05火花、06相机、07放大镜、08链环、09盾牌对勾、10文档文本；新增`Icon`组件按name分发，`SectionCard`渲染，无外部依赖、离线可用。
- 验证：/privacy返回200、页面含`<svg>`、全部emoji已移除；ESLint结果待核。白皮书同步至v1.1.441；未提交。

## AI 生图提示词库优化（用户要求）

- 用户要求优化`/ai-generate`点击风格按钮后填入的提示词。核对`src/lib/ai-style-prompts.ts`（10风格×49—50段）发现三类问题：①500段开头全部为同一句式“展现XX美甲的女性手部近景生活照。”；②中英混杂（medium/tiny/confetti/swirl/cameos/henna/Art Deco/Gatsby风共约45处）；③少量语病冗余（如“背景为带碎花桌布的桌面”重复、“背景为都市混凝土墙面虚化”语序颠倒、“手轻扇香烟”错字、“白色雪点的大杏仁甲”笔误）。
- 新增可复验脚本`model/training/optimize-ai-style-prompts.py`：开头按索引轮转5种句式（近景生活照/特写镜头/近景特写展示/微距视角/自然光中），中英词字面替换（修复`\bmedium\b`因中文属`\w`不匹配的bug），语病修正；校验10风格×50段、开头均属5变体、无英文残留。第一轮`\b`版仅替换2处，修复为字面替换后45处全部清理；脚本幂等（重跑changed=0）。
- 补全5个原本只有49条的风格（欧美/复古/节日/水墨/花草）各1条新提示词（天文星图/维多利亚藤蔓/初冬银杏/寒江渔翁/向日葵田），统一变体4开头。最终500条，分布v0=96/v1=104/v2=100/v3=100/v4=100，校验OK。
- 验证：/ai-generate返回200；ESLint结果待核。白皮书同步至v1.1.442；未提交。

## AI 提示词美颜化（用户反馈：目标用户年轻群体）

- 用户反馈“不要太写实，捎带一些手部美颜，目标用户是年轻群体”。分析500条提示词共37种“皮肤…”短语（128×皮肤自然、111×皮肤真实、74×皮肤白皙真实等）。
- 新增`model/training/beautify-ai-style-prompts.py`：①37种皮肤短语按长度降序映射为“手部肌肤…”美颜描述（柔嫩白皙/光滑细腻/白皙透亮/细腻无瑕/水润有光泽等）；②特殊句“带有真实细节的自然皮肤纹理”→“带有柔光细节的自然手部肌肤”；③结尾“写实风格”按索引轮转5种美颜风格：精致美颜/柔光美颜/清新少女/梦幻柔美/甜美滤镜。
- 结果：500条全部处理（skin_fixes=499+special=1、end_style_changes=500），校验无“写实”/“皮肤”残留；脚本幂等（重跑0变更）。抽查第1—5条：开头5变体保持、皮肤全为手部肌肤、结尾美颜风格轮转。
- 验证：/ai-generate返回200；ESLint结果待核。白皮书同步至v1.1.443；未提交。

## AI 生图隐藏系统提示词（手部解剖修正，用户要求）

- 用户要求：底层生图模型手部质量不高，需要一段统一、隐藏（用户不可见）的系统提示词，保证手部正常（无多指/缺指/手指不全/手掌残缺/手部模型异常/穿模等），并在每次发送生图请求时自动附加在用户提示词之后。
- 结合用户要求与网络资料（AI绘画多指修复技巧、Midjourney手部畸形规避指南、负向提示词清单：extra fingers/missing fingers/fused fingers/deformed hand/mutated hand/bad anatomy/clipping artifacts等），编写隐藏系统提示词：中文正面约束（每只手恰好五根完整独立手指、手掌完整、比例协调、指关节正确、不得穿模、姿势自然透视正确）+ 中文负面清单 + 英文负面关键词。
- 实现：新增`src/lib/ai-hand-anatomy-prompt.ts`（`HAND_ANATOMY_SYSTEM_PROMPT`常量、`AI_IMAGE_SCENE_SUFFIX`原场景后缀、`assembleAiImagePrompt(userPrompt)`拼接函数）；`/api/generate-ai`路由改用拼接函数（用户提示词→场景后缀→隐藏系统提示词），前端输入框不显示系统提示词。
- 测试：新增`tests/ai-hand-anatomy-prompt.test.ts`3项全过（拼接顺序与结尾位置、21个缺陷关键词全覆盖、确定性）；未执行真实生图（避免计费）。
- 验证：ESLint结果待核。白皮书同步至v1.1.444；未提交。

## 系统提示词补充“最多两只手”约束（用户反馈第二轮）

- 用户反馈生成图仍出现超过两只手。更新`HAND_ANATOMY_SYSTEM_PROMPT`：开头改为“画面中最多只能出现两只解剖结构完全正确的手（单手近景或双手场景），严禁出现三只及以上手、多余的手臂或额外的手部”；中文负面清单增加“多余的手、多余的手臂”；英文负面词增加 extra hands, third hand, more than two hands, multiple hands, additional hands, duplicate hands, extra arms。
- 测试新增9个手数量关键词断言（首轮“最多只能出现两只手”断言与文案“最多只能出现两只解剖…”不匹配，修正为“最多只能出现两只”后3/3通过）；未执行真实生图。白皮书同步至v1.1.445；未提交。

## 系统提示词三层强化（用户反馈第三轮：仍出现三只手）

- 用户提供具体失败案例（“手自然地捧着一个复古瓷茶杯”场景仍生成三只手），说明“最多两只”约束响应不足。强化策略（依据生图模型对重复强调/大写负面词/道具接触约束更敏感的实践）：
  1. 数量约束改为“只能出现一只手（单手近景）或最多两只手（双手场景），绝对禁止出现第三只手、多余的手臂或任何额外的手部肢体”；
  2. 新增道具接触约束：“手捧/手持/触摸道具时，道具只能由画面中可见的这（两）只手接触，严禁从画面外伸出额外的第三只手，也不得在画面内凭空出现多余的手”；
  3. 结尾前增加“再次强调”重复句；英文负面词前加NO大写强调（NO extra hands, NO third hand, NO more than two hands, NO multiple hands, NO additional hands, NO duplicate hands, NO extra arms）。
- 测试更新为24个关键词断言（含“绝对禁止出现第三只手”“严禁从画面外伸出额外的第三只手”“再次强调”及NO系列），3/3通过；ESLint通过；未执行真实生图。白皮书同步至v1.1.446；未提交。

## 系统提示词生效性验证（用户质疑）

- 用户质疑“系统提示词真的生效了吗”。验证三层证据：
  1. 代码接入：新增`tests/generate-ai-route.test.ts`静态断言——route.ts必须import并调用`assembleAiImagePrompt(prompt)`且不再使用旧内联后缀模板（2/2通过）；
  2. 拼接正确性：单元测试证明最终prompt = 用户提示词 → 场景后缀 → 隐藏系统提示词（系统提示词在末尾）（已通过）；
  3. 运行中服务：grep `.next` Turbopack编译缓存命中“绝对禁止出现第三只手”共6个文件，证明当前运行的dev server已加载最新系统提示词。
- 结论：系统提示词已确认生效（每次请求发给Agnes的prompt末尾均携带）。若实测仍出现三只手，属于模型对提示词约束的遵循度上限，需采用前置约束/用户提示词注入等更强手段。白皮书同步至v1.1.447；未提交。

## 前置手数量约束（用户确认加入）

- 用户同意尝试前置约束。`assembleAiImagePrompt`改为四段式：`HAND_COUNT_PREFIX`（“画面中只能出现一只手（单手场景）或最多两只手（双手场景），绝对禁止第三只手。以下内容为主题描述：”）→ 用户提示词 → 美甲场景后缀 → 完整隐藏系统提示词（末尾保留）。
- 测试更新为顺序断言（prefix=0位、user紧随、suffix、system在末尾），5/5通过；grep Turbopack缓存命中“以下内容为主题描述”2个文件，确认运行中dev server已加载；ESLint通过。白皮书同步至v1.1.448；未提交。

## 手数量消歧加固（用户实测反馈：明确“双手”后生成正常）

- 用户实测：把提示词中“手自然地捧着一个复古瓷茶杯”改为“双手自然地捧着一个复古瓷茶杯”后不再出现三只手——确认“手捧”（单数）是歧义源，模型无法判断单手/双手。
- 两处加固：
  1. 提示词库21处动作措辞统一：“手捧着”→“双手捧着”（5处）、“手端着”→“双手端着”（11处）、“手举着”→“单手举着”（5处，举杯/蜡烛/灯笼为单手动作）；替换后校验无独立残留；
  2. 系统提示词新增消歧句置于开头：“描述中的动作决定手部数量：手持、手拿、手捏、手举、手指轻触等单手动作时画面中只出现一只手；手捧、手端、手托、双手合握、双手捧着等双手动作时画面中只出现两只手；绝对禁止第三只手。”
- 验证：测试新增5个消歧关键词断言5/5通过；ESLint通过；Turbopack缓存命中“描述中的动作决定手部数量”3文件，运行中服务已加载。白皮书同步至v1.1.449；未提交。

## 捧/举/托动作逐场景细化（用户要求：全部明确单手/双手）

- 用户要求所有“手捧/手举”场景逐条分析单手或双手，统一写成“双手捧着/单手举着/双手举着”，不允许模糊“手捧/手举”。
- 逐条分析并修改5处：
  1. 甜美风第1条“手自然地捧着一个复古瓷茶杯”→“双手自然地捧着一个复古瓷茶杯”（用户实测验证）；
  2. “手轻捧一颗青苹果”→“双手捧着一颗青苹果”（苹果需双手捧）；
  3. “手轻捧一串珍珠项链”→“双手捧着一串珍珠项链”（长链需双手托捧）；
  4. “手轻捧一个信乐烧茶碗”→“双手捧着一个信乐烧茶碗”（茶道双手捧碗）；
  5. “手轻托下巴”→“单手轻托下巴”（托下巴为单手动作，顺带消歧）。
- 既有“单手举着热可可/蜡烛×2/小灯笼/香槟”5处经复核均为单手动作，保持不变；无任何场景需要“双手举着”。
- 校验：独立“手捧/手举/手托”残留0；最终动作分布：双手捧着9（含“双手自然地捧着”）、单手举着5、单手轻托1。测试5/5、ESLint通过、Turbopack缓存已加载。白皮书同步至v1.1.450；未提交。

## 系统提示词收紧手指数约束（用户反馈：有概率生成多指）

- 用户反馈仍有概率生成多指图。收紧策略（同手数量三层强化）：
  1. 前置短句`HAND_COUNT_PREFIX`增加“每只手恰好五根手指，不多不少”；
  2. 完整提示词开头新增“每只手必须且只能有五根手指（一根拇指与四根手指），不多不少，绝对禁止第六根手指、七根手指或任何数量错误的手指”；
  3. 手指描述强化为“数量精确为五根、形态分明、互不粘连、不分叉”；
  4. 新增“再次强调：每只手恰好五根手指，绝不出现第六根手指、七根手指或缺失的手指”；
  5. 负面中文增加“六指、七指、手指分叉”；英文大写负面增加 NO extra fingers, NO sixth finger, NO missing fingers, NO four fingers, NO fused fingers, NO webbed fingers。
- 测试更新（“手指数量准确”→“手指数量精确为五根”并新增12个手指数关键词），5/5通过；ESLint通过；重启dev server后全新编译产物命中“必须且只能有五根手指”2文件，确认运行中服务已加载。白皮书同步至v1.1.451；未提交。

## 用户可见提示词全面优化（模板句式多样化）

- 扫描500条提示词高频模板：“每片指甲有不同”449处、“背景为虚化的”489处、“手轻放在”208处。
- 新增`model/training/diversify-ai-style-prompts.py`按风格内索引轮转替换（幂等、可复验）：
  1. 指甲表达4变体：每片指甲各绘有不同/各片指甲点缀着不同/指甲上错落分布着不同/每片指甲分别饰有不同（原句式全部清除）；
  2. 手放表达3变体：手自然地轻放在/手轻轻搭在/手随意搁在；
  3. 背景表达3变体：背景为柔焦虚化的/柔和虚化的/梦幻虚化的。
- 调试过程发现并修复脚本bug：变体列表第0项误用原文，导致index%N==0的行“替换”成原文无变化（残留117/73/165恰好等于各风格index%N==0行数）；全部改为真变体后残留清零。
- 结果：500条、三模板残留0、幂等（重跑0变化）、无英文残留；最终分布：指甲3种（236/108/105）、手放2种（144/64）、背景2种（331/158），多样性较原100%单一句式大幅提升。抽查语句通顺。
- 验证：测试5/5、ESLint通过；重启dev server后编译产物命中新变体4文件。白皮书同步至v1.1.452；未提交。

## AI 生图字数限制 500→520（用户要求）

- 前端`src/app/ai-generate/page.tsx`：字符计数器`/500`→`/520`、`maxLength={500}`→`{520}`；
- 后端`src/app/api/generate-ai/route.ts`：校验`> 500`→`> 520`、错误消息同步；
- 白皮书§4.4与400错误表同步为520；无测试断言引用旧限制。白皮书同步至v1.1.453；未提交。

## 风格按钮提示词改为随机抽取（用户要求）

- 用户反馈：刷新页面后第一次点击某风格（如甜美风）每次填入一模一样的提示词。原因：`handleStyleClick`原为顺序轮转（首次点击固定取第0段）。
- 修改`src/app/ai-generate/page.tsx`：`styleIndices`轮转改为`lastIndices`记录每风格上次展示索引，每次点击从该风格50段中`Math.random()`随机抽取一段，若与上次相同则顺延下一位（避免连续两次同一段）；各风格独立随机，刷新后首次点击同样随机。
- `AI_STYLE_PROMPTS`内容与数量不变，仅前端交互逻辑变化，无接口变化。
- 验证：ESLint通过、/ai-generate返回200。白皮书同步至v1.1.454；未提交。

## 系统提示词加强双手分离约束（用户反馈：两只手混在一起）

- 用户反馈：生成图片有概率两只手混在一起（交叠、粘连、融合）。
- 修改`src/lib/ai-hand-anatomy-prompt.ts`的`HAND_ANATOMY_SYSTEM_PROMPT`：
  1. 正文在“手掌完整”后新增双手分离约束：“双手场景中，两只手轮廓清晰、彼此分离，不得交叠、粘连、融合或互相穿插，两只手的皮肤、手指与指甲必须边界分明，不得混成一体”；
  2. 中文负面清单新增：双手交叠、双手粘连、双手融合、手指交叉缠绕、手指互相穿插、双手边界模糊；
  3. 英文大写负面新增：NO merged hands, NO overlapping hands, NO tangled fingers, NO interlocked fingers, NO conjoined hands, NO blurred hand boundaries（参考网络资料：负面提示词应具体列举交叠/融合/缠绕/连体手等缺陷描述）。
- 测试`tests/ai-hand-anatomy-prompt.test.ts`关键词断言新增16项（正面“两只手轮廓清晰”“不得交叠、粘连、融合或互相穿插”+中文负面6项+英文负面6项），3/3通过；ESLint通过。未执行真实生图（避免计费）。接口契约不变。白皮书同步至v1.1.455；未提交。

## 背景流光贯穿所有页面（用户要求）

- 用户问"页面没动也有显卡占用"→ 诊断：AppShell/首页/FlowingShell背景均含大模糊光斑无限动画（blur 90—110px，18—26秒循环），这就是GPU持续占用的来源；浏览器自身另有1—5%基础合成占用。
- 用户选择保留效果并要求"流光贯穿所有页面"。盘点：首页与`/ar-demo`为5光斑，其余6个AppShell页面仅3光斑（且模糊110px、动画最简单），视觉不统一。
- 升级`src/components/AppShell.tsx`与`AppShell.module.css`：3光斑→5光斑（粉/紫/金/玫红/白，与首页同款配色与布局参数），模糊110px→90px统一，动画由单一drift改为5套独立液态流动动画（flowA—E，18/20/22/24/26秒周期，平移+缩放+旋转+圆角变形），保留移动端淡化和`prefers-reduced-motion`支持（Windows关闭动画效果可全站停用）。
- 效果：全站8个页面流光视觉统一；GPU占用属性不变（慢速动画持续存在，属设计效果）。
- 验证：ESLint通过、/privacy等AppShell页面200、Turbopack编译产物命中flowA—E新动画。白皮书同步至v1.1.456；未提交。

## 背景流光性能优化（用户选择A+C并增大变形）

- 用户问"有没有更好的技术在不影响效果的前提下削减性能开销"。分析开销来源：①5个600—750px大光斑每帧实时`blur(90px)`高斯模糊（GPU最重操作）；②`border-radius`动画每帧强制重绘（放弃GPU合成优化）；③全屏`backdrop-filter`毛玻璃层（首页/ar-demo）。
- 实施三处（AppShell、首页、FlowingShell）统一优化：
  1. **A 预模糊静态化**：`filter: blur(90px)`全部移除，光斑背景改为`radial-gradient(circle, 中心0.5 → 42%处0.18 → 72%处透明)`径向渐变，柔和光斑效果视觉近似、但模糊只算一次不再每帧计算；移动端媒体查询中的blur(80px)/blur(60px)一并清除；
  2. **增大变形**：去掉`border-radius`圆角变化（90px模糊下本来就不可见），改用更大的非均匀缩放（0.45—1.7）×旋转（±30°）×位移（±200px）——变形效果比原来更明显，且transform-only全部走GPU合成器、零像素重绘；
  3. **C 后台自动暂停**：新增`src/components/AmbientMotion.tsx`（client组件），监听`visibilitychange`，页面切后台/最小化时把所有`[data-ambient-blob]`光斑元素`animation-play-state: paused`，回前台恢复；三个外壳组件均挂载。
- 结果：视觉上光斑更灵动（变形更明显），GPU开销从"每帧模糊+重绘"降到"合成器轻量位移"，后台标签页完全零动画开销。
- 验证：ESLint通过；首页/AI生成/ar-demo均200；编译产物确认radial-gradient、AmbientMotion暂停逻辑、data-ambient-blob标记全部生效；src中无光斑blur残留。白皮书同步至v1.1.457；未提交。

## 背景流光第二轮性能优化（用户反馈仍有GPU压力）

- 用户反馈仍有GPU渲染压力。按用户指令"先记住现在的样子再优化"：先把5个受影响文件（AppShell.module.css、page.module.css、FlowingShell.tsx、AppShell.tsx、AmbientMotion.tsx）备份至`.backup-2026-08-15-gpu/`并记录SHA-256，可随时还原。
- 排查残留压力点并逐一处理（三处外壳+顶栏统一）：
  1. **拆除全部`backdrop-filter`**：首页全屏frostGlass（blur 50px+saturate）、ar-demo全屏`backdrop-blur-[50px]`白罩、**顶栏Header（blur 22px，`position:fixed`所有页面常驻）**——光斑在毛玻璃后面移动时每帧触发整屏/顶栏实时模糊重算，是残留最大重活；全部改为静态半透明背景（frostGlass保留rgba白、Header背景0.68→0.85），视觉几乎无差；
  2. **去掉`will-change:transform`与`translateZ(0)`**：5个600—750px大层不再永久提升为独立GPU合成层，静止/后台暂停时释放显存；
  3. **动画降频**：`ease-in-out`改为`steps(N,end)`阶梯插值（20s动画约每333ms更新一次，60fps→~3fps合成），慢速流动视觉无感，合成次数降约20倍；
  4. **光斑缩小+位移收窄**：尺寸750/580/460/640/520→620/500/400/540/450px（约-17%），位移±200→±140px，变形幅度（scale 0.45—1.7、rotate±30°）保留。
- 验证：ESLint通过、首页/AI生成/ar-demo/隐私均200、git diff --check通过；编译产物确认steps降频、Header无blur、光斑新尺寸全部生效；src中仅剩`prefers-reduced-motion`等预期规则，无backdrop-filter残留（Header.module.css已确认）。白皮书同步至v1.1.458；未提交。备份目录`.backup-2026-08-15-gpu/`为未跟踪文件，还原命令：复制各`.bak`回原路径。

## 流光背景重构为全局单例（用户要求：全站统一 + 切换页面动画不重置）

- 用户反馈两个问题：①所有页面流光效果要统一；②在不同页面间切换时流光效果要保持继续，不能重新渲染。
- 根因：三份独立CSS动画（AppShell.module.css、首页page.module.css、FlowingShell内联style）虽然参数一致，但页面客户端导航时组件重新挂载，CSS动画每次都从头播放，且相位各不相同。
- 重构方案：
  1. 新增`src/components/AmbientBackground.tsx`（"use client"）：5个光斑参数（颜色/尺寸/位置/周期/位移/缩放/旋转/相位）集中为`BLOBS`数组；用`setInterval`(333ms) + `performance.now()`驱动的JS直接设置光斑`transform`（正弦/余弦组合产生连续液态流动：非均匀缩放1±0.65、旋转±15—30°、位移±100—140px）；`visibilitychange`后台暂停/恢复；`prefers-reduced-motion`时静态。无CSS动画，无blur，无will-change。
  2. 挂载于根`layout.tsx`的`<body>`首元素——Next.js客户端导航时layout不卸载、计时器与相位（基于全局时间）持续累积，切换页面光斑位置自然延续，不再重置；全站8页面共用同一份代码、同一套参数、同一相位，效果完全统一。
  3. 删除三处旧实现：AppShell.tsx的ambient层与AmbientMotion挂载、AppShell.module.css光斑样式与keyframes、首页page.tsx的ambientLayer与AmbientMotion、page.module.css光斑样式与keyframes（保留静态frostGlass白罩）、FlowingShell.tsx光斑div与内联style（保留静态白罩）；删除`src/components/AmbientMotion.tsx`（功能并入全局组件）。
- 验证：ESLint通过；8页面（首页/AI生成/ar-demo/隐私/编辑器/图库/AR试戴/性能测试）全部200；git diff --check通过；编译产物确认`AmbientBackground`独立chunk由layout加载。白皮书同步至v1.1.459；未提交。

## 修复流光全部消失（v1.1.459回归）

- 用户反馈：所有页面流光效果都没了。
- 根因（层叠上下文分析）：全局光斑容器是`fixed z-0`（body首子元素）；各页面外壳（AppShell `.shell`、首页 `.page`、FlowingShell外层div）均为`position:relative`（z-index auto）+不透明渐变背景，在根层叠上下文中与光斑同层（z-0/auto定位层）且按DOM顺序**后绘制** → 外壳的不透明背景把光斑整体盖住。旧版光斑在外壳内部DOM靠后所以可见，提到全局后层级关系被破坏。
- 修复：光斑容器z-index由0改为1（`style={{zIndex:1}}`）——正数层绘制在外壳背景之上；页面内容（`.main/.hero/.footer`同为z-1、DOM在光斑之后、背景透明）正常覆盖在光斑之上，有背景的卡片等元素不受影响，层级语义恢复为"光斑在内容之后"。
- 验证：ESLint通过；SSR HTML确认首页与AI生成页各渲染5个光斑div（620/500/400/540/450px）且容器带`z-index:1`；页面200。白皮书同步至v1.1.460；未提交。

## 流光画质与流畅度优化（用户反馈：太卡 + 光晕色彩断层）

- 用户反馈：流光效果太卡，且光晕有色彩断层。
- 卡顿根因：①333ms阶梯跳变（每0.33秒光斑才"跳"一次，且单次位移±140px）造成视觉顿挫；②每帧做非均匀强缩放（0.45—1.7）+大角度旋转（±30°），合成器需对整层重采样。
- 色彩断层根因：渐变仅3个色阶（0.5→0.18→0），8-bit色深下大圆过渡出现可见环状色带。
- 优化（重写`AmbientBackground.tsx`）：
  1. **rAF平滑驱动**：`requestAnimationFrame`每帧连续更新（60fps），消除跳变顿挫；后台标签页rAF自动暂停（无需visibilitychange监听）；
  2. **动画降为合成器廉价路径**：只做translate（±45—70px，17—26s错峰正弦/余弦组合形成不规则流动）+轻微呼吸scale（1±0.05），去掉旋转与强非均匀缩放；
  3. **色彩断层双修复**：渐变3色阶→8色阶平滑过渡（0.50@0%→0.38@16%→0.28@32%→0.19@48%→0.11@62%→0.05@74%→0.01@84%→0@90%）；叠加静态SVG噪点层（feTurbulence fractalNoise灰度、opacity 0.05、不参与动画、合成器缓存一次）打破8-bit色带——业界标准banding消除方案；
  4. 保留：全局单例（layout挂载一次、切换页面动画连续）、z-index:1层级修复、prefers-reduced-motion静态。
- 验证：ESLint通过；首页/AI生成/ar-demo均200；git diff --check通过；SSR确认8段渐变stop、噪点SVG（feTurbulence）、5个光斑div；编译产物确认requestAnimationFrame且旧TICK_MS阶梯逻辑已移除。白皮书同步至v1.1.461；未提交。

## 流光回滚到备份版本（用户反馈：v1.1.461效果太差）

- 用户要求"更换回以前备份的效果"。从`.backup-2026-08-15-gpu/`恢复5个文件（SHA-256与备份一致：AppShell.module.css ADB01C7A、AppShell.tsx 2CD4E665、FlowingShell.tsx 31D7A324、page.module.css FE8B4CE0、AmbientMotion.tsx AC1A6041）；恢复首页page.tsx的光斑层（ambientLayer 5 span + frostGlass + AmbientMotion）；layout.tsx移除AmbientBackground挂载；删除`src/components/AmbientBackground.tsx`；源码全库无AmbientBackground残留。
- 恢复内容＝v1.1.457备份状态：三处各自5光斑CSS动画（750/580/460/640/520px、radial-gradient 3段预模糊、大变形keyframes：位移±200px、非均匀缩放0.45—1.7、旋转±30°、ease-in-out）、will-change常驻层、首页frostGlass与ar-demo毛玻璃（blur 50px）、AmbientMotion后台暂停（visibilitychange）。
- 说明：Header.module.css不在备份内，保持v1.1.458的无毛玻璃优化（rgba .85静态背景）；v1.1.458—461全部方案（steps降频、缩小尺寸、全局单例、rAF平滑、8段渐变+噪点）已撤销。
- 验证：ESLint通过；8页面全部200；git diff --check通过；SSR确认首页/AI/隐私页data-ambient-blob光斑恢复（5×2处）、ar-demo光斑与backdrop-blur恢复；编译CSS确认frostGlass blur(50px)恢复。白皮书同步至v1.1.462；未提交。

## 备份版流光加相位对齐（用户要求：切界面时流光保持连贯）

- 用户要求"在现在的效果上做连贯处理，切界面的时候流光效果保持连贯"。当前是备份版（v1.1.457）三处各自CSS动画，页面切换时组件重挂载、动画从头播放。
- 方案（零视觉改动）：CSS动画负延迟相位对齐。增强`src/components/AmbientMotion.tsx`：
  1. 新增`syncPhase()`：挂载时遍历`[data-ambient-blob]`光斑，用`getComputedStyle`读取`animationDuration`（20s/24s/18s/26s/22s），把`animation-delay`设为`-(performance.now() % periodMs)`（如-7231ms）；
  2. CSS负延迟语义＝动画从第7.231秒处开始播放，相位由全局时间唯一决定 → 新页面挂载时动画直接落在"当前全局相位"，与切换前页面光斑位置无缝衔接，不再从头重播；
  3. 动画曲线（备份版大变形keyframes）原样未动，视觉效果与v1.1.462完全一致；后台暂停逻辑保留。
- 三个外壳（AppShell/首页/FlowingShell）备份版已挂载AmbientMotion，无需任何JSX改动；幂等（重复挂载重复设置无副作用）；`prefers-reduced-motion`下动画none负延迟无影响。
- 验证：ESLint通过；8页面全部200；git diff --check通过；编译产物确认`animationDelay = -(now % periodMs)`相位逻辑已加载。白皮书同步至v1.1.463；未提交。

## 流光全站统一为首页效果（用户选择"统一成首页效果"）

- 用户问"首页效果和其他页面不一样是为什么"。核对三处光斑参数发现差异：①透明度——首页`opacity:.58`×渐变0.5≈0.29（淡），AppShell无opacity限制≈0.5（浓1.7倍），FlowingShell 0.48≈0.24（最淡）；②毛玻璃——首页有全屏frostGlass（blur 50px雾感），AppShell没有；③FlowingShell尺寸略小、位置略偏、keyframes仅3—4帧（轨迹不同）。
- 统一为首页效果：
  1. `AppShell.module.css`：`.ambient span`加`opacity:.58`（与首页一致）；新增`.frostGlass`（fixed z-0、rgba(255,255,255,.3)、`backdrop-filter:blur(50px) saturate(1.3)`，与首页frostGlass完全一致）；删除移动端`opacity:.24`覆盖（首页移动端无此覆盖）；`AppShell.tsx`在ambient层后挂frostGlass div；
  2. `FlowingShell.tsx`（ar-demo）：opacity 0.48→0.58；尺寸720/560/440/620/500→750/580/460/640/520；位置对齐（left 48/2/64/14→45/5/60/15、top 40/-6→42/-5等）；keyframes flowing-amb-1~5补全为与首页ambientOne~Five完全一致的7/5/6/5/4帧轨迹（-160,110 → 90,60全序列）；
  3. 首页不动（基准）。ar-demo已有毛玻璃白罩与首页一致。
- 统一后三处参数完全一致：尺寸750/580/460/640/520px、opacity 0.58、毛玻璃blur 50px、大变形动画轨迹相同。
- 验证：ESLint通过；8页面全部200；git diff --check通过；三处opacity 0.58各1处、AppShell frostGlass blur(50px)存在、光斑标记正常渲染。白皮书同步至v1.1.464；未提交。

## 流光浓度加强（用户反馈：太淡视觉不明显）

- 用户反馈流光太淡。调整三处（首页page.module.css、AppShell.module.css、FlowingShell.tsx）光斑参数：
  1. 元素opacity 0.58→0.78；
  2. radial-gradient中心0%处alpha 0.5→0.65、中段42%处alpha 0.18→0.28（边缘72%处0不变）；
  3. 有效浓度由约0.29（0.58×0.5）提升至约0.51（0.78×0.65），提升约75%，视觉明显但不过曝。
- 毛玻璃白罩（blur 50px、rgba白0.3）、光斑尺寸750/580/460/640/520、动画轨迹均未动；三处同步修改，全站8页面仍完全一致；相位对齐（v1.1.463）与后台暂停不受影响。
- 验证：ESLint通过；首页/AI生成/ar-demo/隐私均200；git diff --check通过；三处opacity 0.78与渐变0.65各5处核对一致。白皮书同步至v1.1.465；未提交。
