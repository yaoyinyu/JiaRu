# 开发日志 - 2026-07-03

## 今日概览

今天主要推进两条线：

1. 把纹理质量门禁从 Phase 4 局部能力推进到 final audit、training release、release decision。
2. 把用户修正样本回流正式做成 active learning 主链，并接入 training release / governance。

## 一、纹理质量门禁继续接主链

### 1. 纹理质量门禁脚本

涉及：

- `scripts/verify-texture-quality-gate.ts`
- `scripts/run-real-model-final-audit.ts`
- `tests/verify-texture-quality-gate.test.ts`
- `tests/run-real-model-final-audit.test.ts`
- `docs/texture-quality-gate.md`

完成内容：

- 新增独立的纹理质量门禁脚本。
- 统计可直接使用率、污染率等核心指标。
- 将 annotation debug 中的质量信号正式收口成量化 gate。

结果：

- “纹理质量”第一次具备明确的量化判断标准，而不是只靠 warning 和人工经验。

### 2. final audit / training release 接入纹理质量门禁

涉及：

- `scripts/run-training-release-pipeline.ts`
- `scripts/run-real-model-final-audit.ts`
- `tests/run-training-release-pipeline.test.ts`
- `docs/real-model-final-audit.md`

完成内容：

- final audit 在有 annotation 数据时可自动运行 texture quality gate。
- `training-release-pipeline-report.json` 透出 `finalAuditTextureQualityGate`。

结果：

- training release 主链已经能直接感知纹理质量门禁结果。

### 3. release decision 接入纹理质量门禁

涉及：

- `scripts/build-release-decision-report.ts`
- `tests/build-release-decision-report.test.ts`
- `tests/run-release-governance-pipeline.test.ts`
- `docs/release-decision-report.md`

完成内容：

- release decision 正式读取 texture quality gate 结果。
- 当核心 gate 通过但纹理质量不达标时，candidate 进入 `manual_review`，而不是直接自动放行。
- decision report 中新增 texture gate 输入与产物字段。

结果：

- “可训练”与“可直接上线使用”开始被正式区分。

## 二、debug sample 主动学习链路

### 4. debug sample 优先级计算

涉及：

- `src/lib/nail-texture-debug-priority.ts`
- `model/training/prioritize-debug-samples.ts`
- `tests/nail-texture-debug-priority.test.ts`
- `tests/prioritize-debug-samples.test.ts`
- `docs/debug-sample-prioritization.md`

完成内容：

- 把“哪些用户修正样本最值得回收”沉淀成统一评分规则。
- 输出优先级分数、优先级层级、原因和摘要。
- 支持批量扫描和 `--top N` 过滤。

结果：

- 用户修正样本回流不再只是“能导入”，而是开始具备“优先导入最有价值样本”的能力。

### 5. priority-driven debug import

涉及：

- `model/training/import-debug-sample.ts`
- `tests/import-debug-sample.test.ts`
- `docs/debug-sample-prioritization.md`

完成内容：

- `import-debug-sample.ts` 支持直接消费 priority report。
- 新增：
  - `--priority-report`
  - `--min-priority`
  - `--top`
- 批量导入顺序正式受优先级控制。

结果：

- 高价值修正样本可以优先进入正式训练集。

### 6. debug sample active learning pipeline

涉及：

- `model/training/run-debug-sample-active-learning-pipeline.ts`
- `tests/run-debug-sample-active-learning-pipeline.test.ts`
- `docs/debug-sample-active-learning-pipeline.md`

完成内容：

- 把 prioritize → import → split / audit / convert → readiness / collection plan / checklist 串成统一 pipeline。
- 用户修正样本首次拥有类似 reviewed batch 的一键导入主链。

结果：

- 页面修正样本已经不再是零散回流，而是正式进入数据集主流程。

### 7. active learning trace draft / handoff

涉及：

- `scripts/build-active-learning-release-trace-draft.ts`
- `scripts/build-debug-sample-active-learning-handoff.ts`
- `model/training/run-debug-sample-active-learning-pipeline.ts`
- `scripts/build-release-trace-index.ts`
- `docs/active-learning-trace-handoff.md`

完成内容：

- pipeline 自动生成：
  - `active-learning-release-trace-draft.json`
  - `debug-sample-active-learning-handoff.json`
- `build-release-trace-index.ts` 开始保留 `activeLearning` 字段：
  - imported sample count
  - imported by priority
  - priority filters
  - readiness snapshot

结果：

- active learning 数据第一次被正式纳入 release trace 体系。

### 8. training release 消费 active learning handoff

涉及：

- `scripts/run-training-release-pipeline.ts`
- `tests/run-training-release-pipeline.test.ts`
- `docs/training-release-pipeline.md`

完成内容：

- training release 主链新增 `--governance-active-learning-handoff`。
- 可自动恢复 active learning release trace draft。
- governance 最终生成的 trace index 会保留 active learning 追踪字段。
- 同时重写并清理了 `docs/training-release-pipeline.md` 的历史编码问题。

结果：

- active learning 已经从训练主链外侧的旁路，变成 training release 可以直接消费的正式输入。

## 验证

今天围绕新增链路做了针对性验证，覆盖：

- `npm.cmd test -- tests/verify-texture-quality-gate.test.ts`
- `npm.cmd test -- tests/run-real-model-final-audit.test.ts`
- `npm.cmd test -- tests/build-release-decision-report.test.ts`
- `npm.cmd test -- tests/run-release-governance-pipeline.test.ts`
- `npm.cmd test -- tests/nail-texture-debug-priority.test.ts`
- `npm.cmd test -- tests/prioritize-debug-samples.test.ts`
- `npm.cmd test -- tests/import-debug-sample.test.ts`
- `npm.cmd test -- tests/run-debug-sample-active-learning-pipeline.test.ts`
- `npm.cmd test -- tests/run-training-release-pipeline.test.ts`
- `npm.cmd run lint`
- `npm.cmd run build`

## 当日结论

到今天为止：

- 纹理质量门禁已经不只是 audit 附属统计，而是开始影响 release decision。
- 用户修正样本回流已经从“导出/导入工具”升级成 active learning + trace + handoff + training release 的连续主链。

## 补充进展：模型运行时多版本缓存隔离

涉及：

- `src/lib/nail-texture-recognition/model-runtime.ts`
- `tests/nail-texture-model-runtime.test.ts`

完成内容：

- 将模型 manifest/runtime 缓存从“全局单例”改为“按 `manifestUrl` 分桶缓存”。
- 修复同一进程内切换不同模型版本或不同 manifest 做验收时，后一次可能误用前一次缓存的问题。
- 新增单测覆盖：
  - 不同 manifest 会分别拉取、分别建 session
  - 相同 manifest 会复用已有缓存

验收：

- `npm.cmd test -- tests/nail-texture-model-runtime.test.ts`
  - 结果：`146 passed / 0 failed / 1 skipped`

## 补充进展：识别耗时统计与按需 fallback

涉及：

- `src/lib/nail-texture-recognition/recognize.ts`
- `tests/nail-texture-model-runtime.test.ts`

完成内容：

- 修复模型识别成功时 `elapsedMs` 误复用 fallback 耗时的问题。
- 现在 `elapsedMs` 会记录真实模型路径的识别耗时。
- 将 fallback 计算改为按需执行，不再在每次 `preferModel=true` 时先无条件跑一遍 fallback。

意义：

- 性能统计更准确，便于后续做真实模型接入验收。
- 模型成功时减少一段不必要的 fallback 计算，避免额外浪费主线程/worker 时间。

验收：

- `npm.cmd test -- tests/nail-texture-model-runtime.test.ts`
  - 结果：`147 passed / 0 failed / 1 skipped`

## 补充进展：worker 元信息透传到 UI

涉及：

- `src/lib/nail-texture-recognition/types.ts`
- `src/workers/nail-texture-recognition.worker.ts`
- `src/lib/nail-texture-recognition/client-worker.ts`
- `src/components/NailArtPicker.tsx`
- `scripts/verify-browser-integration.ts`
- `tests/nail-texture-client-worker.test.ts`
- `tests/verify-browser-integration.test.ts`

完成内容：

- 为 worker 响应补充 `modelInfo` 透传能力。
- `recognizeNailTexturesInWorker()` 现在会保留 worker 返回的 `modelInfo`。
- `NailArtPicker` 顶部识别摘要现在可展示：
  - 模型/回退
  - 实际模型后端（`webgpu` / `wasm`）
  - 模型版本
  - 识别耗时（ms）
- browser integration 合约检查同步升级，防止后续改动把这条元信息链路改断。

意义：

- 浏览器端模型验收时，可以直接看到真实跑的是哪个后端，不再只能看到“模型识别”这种粗粒度状态。
- 后续做性能与兼容性排查时，UI 侧信息更完整。

验收：

- `npm.cmd test -- tests/nail-texture-client-worker.test.ts tests/verify-browser-integration.test.ts`
  - 结果：`148 passed / 0 failed / 1 skipped`

## 补充进展：候选数量上限扩展到 10

涉及：

- `src/lib/nail-texture-recognition/types.ts`
- `src/lib/nail-texture-recognition/fallback-adapter.ts`
- `src/lib/nail-texture-recognition/recognize.ts`
- `src/lib/nail-texture-recognition/client-worker.ts`
- `src/workers/nail-texture-recognition.worker.ts`
- `src/components/NailArtPicker.tsx`
- `tests/nail-texture-preprocess-postprocess.test.ts`
- `tests/nail-texture-model-runtime.test.ts`
- `tests/nail-texture-client-worker.test.ts`

完成内容：

- 为统一识别接口新增 `maxCandidates` 选项。
- fallback 路径不再硬编码只取前 5 个候选。
- 模型后处理路径现在也支持按 `maxCandidates` 截断。
- worker 请求默认上限从 5 提升到 10。
- `NailArtPicker` 当前识别调用改为显式请求最多 10 个候选。

意义：

- 与规划文档里“自动识别 1 到 10 个可用美甲区域”的目标正式对齐。
- 后续无论是多甲片贴图、样板图，还是更复杂的用户上传图，候选数上限不再被 5 卡住。

验收：

- `npm.cmd test -- tests/nail-texture-preprocess-postprocess.test.ts tests/nail-texture-model-runtime.test.ts tests/nail-texture-client-worker.test.ts`
  - 结果：`151 passed / 0 failed / 1 skipped`

## 补充进展：默认手指分配规则与规划文档对齐

涉及：

- `src/lib/nail-texture-recognition/finger-assignment.ts`
- `src/lib/nail-texture-recognition/fallback-adapter.ts`
- `src/lib/nail-texture-recognition/postprocess.ts`
- `src/lib/nail-texture-recognition/index.ts`
- `tests/nail-texture-finger-assignment.test.ts`
- `tests/nail-texture-preprocess-postprocess.test.ts`
- `model/fixtures/nail-texture-model-output-sample.json`
- `tests/verify-model-output-fixture.test.ts`

完成内容：

- 把默认手指分配规则抽成共享模块，避免 fallback 和 model 路径各写一套。
- 按规划文档修正默认规则：
  - 1～3 个候选：不强行判断手指，保持 `null`
  - 4 个候选：映射为 `index/middle/ring/pinky`
  - 5 个候选：映射为 `thumb/index/middle/ring/pinky`
  - 超过 5 个候选：前 5 个按默认手指，额外候选保持 `null`
- 同步修正离线 fixture 验收口径，避免旧规则继续误报失败。

意义：

- 现在代码行为与规划文档的默认手指分配逻辑一致。
- 少量候选时不会再误导用户“系统已经确定是哪根手指”，而是把选择权留给 UI 和用户。

验收：

- `npm.cmd test -- tests/nail-texture-finger-assignment.test.ts tests/nail-texture-preprocess-postprocess.test.ts tests/verify-model-output-fixture.test.ts`
  - 结果：`156 passed / 0 failed / 1 skipped`

## 补充进展：postprocess 默认候选上限与主链对齐

涉及：

- `src/lib/nail-texture-recognition/postprocess.ts`
- `tests/nail-texture-preprocess-postprocess.test.ts`

完成内容：

- 将 `postprocessNailTextureDetections()` 的默认 `maxCandidates` 从 5 调整为 10。
- 新增测试覆盖“不显式传 `maxCandidates` 时，默认最多保留 10 个候选”。

意义：

- 现在不仅主链调用是 10，底层后处理自身的默认行为也与规划文档一致。
- 后续如果其他脚本或工具直接复用 `postprocessNailTextureDetections()`，不会再意外掉回旧的 5 个上限。

验收：

- `npm.cmd test -- tests/nail-texture-preprocess-postprocess.test.ts`
  - 结果：`157 passed / 0 failed / 1 skipped`

## 补充进展：UI 保留原始三档置信度语义

涉及：

- `src/components/NailArtPicker.tsx`
- `src/components/nail-art-picker-quality.ts`
- `tests/nail-art-picker-quality.test.ts`

完成内容：

- 去掉 `NailArtPicker` 中把 `medium` 置信度强行降成 `low` 的旧适配逻辑。
- `NailRegion` 与质量摘要辅助类型恢复为完整三档：
  - `high`
  - `medium`
  - `low`
- 保持“只有低置信才自动进入 review”的现有 UI 规则不变，但不再篡改模型原始置信语义。

意义：

- 现在 UI 展示层与规划文档中的 `high / medium / low` 三档定义一致。
- 后续无论做调试、人工复核还是主动学习回流，`medium` 不会再被误记成 `low`。

验收：

- `npm.cmd test -- tests/nail-art-picker-quality.test.ts`
  - 结果：`158 passed / 0 failed / 1 skipped`

## 补充进展：quality 排序器默认候选上限与主链对齐

涉及：

- `src/lib/nail-texture-recognition/quality.ts`
- `tests/nail-texture-quality.test.ts`

完成内容：

- 将 `rankNailTextureCandidates()` 的默认 `maxCandidates` 从 5 调整为 10。
- 新增测试覆盖“不显式传 `maxCandidates` 时，默认最多保留 10 个候选”。

意义：

- 现在候选数量上限不只是主链、worker、postprocess 对齐，连底层质量排序器本身也统一成 10。
- 后续单独复用 `quality.ts` 时，不会再出现“上层 10、底层默认 5”的隐性分叉。

验收：

- `npm.cmd test -- tests/nail-texture-quality.test.ts`
  - 结果：`159 passed / 0 failed / 1 skipped`

## 补充进展：debug sample 导出补齐 source / medium / runtime 元数据

涉及：
- `src/lib/nail-texture-debug-sample.ts`
- `src/components/NailArtPicker.tsx`
- `tests/nail-texture-debug-sample.test.ts`
- `tests/nail-texture-debug-priority.test.ts`

完成内容：
- debug sample 候选项现在正式保留 `source`，支持 `model / mediapipe / saliency / manual`。
- debug sample 候选项置信度改为完整三档：`high / medium / low`，不再把 `medium` 压扁。
- 导出的本地 debug sample 现在额外保留运行时摘要：`modelBackend`、`elapsedMs`。
- `NailArtPicker` 中：
  - 模型/回退识别结果会把候选 `source` 透传到 UI 区域。
  - 手动新增区域会明确标记为 `source: "manual"`，默认给中性 `confidence: "medium"`。
  - MediaPipe 先验区域会标记为 `source: "mediapipe"`。

意义：
- 后续把用户修正样本导回训练集时，终于可以区分“模型原本识别到的”与“用户手动补出来的”区域。
- active learning / 失败样本分析不再丢失来源语义，也不会再把 `medium` 误记成 `low`。

验收：
- `node --no-warnings --experimental-strip-types --test tests/nail-texture-debug-sample.test.ts tests/nail-texture-debug-priority.test.ts`
  - 结果：`6 passed / 0 failed`
- `npm.cmd run lint`
  - 结果：`0 errors / 6 warnings`
  - 备注：warning 来自 `tests/nail-texture-client-worker.test.ts` 的未使用参数，为既有问题，与本次改动无关。

## 补充进展：debug sample 导入链保留 medium 质量语义

涉及：
- `model/training/import-debug-sample.ts`
- `tests/import-debug-sample.test.ts`

完成内容：
- `import-debug-sample.ts` 现在把候选置信度映射为：
  - `high -> quality 4`
  - `medium -> quality 3`
  - `low -> quality 2`
- 新增单测，覆盖 `medium` 导入后落成 `quality: 3` 的行为。
- 同时把测试样本 fixture 补上了 `source` 字段，避免这条链继续使用旧语义样本。

意义：
- 前面在 UI / debug sample 导出里保住的 `medium`，现在不会在导入训练注释时再次丢失。
- 训练集里的质量分层终于和识别链路里的三档置信度保持一致，后续做主动学习筛样、质量审计时语义会更稳定。

验收：
- `node --no-warnings --experimental-strip-types --test tests/import-debug-sample.test.ts`
  - 结果：`5 passed / 0 failed`
- `npm.cmd run lint`
  - 结果：`0 errors / 6 warnings`
  - 备注：warning 仍为既有的 `tests/nail-texture-client-worker.test.ts` 未使用参数问题，与本次改动无关。

## 补充进展：训练 annotation 保留 candidate/source 与 sample runtime 上下文

涉及：
- `src/lib/nail-texture-dataset.ts`
- `model/training/import-debug-sample.ts`
- `tests/import-debug-sample.test.ts`

完成内容：
- 扩展训练 annotation 类型：
  - `annotation.attributes.debug` 现在保留：
    - `candidateId`
    - `source`
    - `confidence`
    - 既有 extraction / warning 字段
- 扩展训练 annotation 的 `image.debug`：
  - `detectionBackend`
  - `modelVersion`
  - `modelBackend`
  - `elapsedMs`
  - `warnings`
- `import-debug-sample.ts` 导入 debug sample 时，现在会把上述字段一起写入训练 annotation。
- 更新测试，验证导入后 annotation 确实保留：
  - sample 级运行时上下文
  - candidate 级 `source/confidence/candidateId`

意义：
- 到这一步，用户修正样本从 UI 导出、debug sample 落盘、再到训练 annotation 导入，来源语义和运行时上下文终于不再中途丢失。
- 后续做主动学习筛样、失败归因、模型/回退差异分析时，可以直接从训练样本反查“这块区域是模型识别的、手动补的，还是回退路径出来的”。

验收：
- `node --no-warnings --experimental-strip-types --test tests/import-debug-sample.test.ts`
  - 结果：`5 passed / 0 failed`
- `node --no-warnings --experimental-strip-types --test tests/nail-texture-debug-sample.test.ts tests/nail-texture-debug-priority.test.ts`
  - 结果：`6 passed / 0 failed`
- `npm.cmd run lint`
  - 结果：`0 errors / 6 warnings`
  - 备注：warning 仍为既有的 `tests/nail-texture-client-worker.test.ts` 未使用参数问题，与本次改动无关。

## 补充进展：主动学习优先级脚本正式消费 source / runtime 元数据

涉及：
- `src/lib/nail-texture-debug-priority.ts`
- `model/training/prioritize-debug-samples.ts`
- `tests/nail-texture-debug-priority.test.ts`
- `tests/prioritize-debug-samples.test.ts`

完成内容：
- `assessDebugSamplePriority()` 现在正式保留并透出：
  - `modelBackend`
  - `elapsedMs`
- `manual_candidate_added` 规则改为只对真正 `source: "manual"` 的新增候选生效，不再把其他来源的新增候选误算成手动补区。
- 优先级摘要新增：
  - `manualAddedCandidates`
- `prioritize-debug-samples.ts` 输出新增：
  - `backendBreakdown`
  - `modelBackendBreakdown`
  - `correctedCandidateSourceBreakdown`
- 这意味着优先级报告现在不仅告诉我们“哪些样本该优先回收”，还会直接告诉我们这些样本更多来自：
  - fallback 还是 model
  - wasm 还是 fallback
  - corrected candidate 里 manual / model 的占比

意义：
- 主动学习这条链已经不只是“保存了 source/runtime 字段”，而是开始真正用这些字段做筛样和报告。
- 后续排查“为什么样本需要人工修正”时，可以更快看出问题更偏向：
  - 模型覆盖不足
  - 回退路径兜底过多
  - 手动补区比例偏高
  - 某个后端（如 wasm）下样本表现更弱

验收：
- `node --no-warnings --experimental-strip-types --test tests/nail-texture-debug-priority.test.ts tests/prioritize-debug-samples.test.ts`
  - 结果：`6 passed / 0 failed`
- `npm.cmd run lint`
  - 结果：`0 errors / 6 warnings`
  - 备注：warning 仍为既有的 `tests/nail-texture-client-worker.test.ts` 未使用参数问题，与本次改动无关。

## 补充进展：active learning trace draft / handoff 保留 backend/source 统计

涉及：
- `scripts/build-active-learning-release-trace-draft.ts`
- `scripts/build-debug-sample-active-learning-handoff.ts`
- `tests/run-debug-sample-active-learning-pipeline.test.ts`

完成内容：
- `active-learning-release-trace-draft.json` 的 `activeLearning` 区块新增 `prioritySummary`，保留：
  - `backendBreakdown`
  - `modelBackendBreakdown`
  - `correctedCandidateSourceBreakdown`
  - `reasonBreakdown`
- `debug-sample-active-learning-handoff.json` 现在也会继承同一套 `prioritySummary`。
- 这意味着 source/runtime 统计现在已经从：
  - debug sample
  - priority report
  - active learning pipeline
  - release trace draft
  - active learning handoff
  全链打通。
- 主链测试补充了对这些统计字段的直接断言，避免后续改动把 trace/handoff 再改断。

意义：
- 现在不仅本地优先级报告知道“哪些样本来自 fallback / wasm / manual”，连后续交给 training release / governance 的 handoff 文件也知道。
- 后续做 release trace、governance 审核、active learning 回顾时，可以直接追踪样本结构，而不是只看到一个 imported count。

验收：
- `node --no-warnings --experimental-strip-types --test tests/run-debug-sample-active-learning-pipeline.test.ts`
  - 结果：`1 passed / 0 failed`
- `npm.cmd run lint`
  - 结果：`0 errors / 6 warnings`
  - 备注：warning 仍为既有的 `tests/nail-texture-client-worker.test.ts` 未使用参数问题，与本次改动无关。

## 补充进展：release trace index / governance 正式接入 active learning prioritySummary

涉及：
- `scripts/build-release-trace-index.ts`
- `tests/build-release-trace-index.test.ts`
- `tests/run-training-release-pipeline.test.ts`

完成内容：
- `build-release-trace-index.ts` 现在会把 active learning release trace draft 里的 `prioritySummary` 正式带入最终 trace index。
- 进入最终 trace index 的 active learning 统计现在包括：
  - `backendBreakdown`
  - `modelBackendBreakdown`
  - `correctedCandidateSourceBreakdown`
  - `reasonBreakdown`
- training release + governance 场景下，`traceIndex.activeLearning` 现在也会保留这层 `prioritySummary`，不再只剩 imported counts。
- 新增/升级测试：
  - 直接验证 `build-release-trace-index.ts` 会保留 active learning `prioritySummary`
  - 验证 training release 通过 active learning handoff 进入 governance 后，最终 traceIndex 里确实能看到 `prioritySummary`

意义：
- 到这里，active learning 的来源/后端/原因统计已经不只是在中间报告里可见，而是正式进入 release governance 的最终 trace 产物。
- 后续做 release 回顾、版本对比、失败归因时，可以直接从 trace index 回看：
  - 这批训练数据里 manual 比例高不高
  - fallback / wasm 占比如何
  - 哪类 reason 在驱动主动学习导入

验收：
- `node --no-warnings --experimental-strip-types --test tests/build-release-trace-index.test.ts`
  - 结果：`3 passed / 0 failed`
- `node --no-warnings --experimental-strip-types --test --test-name-pattern "run-training-release-pipeline can consume active learning handoff for governance trace context" tests/run-training-release-pipeline.test.ts`
  - 结果：`1 passed / 0 failed`
- `npm.cmd run lint`
  - 结果：`0 errors / 6 warnings`
  - 备注：warning 仍为既有的 `tests/nail-texture-client-worker.test.ts` 未使用参数问题，与本次改动无关。
- 补充了美甲识别取消链路：`RecognizeNailTexturesOptions` 新增 `signal`，`client-worker` 支持 `AbortError` 取消并忽略已取消请求结果；`NailArtPicker` 新增取消自动识别入口并在取消后允许转手动模式。
- 新增并通过针对性测试：`tests/nail-texture-client-worker.test.ts` 覆盖 worker 识别取消场景。
- 当前阻塞：`src/components/NailArtPicker.tsx` 存在较多历史编码损坏字符串，导致 `npm.cmd run lint` 仍在该文件报语法错误；已开始把高风险段落（顶部类型区、MediaPipe 检测函数、warning 文案）替换为干净实现，但还需要继续做一次集中清理。
