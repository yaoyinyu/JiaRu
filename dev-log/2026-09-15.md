# 2026-09-15 开发日志

## 提交推送 2026-09-14 下午至 09-15 工作区差异（白皮书 v1.1.651，首次含产品线业务代码）

用户 01:27 发起「提交并推送」。任务开始前读取白皮书 §13（至 v1.1.650）、`dev-log/2026-09-14.md` 与 README，确认 v1.1.646—650 五轮工作已完整记录。

### 并发安全确认

Codex 最后活动 09-14 20:32，提交时 09-15 01:27，**静止约 5 小时**，未收编在制半成品。

### 收编清单（37 项 = 27 修改 + 10 新增，git status 实测）

**新增 10**

- 产品线：`src/lib/ai-quota.ts`、`tests/ai-quota-guard.test.ts`、`tests/user-db-directory.test.ts`
- 模型线：`model/training/audit-development-cycle-015-annotation-acceleration.py`、`build-development-cycle-015-generated-annotation-workspace.py`、`build-development-cycle-015-per-nail-review-evidence.py`、`finalize-development-cycle-015-prelabel-review.py` + 3 个专项测试

**修改 27**（节选）

- 产品线：`src/app/api/generate-ai/route.ts`、`src/app/api/generate-seedream/route.ts`、`src/app/privacy/page.tsx`、`src/lib/auth/db.ts`、`.env.local.example`
- 模型线：首轮标注/真值终审相关 8 个脚本与 5 个测试
- 文档：README、白皮书、Goal、实施进度、提速文档、`model/training/README.md`、`dev-log/2026-09-14.md`

### 本轮意义：首次包含产品线业务代码

v1.1.649 按 2026-09-05 评审报告修复 P1×2 + P2（纯产品代码变更，模型线零改动）：

- **P1 AI 费用保护**：`src/lib/ai-quota.ts` 进程内配额与预算熔断——登录用户每日 20 次、游客每日 5 次（X-Forwarded-For 首段 SHA-256 前 16 位为身份键）、全局每日 200 次预算熔断、全局并发 3 且同身份同时 1 任务；`reserve/commit/release` 语义对超时与 5xx 保守不返还（保证刻意制造失败无法刷穿预算）；超限 429 + 中文错误 + `Retry-After`；`AI_QUOTA_*` 四变量可调、非法回退默认。两路生图 API 在供应商调用前统一接入。
- **P1 SQLite 目录预检**：`createUserDb` 打开前先对父目录 `mkdirSync(recursive)`，修复干净克隆首次匿名请求即抛 `ERR_SQLITE_ERROR(errcode=14)` 的初始化缺口。
- **P2 隐私口径**：`/privacy` 改「账号完全可选」，新增账号数据流向条目，修正「关闭改进计划后不再上传任何数据」的过度表述。

### 本轮收编的模型线结论（v1.1.646—650）

- **循环015 首个滚动检查点达成**：内置生成冻结累计 **13 图 / 13 来源组 / 85 甲**（第 5 个请求曾触发 `usage_limit_reached`，如实记录 7/133 后额度恢复补齐）；源图纠错：0005 六指显式排除、新增 0014 五甲替换图。
- 隔离工作区首轮 SAM 两轮 40 候选几何通过 30/28 但原分辨率见皮肤/整指/碎片，全部止损转定向多点。
- **PCA 长轴多正点 SAM 提速验证**：一次生成 45 候选、几何 45/45、原分辨率 44/45 可接受（仅 1 甲人工修边），人工边界干预减少 `97.78%`——采纳范围限计数正确候选，复杂图继续混合返修。
- 首批真值终审 13 通过/0 返修/0 排除；发现终结器缺开发评估角色后，以 `67332145…e0dc` 作废 13 份错误 train 报告，按 `d60d72a3…ab64` 正确登记 `development-evaluation` 角色。
- 下一步：新增 10 个独立来源组端到端推进至 23/133 与 23/90。

### README 核验

实测核验（不信任登记声明）：文档索引 v1.1.649、配额说明与 `AI_QUOTA_*` 环境变量表、`/privacy` 口径、循环015 十三图检查点与 PCA 提速条目均已被 Codex 同步到位——本轮仅把文档索引推进到 v1.1.651。

### 验证

- `npm.cmd run audit:encoding`：1040 个文本文件、0 失败。
- 全量串行回归（Python 3.13 置 PATH 首位，`npm.cmd test`）：**949/949 全过**（0 失败 0 跳过，13 分 27 秒；比 v1.1.649 记录的 944 多 5 项，来自本轮新增测试文件）。
- `git diff --check` 通过（仅既有 LF/CRLF 提示）。

### 提交与推送

- 提交标题：「提交AI生图配额熔断与SQLite目录预检，循环015十三图检查点与PCA标注提速，白皮书至v1.1.651」。
- 推送沿用既定模板，一次成功；推送后重建被抹掉的 `.git/refs/remotes/origin/master` 跟踪引用。

> 本条目由 **WorkBuddy** 编写（2026-09-15）。
