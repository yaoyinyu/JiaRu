# 2026-09-07 开发日志

## Git删除统计解释与远端跟踪边界验收（Codex，白皮书v1.1.608）

- 复核Git显示的2,505,872行删除：`辅助材料`机器产物2,405,690行、`model`数据/逐文件清单/报告97,865行、Playwright调试快照2,248行、旧归档50行，其余19行为测试和必要说明的正常改动；`src/`业务代码删除0行，训练/审计脚本删除0行。
- 本轮所有“大量删除”均为从Git索引取消跟踪。暂存删除的1,688个文件逐项通过`Test-Path`确认仍在本机，缺失0个；未清理、重置或覆盖训练数据、标注、审核结果和机器报告。
- 索引禁入复核通过：`.archive`、`.playwright-cli`、`model/reports`、`model/reviews`、`model/training/*.json`、生成型metadata及`辅助材料`非Markdown文件均为0；`辅助材料`原有7份必要Markdown说明继续跟踪。
- 更新后的干净暂存树全量测试851/851通过；原工作区`npm.cmd run build`通过Next.js编译、TypeScript和26个页面生成；编码审计905个文件、0失败；工作区和暂存区差异检查通过。干净快照构建仅因Turbopack拒绝跨项目根目录的`node_modules`目录链接而中止，不属于源码失败。
- README、`.gitignore`、白皮书§10及§13已同步；模型、数据角色、接口、发布质量门与产品HOLD状态未改变。

> 本条目由 **Codex** 编写（2026-09-07）。

## “约200万行删除”第三次只读核验（Codex，白皮书v1.1.613）

- 用户界面再次显示的删除统计仍来自历史提交`39e89db`，并非本轮再次删除；该提交取消Git跟踪1,688个生成型训练、审核与调试路径，Git据此统计2,505,872行删除。
- 按提交删除清单逐路径复验：本机保留1,688/1,688、缺失0；`src/`、`app/`、`public/`删除均为0。
- 当前为`master`、HEAD `1af3e99`、与`origin/master`一致、单一worktree；工作区没有删除状态，暂存区为空。本轮未执行删除、清理、重置、提交或推送。
- 白皮书§13同步v1.1.613；接口、模型、数据角色与产品HOLD状态不变。

> 本条目由 **Codex** 编写（2026-09-07）。

## “约200万行删除”再次只读复核（Codex，白皮书v1.1.610）

- 该数字仍来自已推送提交`39e89db`，不是本轮新删除：提交从Git跟踪中移除1,688个机器产物路径、2,505,872行文本；1,688个原文件现均仍在本机，缺失0。
- 被取消跟踪的路径以1,408个JSON、124个PNG、73个CSV、43个JPG为主；`src/`和业务页面目录删除均为0。
- 当前工作区仅9个既有开发文件发生修改，合计新增327行、删除20行，暂存区为空、删除状态0；本轮未执行删除、清理、重置、提交或推送。
- 白皮书§10既有仓库边界无需修改，仅在§13追加本次复核结论；模型、数据角色、接口和产品HOLD状态不变。
- 全量测试855/855通过，编码审计921文件/0失败，`git diff --check`通过。

> 本条目由 **Codex** 编写（2026-09-07）。

## train内开发循环001—005与固定阈值逐实例复评（Codex，白皮书v1.1.609）

- 复核并重放train内`sourceGroup`互斥开发折：训练263张正图/1581 mask与120张困难负图，评估65张正图/400 mask与40张困难负图，test0、来源组交叠0；本轮未读取旧val30、旧test100或发布留出。
- 补齐循环001/002历史逐实例证据：YOLO11n基线召回0.890、完整率0.775、漏甲图率0.400、杂散率0.1625；YOLO11s扩大容量和延长训练均未达到预注册改善门。
- 循环002首次启动在第1轮验证边界前由Windows `nvrtc64_120_0.dll`触发`0xc0000005`退出，未产生权重或质量统计；训练器新增`--windows-cuda-epoch-sync`后修订训练及循环003—005均正常完成。该修复只同步验证/checkpoint边界，不改变质量参数。
- 循环003只把训练输入512提高到640、评估仍512，得到召回0.8625、完整率0.755、漏甲图率0.4615、杂散率0.16125，训练分辨率分支关闭。
- 扩展开发数据物化器，允许以哈希绑定的精确来源组清单选择训练困难负样本，同时保持正样本、评估集、来源组原子和角色隔离。旧120张负样本报告和新60张负样本报告均可深度重放。
- 循环004只把训练困难负样本从120张降为60张，得到359/400匹配、312个完整mask、26张正图漏甲，召回0.8975、完整率0.780、漏甲图率0.400、杂散率0.130；虽是当前较优开发基线，仍未达到绝对门和预注册晋级门，不执行全量训练或正式候选治理。
- 循环005在循环004上只启用hardBoundaryWeight=0.5，结果退化为召回0.8625、完整率0.750、漏甲图率0.4308、杂散率0.19875、直接提取率0.3231；当前形态学边界监督分支关闭，不继续扫描权重。
- 白皮书v1.1.609同步§11.1、§12.13和§13；实施规范升至v1.3并新增§52，进度dashboard更新`REL-CURRENT-DEVELOPMENT-002`为PARTIAL，完成度文档明确开发证据不增加发布PASS。下一步转向train角色透明/低对比/相邻长甲/侧视/多手/复杂背景困难正样本覆盖或重采样。
- 产品继续HOLD：当前无正式候选、无生产ONNX，不执行导出、登记、`/ar-tryon`接入、commit或push。

> 本条目由 **Codex** 编写（2026-09-07）。

## 提交推送09-07工作区差异并同步README（白皮书 v1.1.611）

- 任务开始前读取白皮书 v1.1.610 的§10仓库跟踪边界、§11.1已知限制、§12.13开发循环链路与§13最新变更记录，并核对当日 dev-log 三个章节（v1.1.608—610）与 Git 状态：`master`、HEAD `39e89db`、远端同步、单一 worktree，工作区保留 10 个修改 + 2 个未跟踪文件。
- **仓库边界核验（AGENTS.md 新增规则）**：两个新增文件 `build-development-positive-source-selection.py`（构建并重放开发正样本源图选择，阻止与受保护角色身份交叠）与 `build-train-positive-difficulty-selection.py`（仅用 train 图像与真值几何构建来源组原子的困难正样本重采样计划），连同更新的 `generate-yolo-prelabels.py`，经 `head`/行数检查均为可执行训练脚本（250—370 行，含 argparse 与哈希绑定），属"完整可运行的训练代码"，不属于模型权重、数据集、逐文件开发清单、训练输出或可重建机器报告，故纳入提交。
- **并发确认**：Codex 最后改动时间戳为 09:34，提交前复核已静止约 20 分钟且期间无新增文件，可以纳入。其中包含两项 Codex 尚未写入当日日志的产物，按"原样提交、不代写他人工作细节"处理。
- 暂存区为空（与历次一致，用户所述"暂存的文件"实为工作区差异），显式 `git add` 指定文件，未用 `git add -A`，复核无 `output/`、`.workbuddy/` 等无关产物混入。
- **README 同步（提交前强制检查）**：文档索引白皮书版本由 `v1.1.606` 更正至 `v1.1.611`；Phase 4 把 development-cycle-001 由未完成改为完成（YOLO11n 基线召回`0.890`/完整率`0.775`，YOLO11s 未达预注册改善门、容量扩展分支关闭），补入循环 003—005 三条单变量复评结果与下一步困难正样本覆盖方向，并注明开发证据不增加发布 PASS。
- 验证：`npm.cmd run audit:encoding` 通过（0 失败）；按项目规则把 Python 3.13 置于 PATH 首位后 `tests/model-training-python-scripts.test.ts` 为 24/24 通过；`git diff --check` 通过，仅有既有 LF/CRLF 提示。未运行全量测试或生产构建。
- 以中文提交信息建提交并推送至 `origin/master`；推送沿用 v1.1.594 更正后的完整模板（裸 `git push` 会挂起至超时，不可依赖），凭据本身始终有效，不需要用户提供任何凭证。推送后按已知现象重建被抹掉的 `origin/master` 跟踪引用。
- 白皮书更新至 v1.1.611（§13 新增本轮记录并标注 WorkBuddy 编写，顶部文档版本同步且保留 Codex 既有标记）；无正式候选、无生产 ONNX、candidate57 TEST HOLD 与产品 HOLD 状态均不变。

> 本条目由 **WorkBuddy** 编写（2026-09-07）。

## candidate58困难正样本增量与开发循环006—008（Codex，白皮书v1.1.612）

- 5张/5个独立来源组的OpenAI内置生成源图通过原分辨率源图质量审核，全部25枚完整可见甲面形成唯一完整polygon；源图选择与train/旧val/旧test/受保护困难负样本零交叠。
- 规范训练真值索引更新为333张正图/2006 mask/117来源组；连同160张训练困难负样本物化为493张train图，test0、孤儿0，文件树与输入审计重放PASS。生成图只计数据扩充，不登记为OpenAI知识蒸馏。
- 开发折v2固定训练267张正图/1601 mask，评估66张正图/405 mask与40张困难负图；旧val30、旧test100和发布留出全程读取0。
- 循环006的困难正来源组2倍重采样退化为召回0.8775、完整率0.770、漏甲图率0.41538462、杂散率0.165，重采样及倍数扫描关闭。
- 循环007只新增困难正来源后，召回升至0.90864198、漏甲图率降至0.31818182、直接提取率升至0.40909091，证明新来源有训练价值；但杂散率升至0.15925926，违反预注册0.13上限，权重不晋级。
- 循环008只把训练困难负样本从60增至80张，召回0.89876543、完整率0.78518519、漏甲图率0.37878788、杂散率0.16296296；未压低杂散且正指标回退，关闭负样本数量扫描。
- 代码把候选工作区决策、真值终结器、开发折/物化/质量报告从candidate52固定计数扩展为权威索引驱动的单调语料增长，同时保留328/1981/112冻结下限、160训练负样本和旧val角色合同。新增回归覆盖333/2006/117增长场景及candidate58候选决策。
- 白皮书v1.1.612同步§11.1、§12.14和§13；实施规范升至v1.4并新增§53，进度`REL-CURRENT-DEVELOPMENT-002`和完成度文档v1.5同步。当前无正式候选、无生产ONNX，产品继续HOLD；下一步只做循环007的17重复、17假阳性、9无效预测错误剖面，再在结构性候选抑制与针对性真实正样本补强中选择一个单变量。
- 一次误调用全量测试命中了缺少Pillow/Shapely的系统Python，567通过/245失败，该轮作为环境无效运行；随后按项目规则使用Python 3.13执行专项Python编译及8项回归，8/8通过，并完成有效全量回归858/858。Next.js 16.2.9生产构建通过（TypeScript与26个页面生成完成），编码审计942文件/0失败，`git diff --check`通过；LF/CRLF提示为既有换行策略警告，不是差异错误。
- completion audit v3真实重放为18门5通过/13失败、当前7项发布要求1通过/6未完成，`ok=false`、`decision=hold`，报告SHA-256为`059e991552b574aed1b4d04c6d4d1f5a31e4378ec85644373b9f6e9ac20cd327`；与“新增训练资产但尚无胜出配方/发布身份”的文档结论一致。
- 未运行旧test100、发布留出、ONNX导出、生产登记、前端接入、git commit或git push。

> 本条目由 **Codex** 编写（2026-09-07）。

## 可持续Goal权威文件与稳定启动器（Codex，白皮书v1.1.614）

- 复核用户当前长篇Goal、白皮书§9.4/§9.9/§11.1/§12.12—12.14、三份引用文档和最新completion audit v3机器报告。确认原Goal的最终产品语义、standing授权、数据隔离、正负发布门、运行时、真机、Beta与回滚标准应保留，但“首先修复audit v3”已经过期，且静态正文未包含candidate58语料及开发循环006—008的当前状态。
- 新增`docs/nail-texture-current-goal.md`：§1—4固定最终结果、权威输入、授权和发布合同；§5—9记录每轮动态事实、活动里程碑、最多3项下一步、七项发布状态和外部输入；§10—11要求每轮以机器证据更新并对受控合同变更执行版本化追溯。
- 当前活动里程碑固定为`REL-CURRENT-DEVELOPMENT-002`：先对循环007的17重复、17假阳性和9无效预测完成来源组与形态错误剖面，再只选择结构性候选抑制或来源隔离真实困难正样本之一。禁止读取旧val30、旧test100或发布留出。
- 新增`docs/continuous-goal-template.md`，提供跨项目复用的稳定Goal启动器、`docs/current-goal.md`结构和`AGENTS.md`规则；本项目已把Goal任务开始读取、结束回写及受控字段版本化约束写入`AGENTS.md`。Codex Goal正文保持稳定指针，持续变化发生在权威文件中；受控完成标准不得随轮次静默降低。
- 当前Goal接口只读结果为空；现有状态更新能力不提供活动objective原地改写。用户可把稳定启动器设置为新Goal，此后Agent按权威文件持续推进和更新。
- 修正`docs/nail-texture-goal-and-reference-docs-review-2026-09-05.md`的过期时点说明：audit v3已落地，最近机器报告SHA-256为`059e991552b574aed1b4d04c6d4d1f5a31e4378ec85644373b9f6e9ac20cd327`，18门5通过/13失败、当前七项要求1通过/6未完成、`ok=false`、`decision=hold`。
- 白皮书升至v1.1.614，新增§9.10及§13记录。未训练、读取旧val/test、导出、部署、提交或推送；无批准release identity和生产ONNX，产品继续HOLD。

> 本条目由 **Codex** 编写（2026-09-07）。

## 精简可持续Goal正文与启动器（Codex，白皮书v1.1.615）

- 按用户要求将`docs/nail-texture-current-goal.md`从11节治理型文档压缩为“目标、核心要求、当前状态、下一步计划”四部分；删除YAML元数据、权威输入说明、文件内更新协议、重复发布状态表和变更账本，保留实际产品语义、质量门、授权/数据隔离、当前证据和执行顺序。
- `docs/continuous-goal-template.md`同步改成四段目标文件模板；Codex Goal启动器只负责引用目标文件、按当前状态推进、每轮回写和最终完成判定。
- `AGENTS.md`改为读取四部分并在任务结束时更新当前状态与下一步；字段保护、证据追溯和文档同步继续由项目规则约束，不再引用已删除的§1—11或`goalVersion`字段。
- 白皮书§9.10同步精简后的指针合同；复核时发现§3最高优先级仍停在循环001—005，已纠正为333张正图/2006 mask/117来源组、循环007/008结论和43个杂散事件错误剖面下一步。
- 白皮书升至v1.1.615并追加§13记录。文档变更不修改模型、训练数据角色、阈值、审计门或产品状态；无批准release identity和生产ONNX，产品继续HOLD。未执行训练、旧val/test读取、导出、部署、commit或push。

> 本条目由 **Codex** 编写（2026-09-07）。

## 循环007错误剖面完成并切换下一轮Goal（Codex，白皮书v1.1.616）

- 按用户新规则更新`AGENTS.md`：每轮完成的活动目标必须从下一步移除，`docs/nail-texture-current-goal.md`目标段必须改成下一轮唯一可执行里程碑，禁止只追加进度或继续沿用已完成/证伪的旧目标。
- 循环007错误剖面机器报告重放通过，报告SHA-256为`099d956fca21aa84bf3f312db341d18937472dee1ec75100558892f14e7d568a`；专项测试2/2通过。43个杂散事件全部闭合，正图承担`60.5/64.5`加权杂散质量，25张错误图中17张同时漏甲。
- 据此不再保留“错误剖面”或“结构抑制/正样本二选一”为当前Goal，唯一下一变量切换为来源隔离真实困难正样本补强。2026-07-14真实素材经文件名、SHA-256和`sourceGroup`三重排除现train、旧val/test和受保护登记素材后，候选池为469张/68来源组；68张代表图与6张联系表已生成到Git外审核工作区。
- 当前活动目标是完成原分辨率源图筛选、哈希绑定清单和完整mask准备；未训练、未读取旧val30/test100/发布留出、未导出、未部署、未提交或推送，产品继续HOLD。
- 同步白皮书§3、§12.14和§13 v1.1.616，实施规范v1.5 §54、实施进度和完成度审计v1.6；正式发布门未因诊断完成或素材池建立而晋升。
- completion audit v3同步重放为18门5通过/13失败、`ok=false`、`decision=hold`，报告SHA-256为`3f57b504deb09cdfa53e75103d456c3a085a9249991828230e340e63a27baf33`；编码审计947文件/0失败，`git diff --check`通过。

> 本条目由 **Codex** 编写（2026-09-07）。
