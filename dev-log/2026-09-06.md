# 2026-09-06 开发日志

## 远端仅保留完整代码与必要说明（Codex，白皮书v1.1.606）

- 从Git索引移除`output/`179个审核/调试图片约50.3MB、`model/debug/`11个调试输出约23.5MB、无人引用的32.5MB合成模型输出、一个生成型fixture及根`model/`下4个调试产物；本地文件均保留。
- `.gitignore`改为覆盖整个`/output/`、既有`/model/debug/`、合成输出、生成fixture与根模型调试产物。MediaPipe WASM/TFLite、必要小型fixture、源码、测试、配置和文档继续跟踪。
- 同一跟踪边界已写入本地`AGENTS.md`：后续Agent不得把权重、数据集、逐文件开发清单、训练/评估输出、审核截图或可重建机器报告提交到Git。
- `batch-verify-nail-detection`测试不再读取已生成的`model/nail-detection-debug.png`，改用正式小型PNG fixture模拟应跳过的调试文件名，保证干净克隆也可运行。
- 本轮不改写Git历史，不改变模型、数据角色、接口、发布质量门或HOLD状态；README与白皮书§13同步。

> 本条目由 **Codex** 编写（2026-09-06）。

## 从Git和远端剔除可重建大报告（Codex，白皮书v1.1.605）

- 将三个可重建的逐文件开发报告从Git索引移除，本地副本保留：来源组开发折7,030行、物化明细5,914行、YOLO11n逐图质量报告2,198行，共15,142行。
- `.gitignore`新增三个精确文件族规则，避免后续同类开发折、物化明细和逐图质量报告再次被提交；保留预注册计划、环境快照、失败链、训练/审计代码和测试。
- README同步训练目录的仓库策略并把技术白皮书索引更新到v1.1.605。跨机器需要完整逐文件证据时，应从外部权威输入重新生成；当前模型、数据角色、接口、质量门和产品HOLD均不变。

> 本条目由 **Codex** 编写（2026-09-06）。

## 提交行数构成复核（Codex，白皮书v1.1.604）

- 复核提交`15010de`：新增18,420行、删除98行。新增中JSON为16,005行（约86.9%），其中新建机器证据JSON为15,946行；三个最大文件分别是来源组开发折清单7,030行、物化逐文件报告5,914行和YOLO11n逐图质量报告2,198行。
- Python/TypeScript新增2,354行，其中测试391行，非测试实现约1,963行；本提交没有修改`src/`，因此不会把18,000行打入浏览器运行包。
- 结论：该数字对需要逐文件路径、角色和SHA-256可重放证据的审计型ML提交是可解释的，但不能称为18,000行业务代码。机器证据应保留，后续实现继续复用公共校验器并避免无必要报告副本。
- 本轮只复核Git统计和文件角色，不修改接口、模型、数据、质量门或发布状态；candidate57与产品继续HOLD。同步白皮书§13 v1.1.604。

> 本条目由 **Codex** 编写（2026-09-06）。

## completion audit v3身份、逐实例重放与一次性消费台账（Codex，白皮书v1.1.602）

### 实现

- 接续并读取此前真实审计进程终态：旧`v3-migration`报告正确HOLD，16门4通过/12失败，统一身份门返回`no_approved_release_candidate`；报告SHA-256为`21875b8f…9c33`。
- 新增`model/training/positive-release-consumption-ledger.py`：冻结正样本发布留出在任何读图前必须原子独占认领；严格记录`claimed→image-read-started→prediction-started→completed`。只有明确`aborted-no-data-read`的尝试可追加重试，一旦读图或预测开始，重复认领和中止均拒绝；完成态绑定预测制品索引路径与SHA-256。
- `build-positive-recognition-quality-report.py`新增正式schema v3：必须同时绑定不可变release identity文档、完成态消费台账和运行时锁；身份固定512与score threshold，报告保留schema v1/v2历史重放兼容。
- 新增`nail-texture-positive-release-evidence.ts`并接入总审计：固定核验召回0.90、完整mask0.85、漏甲图片率0.10、加权杂散率0.02、spurious权重、每图输出、逐图items哈希、模型与运行时锁；随后直接调用Python`--verify-report`重建全部证据，并独立深验消费台账。
- completion audit升级为`nail-texture-local-inference-completion-audit/v3`，新增`positiveRecognitionQuality`和`positiveHoldoutConsumption`两门；审计实现门PASS，但候选证据门必须真实满足，不再用迁移占位永久阻断。

### 验证与机器状态

- Python 3.13语法检查通过；releaseIdentity/活动标记/台账联合专项20/20通过。
- 正样本报告历史兼容、schema v3端到端及台账专项11/11通过；完整completion audit回归在同一进程中以退出码0完成。
- 真实`npm.cmd run audit:nail-texture-completion`生成v3报告：`ok=false`、`decision=hold`，18门5通过/13失败；七项当前发布要求1项通过、6项待完成；`releaseIdentity`、正样本质量和消费门均因尚无批准候选返回`no_approved_release_candidate`。报告SHA-256为`d527012257c324314a3309058621ba9e6bffc2f4119e003b192ab23caf899798`。
- 同步实施规范、实施进度、完成度说明v1.4及白皮书§9.9、§11、§12.12、§13；`REL-CURRENT-AUDIT-V3-001`只标记审计基础设施PASS，不冒充候选质量PASS。下一工程主线转入train内`sourceGroup`互斥开发折与单阶段512配方。
- 本轮未训练、读取或重跑旧test100、导出、登记、部署、提交或推送；candidate57与产品继续HOLD。

> 本条目由 **Codex** 编写（2026-09-06）。

## 白皮书多 Agent 来源标注规则固化（Codex，白皮书v1.1.601）

- 任务开始前复核白皮书当前模型状态、§12.12 audit v3迁移、§13最新变更记录及今日唯一开发日志；当前仍为candidate57 TEST HOLD、无批准生产候选、产品HOLD。
- 现行`AGENTS.md`已经要求Codex与WorkBuddy分别标注自己编写或修改的白皮书内容。为提高白皮书自身可发现性，新增§1.5，把该要求固化为正文维护合同：Codex内容使用Codex来源标记，其他Agent使用各自约定标记；任何Agent不得替其他Agent代签、补署、删除或改写既有来源标记。
- 白皮书升级至v1.1.601并在§13追加Codex署名变更记录。本次未修改源码、模型、数据、审计证据或发布状态；工作区既有releaseIdentity相关差异全部保留，未执行Git提交或推送。

> 本条目由 **Codex** 编写（2026-09-06）。

## audit v3活动标记源码迁移（Codex，白皮书v1.1.599）

验证补记：活动标记专项4/4通过；编码审计882文件/0失败；`git diff --check`通过。本阶段未运行完整旧审计集或刷新旧机器报告，保存的v2报告仍是历史快照，不能作为迁移实现的验收报告；后续身份和重放接通后需补全端到端迁移测试。

已复核白皮书v1.1.598当前状态、§9.9、最新变更及三份Goal文档。新增release-progress解析器并接入总审计，严格检查七个固定发布要求、生命周期与质量结果；旧记录保留原文并标记historical/legacy-unclassified，不再因历史拒绝永久阻断。删除、降级、重复、缺失字段和矛盾PASS均拒绝。4项专项测试通过，真实530条记录分为7当前要求及523历史。统一身份、逐实例重放与一次性消费台账尚未接入，版本明确为v3-migration并设强制HOLD门；未宣称完整v3验收。同步白皮书§12.12/§13及三份引用文档；未训练、重跑测试集、提交或推送。

> 本条目由 **Codex** 编写（2026-09-06）。

## Goal引用技术文档同步与活动合同纠偏（Codex，白皮书v1.1.598）

### 任务与基线复核

- 任务开始前按项目规则复核`docs/technical-whitepaper.md`的§3当前模型状态、§9.4 Goal、§9.9一致性审计、§11.1阻塞项及§13最新记录；当前事实仍为candidate57 TEST HOLD、无批准生产候选、无生产ONNX，旧val30/test100只能作历史回归。
- 只读复验当前机器文件：保存的completion报告仍为`nail-texture-local-inference-completion-audit/v2`，14门中4门通过/10门失败、523个历史进度标记中472个PASS；evidence profile仍绑定candidate5单权重/0.50阈值；生产manifest输入640且所指ONNX不存在；candidate57决定仍为`reject_candidate57_on_protected_test100_positive_quality_gate`且`protectedTest100Used=true`。

### 三份引用文档修改

- `docs/nail-texture-local-inference-implementation-spec.md`升级为v1.2：重写当前状态、数据角色、standing授权、固定512部署、逐实例正式门、fallback语义和发布顺序；旧candidate1—57、旧val30/test100及只打分不替换mask的ROI路线降为历史；新增§51当前发布候选活动合同及§16.2七项current-release清单。manifest目标合同采用不含manifest自身哈希的identity core，再由报告绑定manifest哈希，避免循环依赖。
- `docs/nail-texture-local-inference-implementation-progress.md`更新到2026-09-06：新增candidate57 TEST HOLD dashboard和七个`REL-CURRENT-*`要求；结构化字段固定为`lifecycle/outcome/gateRole/required`，当前七项均为`planned/pending/current-release/true`。candidate5 profile、旧授权等待、旧test100和candidate52/53计划均明确为历史或superseded；历史记录保留真实失败，不改写为PASS。
- `docs/nail-texture-completion-audit.md`升级为v1.3：明确audit v2只能证明当前不得发布，不能作为最终`complete`判据；登记audit v3 active gate、统一`releaseIdentity`、固定0.90/0.85/0.10/0.02逐实例门、全新校准/正样本发布留出/困难负样本、一次性消费台账和正确发布顺序。当前代码仍是v2，文档更新不冒充实现完成。
- `docs/nail-texture-goal-and-reference-docs-review-2026-09-05.md`增加2026-09-06整改状态，明确文档层问题已写回、代码层audit v3/profile/manifest/fallback仍待实现。

### 白皮书同步

- 白皮书升级为v1.1.598；§3纠正candidate17/18/21等旧“当前基线”、数据角色和fallback已安全完成的误述；§9.9登记三份引用文档已先行同步及剩余代码顺序；§11.1登记当前合同与仍需audit v3；§13新增v1.1.598变更记录。所有本轮新增或修改的白皮书内容按Codex来源规则署名。

### 验证

- `npm.cmd run audit:encoding`通过：880个文本文件、0失败。
- 自定义Markdown/UTF-8结构检查通过：四份相关文档表格结构正常；进度文档530个标记ID唯一；七个current-release required标记字段完整。
- 语义断言通过：三份文档和白皮书统一为candidate57 TEST HOLD、固定512、旧val/test历史回归、audit v3待实现；旧顶部fallback和“当前最优识别基线”表述已清除。
- 首次`git diff --check`只发现本轮四处Markdown行尾双空格，删除后最终复验通过；无源码、模型、数据、manifest或运行时行为变化。
- 本轮未训练、未读取或重跑test100、未执行Git提交或推送。

> 本条目由 **Codex** 编写（2026-09-06）。

## 提交推送09-06工作区差异并同步README（白皮书 v1.1.600）

- 任务开始前读取白皮书 v1.1.599 的§3当前模型状态、§9.9一致性审计、§12.12 audit v3 链路与§13最新变更记录（v1.1.598、v1.1.599），并核对当日 dev-log 与 Git 状态：`master`、HEAD `cfb57c5`、远端同步、单一 worktree，工作区保留 6 个修改 + 5 个未跟踪文件。
- 暂存区再次为空（与 08-29、08-30、09-04、09-05 历次一致），显式 `git add` 指定文件，未用 `git add -A`，复核无 `output/`、`.workbuddy/` 等无关产物混入。
- **README 同步（提交前强制检查）**：文档索引中技术白皮书版本仍为 `v1.1.592`，与实际 `v1.1.599` 相差 7 个版本（本次提交后为 v1.1.600），已更正；Phase 4 路线图补入 audit v3 两条——发布要求解析器接入总审计（530 条记录分离为 7 当前/523 历史）标记为完成，统一身份、逐实例重放与一次性消费台账仍为未完成并保留强制 HOLD 门，不宣称完整 v3 验收。
- 验证：`npm.cmd run audit:encoding` 通过（0 失败）；按项目规则把 Python 3.13 置于进程 PATH 首位后 `tests/nail-texture-release-progress.test.ts` 为 4/4 通过，与 Codex 记录一致；`git diff --check` 通过，仅有既有 LF/CRLF 提示。未运行 test100、全量测试或生产构建。
- 以中文提交信息建提交并推送至 `origin/master`；推送沿用 v1.1.594 更正后的完整模板（裸 `git push` 会挂起至超时，不可依赖），凭据本身始终有效，不需要用户提供任何凭证。推送后按已知现象重建被抹掉的 `origin/master` 跟踪引用。
- 白皮书更新至 v1.1.600（§13 新增本轮记录并标注 WorkBuddy 编写，顶部文档版本同步且保留 Codex 既有标记）；candidate57 TEST HOLD、无批准生产候选、无生产 ONNX 与产品 HOLD 状态均不变；Audit v3 仍为 `v3-migration`。

> 本条目由 **WorkBuddy** 编写（2026-09-06）。

## 提交推送09-06第二轮工作区差异并同步README（白皮书 v1.1.603）

- 任务开始前读取白皮书 v1.1.602 的§3当前模型状态、§9.9一致性审计、§11已知限制、§12.12 audit v3链路与§13最新变更记录，并核对当日 dev-log 五个章节与 Git 状态：`master`、HEAD `75a73c7`、远端同步、单一 worktree，工作区保留 13 个修改 + 17 个未跟踪文件。
- **并发确认**：这批文件时间戳集中在 15:31—16:37，最晚为 `nail-texture-development-cycle-001-plan-v4.json`（16:37）；提交前确认距最后修改已静止约 70 分钟且无 Agent 活跃改动，可以纳入提交。
- **如实说明归属**：提交主体为 Codex 的 v1.1.602 工作（一次性消费台账、schema v3 报告、正样本发布证据核验、completion audit 升 v3）；另含一批 `nail-texture-development-cycle-001` 预注册容量实验 `H-CAPACITY-001` 产物（plan-v1—v4、launch-001—003 三份失败记录、sourceGroup 开发折与物化报告），**这批产物 Codex 尚未写入当日日志与白皮书**，本次仅按"原样提交、不代写他人工作细节"处理，其技术细节未由我代述。
- 暂存区再次为空（与历次一致，用户所述"暂存的文件"实为工作区差异），显式 `git add` 指定文件，未用 `git add -A`，复核无 `output/`、`.workbuddy/` 等无关产物混入。
- **README 同步（提交前强制检查）**：文档索引白皮书版本由 `v1.1.600` 更正至 `v1.1.603`；Phase 4 把「audit v3统一身份/逐实例重放/一次性消费台账」由未完成改为完成（注明候选证据门仍须真实满足），并补入 development-cycle-001 预注册容量实验条目。
- 验证：`npm.cmd run audit:encoding` 通过（0 失败）；按项目规则把 Python 3.13 置于 PATH 首位后**串行**复验五个相关测试文件合计 63 项全部通过（`audit-nail-texture-local-inference-completion` 44/44、`build-positive-recognition-quality-report` 8/8、`nail-texture-release-progress` 4/4、`build-source-group-development-folds` 4/4、`positive-release-consumption-ledger` 3/3）；`git diff --check` 通过，仅有既有 LF/CRLF 提示。未运行 test100、全量测试或生产构建。
- 以中文提交信息建提交并推送至 `origin/master`；推送沿用 v1.1.594 更正后的完整模板（裸 `git push` 会挂起至超时，不可依赖），凭据本身始终有效，不需要用户提供任何凭证。推送后按已知现象重建被抹掉的 `origin/master` 跟踪引用。
- 白皮书更新至 v1.1.603（§13 新增本轮记录并标注 WorkBuddy 编写，顶部文档版本同步且保留 Codex 既有标记）；candidate57 TEST HOLD、无批准生产候选、无生产 ONNX 与产品 HOLD 状态均不变。

> 本条目由 **WorkBuddy** 编写（2026-09-06）。
## Git与远端跟踪边界第二轮收敛（Codex，白皮书v1.1.607）

- 任务开始前读取白皮书状态表、§9.9、§11、§12.12和§13最新记录；只读核验为`master`、单一worktree、HEAD `1eefe0d39c…`，起始工作区与暂存区干净。
- 全树审计发现仍被跟踪的本地机器资产：`辅助材料`1,321项/约163.21MB，其中1,314项不是Markdown说明；另有`model/training/*.json`292份逐文件清单、训练计划与判定，`model/reports`24份机器报告，`model/reviews`1份审核结果，数据集metadata 16项，`.playwright-cli`40份浏览器调试快照和`.archive`1份旧文件，共1,688项。
- 以上文件全部只从Git索引移除，磁盘原件逐项保留；`辅助材料`原有7份Markdown说明、训练/审计代码、测试、`dataset.yaml`模板、浏览器运行时必需资产和稳定小型fixture继续跟踪。
- `.gitignore`新增`model/reports`、`model/reviews`、`model/training/*.json`、数据集metadata、`.archive`及“辅助材料仅Markdown”边界；README同步新克隆仓库的跟踪策略。白皮书§10与§13升级到v1.1.607，模型、数据角色、质量门及产品HOLD均未改变。
- 首次从暂存树导出的独立干净快照全量回归为851项中841通过、10失败；按Python 3.13重跑后确认失败不是移除业务代码，而是既有测试依赖本机忽略的`metadata/split.json`、真实数据集、命名checkpoint、项目授权JSON和LF换行。训练环境、evaluate与pipeline测试改为每次自建最小临时数据集和假权重；困难负样本测试显式传入fixture授权；AI生图路由源码断言改为LF/CRLF均稳定的结构正则。四文件专项45/45通过，pipeline单文件18/18通过。
- 更新后的干净暂存树全量回归851/851通过；原工作区生产构建通过（Next.js编译、TypeScript及26个页面）。干净快照的构建仅因Turbopack拒绝`node_modules`指向项目根目录外的目录链接而中止，不属于源码失败。编码审计覆盖905个文件、0失败，差异检查通过。索引禁入计数、磁盘原件保留与提交/推送结果在最终执行后补记。

> 本条目由 **Codex** 编写（2026-09-06）。
