# 甲如美甲纹理正式模型目标

更新日期：2026-09-08  
当前状态：`ACTIVE / PRODUCT HOLD`

## 目标

交付可商用、在浏览器本地运行并接入`/ar-tryon`的正式美甲纹理识别模型。模型必须为每枚完整可见甲面输出且只输出一个可映回原图的完整像素级mask，并可直接用于纹理提取。模型不可用或只有MediaPipe、颜色、边界、矩形、椭圆等辅助区域时，产品必须显示“辅助定位、需要人工确认”，不得计入正式识别成功率或Beta直接可用率。

本轮活动目标：对循环011相对循环007的8张新漏图（集中于69f400a8、68cbdc00等少数来源组）与4张恢复图完成逐图原分辨率错误剖面，结合循环009剖面判定剩余漏检的类型与来源，据此确定下一个唯一单变量假设并重新预注册；当前蒸馏配方与来源组均衡回放配方均保持关闭，禁止扫描种子/配额/阈值、读取旧val/test或原样重训。

## 核心要求

- 采用单阶段YOLO实例分割、512方形letterbox和产品去重；candidate53—57的旧ROI stage2路线保持关闭，除非新证据证明需要真正替换mask的stage2。
- 每轮只验证一个可证伪假设，在train内按`sourceGroup`使用互斥开发折，至多运行两个短程单变量实验；禁止同输入原样重训、使用正式发布集调参或连续扫描微小阈值。
- 每轮形成可重放的阶段结论后，必须立即把已完成或已证伪的活动目标从“目标”和“下一步计划”中移除，并将“本轮活动目标”切换为下一项唯一可执行里程碑；不得用只追加进度的方式继续沿用旧目标。若当前轮次只补充治理规则而尚未完成模型里程碑，则保留真实未完成活动目标，不得伪造推进。
- 训练图片必须具备商业训练授权，并通过原分辨率源图、完整甲面mask、多边形合法性、同图零交叠和数据角色隔离审核。裁边、严重模糊或无法确认完整轮廓的正图不得训练。
- 正式识别队列只接纳完整可见且轮廓可确认的甲面；残缺、触边、严重遮挡或轮廓不可确认甲面应在候选资格层拒绝并交给人工流程，不得强行输出或计为成功。锁定真值中已经确认完整可见的甲面不得因模型漏检而移出分母。
- 水印图允许进入train，但水印不得进入甲面mask；必须登记水印位置和类型，并完成去除、遮挡、模糊及位置变化消融，证明模型没有学习水印捷径。
- 配方锁定后建立不少于30张全新来源隔离校准集，只校准一次阈值；随后冻结权重、512预处理、阈值、后处理和唯一`releaseIdentity`。
- 使用不少于100张全新来源隔离正样本发布留出进行一次正式评估，要求实例召回率≥0.90、完整mask比例≥0.85、漏甲图片率≤0.10、加权杂散率≤0.02。
- 候选锁定后建立不少于100张全新未见困难负样本；部署512下原图、裁右下12%、模糊右下角均须误检图片数=0、误检检测数=0、相对原图delta=0，其他位置水印增加对应变体。
- 生产ONNX、manifest、训练框架、WebGPU、WASM、浏览器、桌面、四类移动设备、Beta、产品质量和回滚证据必须绑定同一`releaseIdentity`，禁止跨候选拼接。
- 四类移动设备分别至少完成20次有效运行；Beta人工审核不少于100张；完成两个不同权重且完整批准版本的回滚建立与实际演练。
- 现有项目图像和本机计算资源可按standing授权直接用于商业模型开发；清单、来源、许可、角色和SHA-256仍须记录。已消费val/test/holdout及其派生物不得回流训练。
- 只有completion audit v3对同一`releaseIdentity`的全部当前发布门重放为`ok=true`且`decision=complete`，才允许解除产品HOLD并标记目标完成。

## 当前状态

- 当前没有批准的正式候选或`releaseIdentity`，没有生产ONNX；生产manifest仍为640占位，产品继续HOLD。
- candidate57已在唯一一次有效test100上因漏甲图片率`0.13`和加权杂散率`0.04693141`失败并固定为TEST HOLD，不得重跑、反调、导出或部署。
- audit v3基础设施已经完成；最近总审计为18门5通过、13失败，当前七项发布要求1项通过、6项未完成，`ok=false`、`decision=hold`。
- 当前训练语料为333张正图、2006个完整mask、117个来源组，另有160张训练困难负样本；训练物化和角色隔离重放通过。
- train内开发折为训练267张正图/1601 mask，评估66张正图/405 mask和40张困难负图；循环006—008均未读取旧val30、旧test100或发布留出。
- 循环007当前最好：召回`0.90864198`、完整率`0.79012346`、漏甲图片率`0.31818182`、加权杂散率`0.15925926`、直接提取率`0.40909091`。召回改善，但杂散和漏甲图片率未达门，配方不晋级。
- 循环007逐实例错误剖面已完成并通过重放：43个杂散事件闭合为17个重复、17个假阳性和9个无效预测；加权杂散质量`64.5`中正图占`60.5`（`93.79845%`），25张错误图中17张同时漏甲（`68%`）。因此关闭“先做结构性候选抑制”的方向，唯一下一变量确定为来源隔离真实困难正样本补强。报告SHA-256为`099d956fca21aa84bf3f312db341d18937472dee1ec75100558892f14e7d568a`，且未读取旧val30、旧test100或发布留出。
- 原469张/68来源组池经磁盘追溯后发现大部分候选存在裁边、模糊、遮挡、拼图或历史排除事实；同时从未进入当前角色的2026-07-11/12真实素材中重建出90张未用池。最终按原分辨率只保留10张、10个稳定文件级来源组、预计69个完整可见甲面，精确选集位于`E:\AI Project\Codex\JiaRu_image\审核工作区\2026_9_8_development_positive_selection_v2`。
- 精确选集逐甲真值终审已完成：65个完整甲面mask（0010/0017/0039按`visibleNailCountCorrection`核减69→65，理由码`nail-not-fully-visible-on-recheck`），3处人工多边形返修、3处假候选删除、1处SAM拓扑修复、1处新月嫌疑豁免（0011#9），合法性/零交叠/几何审计与逐甲原分辨率审核全部通过；candidate58b合并索引343张/2071 mask/127来源组，物化503张train、输入审计PASS。
- 循环009信号测试已在评估折逐字节锁定（fold-0 recordsSha256 `c87cc03e…83d7`与cycle007相同）下完成：漏甲图片率0.31818182→`0.42424242`（21→28张，未降反升）、逐甲召回0.90864198→`0.88148148`（低于护栏0.8975）、完整率`0.77777778`、加权杂散率`0.17283951`、直接提取率`0.36363636`。机器逐图复算纠正旧文字：12张回归图当前漏17实例，其中基线已漏3、净新增14；3张改善共恢复3实例，91张不变，合计净漏甲增加11（37→48）。主判据与护栏同时未过，『追加真实困难正样本直接续训』分支关闭。
- 12张回归图的17个当前漏甲均经原分辨率确认属于完整可见甲面，覆盖普通清晰大目标及透明/低对比、侧视、远小目标、弯曲遮挡风险和复杂背景；因此残缺甲面拒识是正确产品策略，但不能解释或掩盖循环009退化。错误剖面报告SHA-256为`39d6ee58…052f`。
- 循环010只加入冻结cycle007教师的特征、软分数、框分布、软mask与边界蒸馏，训练12轮，权重`fd9beb0d…76dc`；固定逐实例复评相对循环009改善为漏甲图28→25、漏甲实例48→41、召回0.88148148→0.89876543、加权杂散率0.17283951→0.16296296，但仍未恢复cycle007的21图/37实例/0.90864198，完整率仍0.77777778、直接可提取率仍0.36363636。预注册allRequired判据失败，当前蒸馏配方关闭且不晋升。
- 循环011只把train每epoch抽样分布改为来源组均衡回放（新增`nail_texture_balanced_sampler.py`：337图/111组每组3-4样本、种子20260908、组间轮转交错；`build_dataloader`补丁仅train路径生效，评估折逐字节不变，专项测试4/4、既有训练入口测试27/27通过）：漏甲图25→`22`、漏甲实例41持平、召回`0.89876543`、完整率`0.78765432`、加权杂散率`0.1382716`与直接可提取率`0.43939394`均创循环001-011历史最佳，但漏甲图差1张未达21、召回未达0.90864198，预注册allRequired失败，决策`signal_improved_but_failed_close_current_balanced_replay_recipe`按branchClosure关闭当前均衡回放配方。
- 循环011逐图剖面：相对cycle007新漏8图/8实例（集中于69f400a8与68cbdc00等少数来源组）、恢复4图/4实例、13图持平（37+8-4=41闭合）；相对cycle010漏甲实例零和徘徊（7图改善/6图回归）但杂散率0.16296296→0.1382716、直接可提取率0.36363636→0.43939394。
- 新增65 mask真值已进入规范索引并保留；循环009、010、011权重均不得导出、登记、接入或部署。
- 模型容量、延长训练、640训练输入、现有边界权重、困难正样本2倍重采样和训练负样本60→80等分支均已关闭。
- 当前无外部阻塞；只有Beta人工判断或物理设备真实输入阶段可能需要用户参与。

## 下一步计划

1. 对循环011相对循环007的8张新漏图（nail_01105/01111/01112同组69f400a8、nail_00774/00777同组68cbdc00、nail_00624、nail_00967、nail_01183等）与4张恢复图做逐图原分辨率错误剖面，归类漏检形态与来源组聚集性，并与循环009剖面交叉对照。
2. 基于剖面确定下一个唯一单变量假设并重新预注册；当前蒸馏与均衡回放配方均保持关闭，禁止扫描种子/配额/阈值、读取旧val/test或原样重训。
3. 若后续某轮预注册判据通过，胜出配方依次进入全量训练、全新calibration30、`releaseIdentity`冻结、全新positive holdout100、全新negative holdout100、生产ONNX与多后端一致性、浏览器/真机/Beta/产品质量及回滚验收。
