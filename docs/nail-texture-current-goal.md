# 甲如美甲纹理正式模型目标

更新日期：2026-09-21
当前状态：`ACTIVE / PRODUCT HOLD`

## 目标

交付可商用、在浏览器本地运行并接入`/ar-tryon`的正式美甲纹理识别模型。模型必须为每枚完整可见甲面输出且只输出一个可映回原图的完整像素级mask，并可直接用于纹理提取。模型不可用或只有MediaPipe、颜色、边界、矩形、椭圆等辅助区域时，产品必须显示“辅助定位、需要人工确认”，不得计入正式识别成功率或Beta直接可用率。

本轮活动目标：只使用已重放通过的循环016 v2人工残项工作区，完成21个SAM止损残项的原分辨率人工polygon修正与逐实例视觉通过；随后把21个人工polygon、36个已冻结SAM候选、221个冻结保留实例和4张源图/25实例整体排除动作合成为预计278实例、18来源组且与train来源组零交叠的v2验证真值，v2全量复核通过前不启动第三轮训练。

## 核心要求

- 512方形letterbox、产品去重和单阶段YOLO仍为冻结对照。MediaPipe只可生成待验证ROI，辅助ROI不得计为正式识别结果；定位失败转人工确认，禁止复活固定网格兜底。
- 每轮只验证一个可证伪假设，在train内按`sourceGroup`使用互斥开发折，至多运行两个短程单变量实验；禁止同输入原样重训、使用正式发布集调参或连续扫描微小阈值。
- 训练图片必须具备商业训练授权，并通过原分辨率源图、完整甲面mask、多边形合法性、同图零交叠和数据角色隔离审核。裁边、严重模糊或无法确认完整轮廓的正图不得训练。
- 正式识别队列只接纳完整可见且轮廓可确认的甲面；残缺、触边、严重遮挡或轮廓不可确认甲面应在候选资格层拒绝并交给人工流程，不得强行输出或计为成功。锁定真值中已经确认完整可见的甲面不得因模型漏检而移出分母。
- 水印图允许进入train，但水印不得进入甲面mask；必须登记水印位置和类型，并完成去除、遮挡、模糊及位置变化消融，证明模型没有学习水印捷径。
- 配方锁定后流程：建立不少于30张全新来源隔离校准集，只校准一次阈值；随后冻结权重、512预处理、阈值、后处理和唯一`releaseIdentity`；使用不少于100张全新来源隔离正样本发布留出进行一次正式评估，要求实例召回率≥0.90、完整mask比例≥0.85、漏甲图片率≤0.10、加权杂散率≤0.02；候选锁定后建立不少于100张全新未见困难负样本，部署512下原图、裁右下12%、模糊右下角均须误检图片数=0、误检检测数=0、相对原图delta=0，其他位置水印增加对应变体。
- 发布证据必须绑定同一`releaseIdentity`，禁止跨候选拼接：生产ONNX、manifest、训练框架、WebGPU、WASM、浏览器、桌面、四类移动设备（分别至少完成20次有效运行）、Beta人工审核（不少于100张）、产品质量，以及两个不同权重且完整批准版本的回滚建立与实际演练。
- 现有项目图像和本机计算资源可按standing授权直接用于商业模型开发；清单、来源、许可、角色和SHA-256仍须记录。已消费val/test/holdout及其派生物不得回流训练。
- 只有completion audit v3对同一`releaseIdentity`的全部当前发布门重放为`ok=true`且`decision=complete`，才允许解除产品HOLD并标记目标完成。

## 当前状态

### 发布状态

- 当前没有批准的正式候选或`releaseIdentity`，没有生产ONNX；生产manifest仍为640占位，产品继续HOLD。
- audit v3基础设施已完成；2026-09-20最新深度重放（报告`b89b6b26…737f`）仍为18门5通过/13失败、`ok=false`、`decision=hold`，`releaseIdentity`状态为`no_approved_release_candidate`。该审计只复验既有绑定证据，没有新增发布PASS。

### 当前权威基线

- candidate57的唯一一次有效test100：实例召回率`0.95848375`与完整mask率`0.86462094`通过，漏检图片率`0.13`与加权杂散率`0.04693141`失败，固定TEST HOLD；禁止重跑、反调、导出或部署。
- clean98当前最佳基线为循环011锁定权重`2d528876…04f3`：实例召回`0.94067797`单项通过，完整率`0.83898305`、漏甲图片率`0.22413793`、加权杂散率`0.0960452`未过，`decision=fail_train_internal_development_floor`。开发评估口径为98图只读集（58张正图/354 mask真值v2＋原40张困难负图，文件树SHA-256 `c4a12153…e1bd`）。

### 已关闭路线

- candidate5—29系列候选与循环001—014全部分支（容量扩展、640训练输入、边界权重、困难负样本数量扫描、冻结教师多信号蒸馏、来源组均衡回放、定向真实正图增广、逐甲高分辨率ROI mask替换、固定四网格候选供给）均按预注册合同关闭、不晋升。循环013/014由零推理可达性判决证伪：瓶颈是stage1候选供给与统计分辨率，不是替换成功率或训练配方；58图规模下任何相对信号在噪声内，判杂散率需148图、判漏甲率需352图（Wilson 95%口径）。全部历史数字、权重、报告哈希与失败判定原样保留，详见白皮书§12.9—12.28及对应dev-log。

### 当前缺口与阻塞

- 循环015供给账：旧333图库存按当前角色重隔离仅7图/7来源组无交叠（审计`de954b63…1c00`），v4权威源图门复核只留下`01063`。继续复核发现其第15枚所谓完整甲面实际仅剩被相邻手遮挡的狭窄甲尖；普通SAM v1/v2/v3是同一字节快照，连同ranked和后续18候选三类独立方案均为返修失败。止损审计`5b79eee9…60b2`据此排除唯一恢复图，现有可返修容量为0；所有当前候选均须新采，总最低新批准正图缺口424张（clean 352＋calibration30＋positive holdout100），Beta100与新增训练正图另计。**素材供给列为一等风险。**
- 提速路线审计`c142416b…9445`绑定历史1166张源图门、160张精确授权候选、120张/636 mask最终真值、容量与吞吐报告并深重放通过。历史源图通过率为`565/1166=0.48456261`，源图合格候选的真值批准率为`120/160=0.75`、Wilson 95%下界`0.67757630`；旧50张里程碑点估计只能形成37.5张批准图，无法补足clean 58→148所需90张。新里程碑采用保守下限133张源图合格候选；从未经筛选原图开始的容量规划为点估计248张、双下界保守包络292张。该比例只用于产能规划，不替代逐图审核或保证批准。
- 循环015源图账现以真实素材入库v1为准：用户2026-09-19指令停用AI生成供给（今后生效），v9的43图/43来源组/265甲生成源图经用户裁决保留计入；首批真实素材12图/12来源组/57甲冻结（Givenchy与CHANEL两张品牌logo图按“所有图片均可使用”裁决登记水印并挂训练前消融债，10.jpg裁断与15.jpg遮挡按Goal核心要求排除），入库审计`326eb3bb…b680f`深重放确认身份或角色交叠0，源图合格进度55/133、剩余78张。
- 第二批10图/60甲已完成终审。守卫版YOLO生成63个粗候选，3个过量低置信候选经原图确认均为皮肤；PCA长轴三正点与四角负点SAM一次生成60个目标候选，0 fallback、0错误，几何60/60，逐甲2倍与全图原分辨率复核60/60通过，人工边界返修0。最终候选合法60/60、同图交叠0、终审10通过/0返修/0排除。
- 首轮未安装Ultralytics只读图片守卫的预标注运行已显式排除；守卫版规范重跑与旧10份polygon逐文件字节一致、差异0/10，缓存0，工作区清单`1e3ee48b…1d66`深重放不变。该事件没有被改写为通过，也没有污染规范证据链。
- 第三批10图/60甲包含短自然甲、深肤色、成熟手、透明/高光和双手斜视等困难场景。守卫版YOLO候选覆盖率`0.933333`，模型少检的5甲仍留在分母并由原图人工提示补齐；4个过量候选经原图确认均为已有甲面的重复候选。PCA多点SAM经原分辨率审核直接接受51/60，9甲转人工；短自然甲的一次紧框返修仍污染皮肤，按止损转人工。含皮肤污染的final-v1由拒绝记录保留，纠正后的final-v2为60/60合法、零交叠、终审10通过/0返修/0排除。
- 加速审计`ce00a141…2e31`深重放为`retain_guarded_pca_sam_with_one_retry_stoploss_and_residual_manual`：第三批SAM直接接受率85%，最近两批合计111/120即92.5%；本批守卫预标注与SAM合计39.290秒，最终60甲原分辨率复核99.883秒。人工polygon绘制活跃时长因任务挂起无法可靠分离，未伪造；该证据证明路线减少大多数边界绘制，并证明一次失败后的人工止损优于重复SAM，但仍不能直接外推正式发布日期。累计开发评估真值索引为33图/205 mask、0冗余、0冲突，全部`trainingUse=prohibited`；当前进度33/90、剩余57张，总最低新批准正图缺口391张。
- 第四批10图/60甲完成终审。守卫版粗候选为8图精确、1图过量、1图少检；唯一被裁候选经原图确认是非甲面的指关节误检，少检图的拇指仍保留在分母。9图55个PCA多点SAM经原分辨率审核55/55接受；少检图一次拇指定向SAM误分木纹后停止重试，最终该图5甲转人工polygon。final-v1拇指覆盖不足、final-v2纠错目标错位、final-v3继承双定义均保留拒绝记录；final-v4为60/60合法、零交叠，终审10通过/0返修/0排除。
- 加速审计`818e7079…ec38`深重放为`retain_guarded_pca_sam_and_count_mismatch_stoploss`：本批55/60即91.67%最终边界无需人工描绘，近三批合计166/180即92.22%；规范机器阶段为44.896秒。248.534秒终审窗口包含证据构建失败和纠错迭代，不作为纯人工活跃工时。累计开发评估真值索引`1a2a752a…3b16`为43图/265 mask、0冗余、0冲突，全部`trainingUse=prohibited`；当前进度43/90、剩余47张，总最低新批准正图缺口381张。源图、工作区和真值均完成独立重放，缓存0。
- 训练吞吐：`workers: 0→8`是唯一零语义代价杠杆（约22%），但位于冻结合同内且切换增强RNG流，其指标中性尚未实测，是下一次训练启动的预注册前置；`workers=8`相对12轮仅省约1.72分钟，日历关键路径是采集/筛选/标注/终审供给而非训练墙钟。措施与采纳前置条件见`docs/training-throughput-speedup-measures.md`。
- 2026-09-10证据完整性事件（基准脚本无守卫误改10张JPEG）已按`records[].imageSha256`恢复并重放`verified`；对哈希绑定数据集跑临时脚本必须先装只读图片守卫、优先隔离副本（规则已入`AGENTS.md`），细节见白皮书§12.25。
- 首批真实素材逐甲标注与终审完成（2026-09-19）：0002因失焦排除，11图/52甲经geometry-audit-v6（pass:52/suspect:0）与逐甲原分辨率终审入开发评估真值索引v5（54图/317 mask＝43 AI+11真实，sha256 `5bb70b56…2eed`，权威重放通过，全部`trainingUse=prohibited`）。0011-n4无名指过分割经三版多边形迭代与数值化边界检测定案，未降低任何审核门。
- 经用户明确指示（继续优化模型、加速推进），在148门未达（54/90）下预注册并启动单变量诊断性短训：新增`materialize-development-cycle-015-real-material-dataset.py`在cycle012基础数据集上仅追加11图/52 mask（train 361图/1783 mask，`90a1bf08…da88b`，重放验证通过）、361图/135组均衡回放映射；预注册计划`nail-texture-development-cycle-015-real-material-train-plan-v1.json`（唯一变量`realMaterialPositiveAugmentation`，学生起点cycle004、冻结教师cycle007、其余合同与cycle012逐字一致，显式声明偏差148门与`workers 0→8`）。该短训仅作诊断信号，正式候选/发布资格与clean148门不变。
- 短训结果（2026-09-19）：5m59s、11 epochs早停（best@epoch7）。clean98只读评估（阈值0.25单次推理）：召回`0.92655367`、漏图率`0.27586207`（16图）、加权杂散`0.13559322`、直接可提取`0.43103448`，decision=`fail_train_internal_development_floor`；预注册五项信号门全部不通过。vs cycle012唯一改善为漏图18→16，其余差异在Wilson噪声内——`realMaterialPositiveAugmentation`配方按falsifiedWhen条款关闭，权重不部署不导出，真值索引v5与物化数据集保留供clean148里程碑复用。信号判定再次确认：供给规模（58图评估）下训练信号不可判别，日历关键路径仍是审核/标注供给而非训练配方。
- batch2快速通道（2026-09-19，用户三条裁决）：①甲面级过滤——图中有完整合适甲面即可用，遮挡/失焦甲面退出分母；②裁断输出通道——受影响素材的可用区域按原分辨率裁断输出到`真实素材/美甲图片素材/处理`（0013/0021/0022三张裁断图入冻结目录v2，衍生组与原图共享来源）；③免审直接训练——购入授权素材跳过逐图机器审查。10图0013–0022以YOLO cycle012 best预标注（conf≥0.6或白名单+边距≥8px）得37掩码，修复白名单键名bug后恢复0013 n4至38掩码；预标注多边形直接作为训练真值（证据等级偏差已预注册声明，仅限诊断性实验）。数据集物化`2cd219c7…801b`（train 371图/311正图/1821 mask，累计真值64/90），计划`nail-texture-development-cycle-015-batch2-supply-scale-train-plan-v1.json`（唯一变量`realMaterialSupplyScaleBatch2`）。
- 第二次短训结果（2026-09-19）：两次基础设施失败（err1455页面文件不足、worker对存在文件报FileNotFoundError）后回退`workers 8→0`（cycle012基线值，语义零变化）成功：5m04s、7 epochs早停（best@epoch4），val Mask mAP50 0.943。clean98单次只读评估：召回`0.92655367`（与v1逐位持平）、漏图率`0.29310345`（17图）、加权杂散`0.17372881`、直接可提取`0.4137931`、硬负误检图0，decision=`fail_train_internal_development_floor`；五项信号门再次全部不通过，`realMaterialSupplyScaleBatch2`配方按falsifiedWhen关闭。结论：累计+21张真实素材（两种真值等级）在clean98上均无可判别改善，瓶颈不在小批量供给规模，预标注真值也不支持替代人工审查；下一步回到供给结构性扩量，148门前不再训练。
- batch3扩量与第三次短训（2026-09-19，用户指令『继续训练，不要停』）：30图（0023–0052）全部取自全新文件夹2（2/451–2/480，冻结目录v3），零交叠；预标注230候选→统一过滤209 mask（0零掩码图）；数据集物化`c4193cc8…58e`（train 401图/341正图/2030 mask，累计真值账94/90），计划唯一变量`realMaterialSupplyScaleBatch3`（workers 0稳定基线）。8m32s、11 epochs早停（best@epoch4，val Mask mAP50 0.945）。clean98单次只读评估：召回`0.92372881`、漏图率`0.29310345`（17图）、加权杂散`0.12429379`、直接可提取`0.44827586`、硬负误检图0，decision=`fail_train_internal_development_floor`；五项信号门第三次全部不通过，配方按falsifiedWhen关闭。
- batch4扩量与第四次短训（2026-09-19，用户指令『持续推进模型训练进度』）：54图（0053–0106）取自文件夹2/481–534（冻结目录v4），零交叠；预标注407候选→统一过滤345 mask，1图零掩码剔除留档（53图/345 mask入训练）；物化`e20497c4…cabe`（train 454图/394正图/2375 mask，真实素材账147图）。attempt1被系统内存压力硬崩溃中断（归档+重放验证后attempt2成功）：12 epochs全跑完，best val Mask mAP50 0.9494。clean98：召回`0.93220339`（0.9237→0.9322改善）、漏图17、漏实例24（27→24）、杂散`0.12429379`、直接可提取`0.43103448`、硬负误检图1，decision=`fail_train_internal_development_floor`；五项门仍未全通过，`realMaterialSupplyScaleBatch4`配方按falsifiedWhen关闭。
- 全量供给终局测试（2026-09-19，用户指令『一次性把所有图全部加到训练队列，一次性完成全部训练』）：batch5冻结689张（文件夹2剩余366张2/535–900+文件夹3全部323张，冻结目录v5、工作区v7），真实素材账达837图；预标注→过滤→物化→长训练流程执行中，结果另条登记。
- batch5终局结果（2026-09-19）：预标注4725候选→统一过滤3975 mask（10图零掩码剔除留档，679图入训练）；物化`8b1e6249…a4fd`（train 1133图/1073正图/6350 mask）；计划唯一合同偏差epochs 12→60/patience 4→6（预注册）。长训早停于epoch 9（best@5，val Mask mAP50 0.9442）。clean98：召回`0.92372881`、漏图17、漏实例27、杂散`0.14265537`、直接可提取`0.48275862`（历史最高、最接近0.5门但其他轴回落）、硬负误检图1，decision=`fail_train_internal_development_floor`；五项门仍未全通过。**终局判定：五次单变量实验（累计837图账）均无可判别改善，预标注真值供给路线在当前评估合同下终局证伪**；后续改进方向转向评估集缺口分析或真值质量（人工标注），不再以供给规模为训练变量。
- 2026-09-20路线诊断核对本地Ultralytics 8.4.92下batch5权重：`YOLO11n-seg`在512输入时分割头stride为8/16/32，mask prototype实测为`1×32×128×128`，`mask_ratio=1`只提高训练真值栅格精度，不会把原始prototype提升到512。clean98中354个真值只有327个匹配、297个达到IoU≥0.75，17图/27实例漏检；逐图诊断显示约14个为检出但定位/边界偏移、约8个为无候选的完全漏检，主要集中在侧视细甲、非常规手姿、超长stiletto和远距小目标。结论是当前单阶512 YOLO不宜再作为唯一精细抠图路线；旧模型自预标注会遗漏正是需要学习的盲区，已形成回音壁。10张零保留mask盲区图已人工审出23枚可标甲面，可作为两阶段可达性审计的起点。
- 循环016首轮两阶段审计已完成且结论严格分层：①静态`1×3×384×384` SegFormer-B0兼容随机权重ONNX通过checker、ORT CPU及真实Chromium 153的WASM/WebGPU有限值推理，证明现有浏览器栈可承载独立ROI分割器，但不证明质量或真机时延；②clean98上固定MediaPipe Hands（最多2手/10候选）与batch5 YOLO并集仅覆盖`336/354=0.94915254`，仍有12图/18实例无ROI，Stage A失败，MediaPipe不得作为召回保底；③现有batch5全图YOLO在真值1.6倍ROI上的oracle边界审计仅28/354 ROI有预测，IoU均值`0.00310480`、Boundary IoU均值`0.00017110`、2px Boundary F1均值`0.00063984`、联合通过0，旧权重不得复用为Stage B；④仅由cycle012训练角色派生1731个384 ROI，按sourceGroup拆为1428 train/303 val、组交叠0，随机初始化SegFormer-B0兼容pilot训练30轮，最佳epoch27内部val IoU`0.74888171`、Boundary IoU`0.08546670`、2px Boundary F1`0.24723864`、联合通过率0，未过`0.85/0.90/0.80`预注册pilot门。由此“两阶段运行架构可部署”成立，但“当前配方可达95%边界质量”未获验证；首轮配方关闭，不晋升候选。`nvidia/mit-b0`官方预训练权重因许可证限定非商业用途被排除，正式路线只允许许可兼容权重或项目数据从零训练。四份主报告SHA-256分别为浏览器运行`2e86adbe…56d1`、Stage A `15ec747e…e5ad`、oracle Stage B `17bb6ac7…fbb`、ROI pilot `8940c268…1c69`，全部可重放；clean98与cycle012文件树重放未变，旧val30/test100/发布留出未用于训练，产品HOLD不变。
- 循环016固定误差归因已完成：303个隔离val ROI逐实例预测全部保存并绑定哈希，报告`e1673b52…0b66`重放通过。首轮模型均值IoU/2px Boundary F1/边界距离为`0.74888171/0.24723864/11.43975062px`；把真值直接压到96再恢复到384的1/4步幅理论上限仍只有IoU`0.96299971`、Boundary F1`0.89934909`，且仅`0.48844884`实例达到0.90，证明固定双线性1/4输出本身无法稳定满足边界门。低前景/邻环色差组Boundary F1仅`0.18104251`，高色差组为`0.33358797`；高边界复杂度组IoU`0.70509772`，监督与学习难度同时存在。
- 按归因只改变最终输出头的第二轮pilot已完成：保持同一数据、来源组拆分、随机初始化、30轮、优化器、阈值和`0.85/0.90/0.80`门，只把固定双线性输出替换为接收384 RGB与粗logit的全分辨率浅层残差细化头。最佳epoch30为IoU`0.75931897`、Boundary IoU`0.09232614`、2px Boundary F1`0.26289865`、联合通过率0；相对首轮仅`+0.01043726/+0.00685944/+0.01566001/0`，报告`46f944b1…01af`重放通过。方向性小幅改善不足以过门，第二轮配方关闭、不晋升、不导出；下一瓶颈切换为固定样本的原分辨率真值边界质量审计，避免继续盲训。
- 固定36例原分辨率真值审核已完成：原图/标签哈希、cropBox和ROI字节均36/36精确重建；27例通过、5例过分割、1例混合错误、3例因低清无法可靠确认，覆盖13个来源组。4例共享`decoration_tip_contamination`，达到预注册系统误差阈值；最终报告`b8bbd512…4531`重放决定`full_validation_truth_reaudit_and_correction_before_training`。因此首轮和第二轮pilot指标继续保留为历史诊断，但当前303实例val折在全量真值复审和v2重建前不得用于模型选择或阈值校准。
- 全303实例原分辨率审核已完成。分片001—013全部重放通过，最终冻结报告`4aa2ff6e…1298`为230通过、36过分割、4欠分割、19混合错误、14低清边界不可确认，覆盖21来源组，决定`full_validation_truth_reaudit_and_correction_before_training`。绑定13个分片哈希的合并裁决`eafe2df2…5fc7`已冻结；修正/排除manifest `ff05e428…6825`按源图硬门把14个低清实例所在4张图/3来源组整体排除，共25实例，另列57实例返修、221实例保留。一次错误把全量报告`expected`写成36的构建结果仍保留为`rejected-expected-count-bug`，未伪装为通过。
- 循环016 v2返修计划与隔离逐点编辑工作区已物化。计划只绑定修正manifest `ff05e428…6825`、全量审核报告`9f369e01…3ae9`和cycle012物化报告`f47b62f1…61fc`；构建器安装只读图片守卫，逐项验证源图/标签哈希，并生成25张原图上下文、57组原始像素裁块/旧边界叠加和浏览器逐点编辑入口。规范v2工作区报告`54eceb5f…3b28`及资产聚合哈希`768c509f…ed11`重放通过，计数固定为57返修、221保留、25实例/4源图排除、预计v2 278实例/18来源组；57项全部保持`pending_original_resolution_manual_repair`和`trainingUse=prohibited_until_v2_full_review_pass`，未自动批准任何polygon。首版导出绑定字段为空的工作区`4c0ed0fb…c7ce`已被v2替代，只保留为历史构建产物，不作为返修入口。cycle012 ROI数据集文件树`0b5f09e8…76cb`重放未变。
- 循环016 v2返修已执行一次固定SAM2.1-L候选止损通道：57/57候选均合法、与同图其他真值交叠0，审核包`129a1d55…fcdf`逐项绑定原图、标签、候选及15页三联裁块。视觉裁决冻结报告`2f918ecf…4c39`为36枚`accept_sam`、21枚`manual_repair`，人工描边残量由57降至21（减少63.16%）；失败项禁止再次运行SAM。规范残项工作区v2报告`6329d859…db6d`与资产聚合哈希`9c4b2dd6…385f`重放通过，使用独立浏览器存储键和动态21项计数；21项来自11张源图且全部保持待人工修正；36个候选仅冻结为待v2全量复核输入，当前训练仍禁止。cycle012 ROI文件树`0b5f09e8…76cb`再次重放未变。
- 三次实验总判定（2026-09-19）：+10（人工终审真值）、+10（预标注真值）、+30（预标注真值扩量）三次单变量实验在clean98上均无可判别改善——预标注真值供给路线在58图评估规模下已三次证伪。后续真实素材入库恢复低成本过滤继续累积（已至94图账），但不再单独作为训练变量消耗实验位；若继续扩量应一次性到clean148账（≥148图）后再预注册下一次短训判杂散率轴。权重不部署不导出，产品HOLD不变。

## 下一步计划

1. 只使用报告`6329d859…db6d`绑定的残项工作区修正21个`pending_original_resolution_manual_repair`实例；逐枚同时检查原始像素裁块、旧边界红线、SAM候选绿线和整张源图上下文，导出裁决必须绑定冻结SAM视觉报告`2f918ecf…4c39`与基础工作区`54eceb5f…3b28`。21/21视觉通过后，与36个冻结SAM候选和221个保留实例合并，并整体排除4张源图/25实例，重建预计278实例/18来源组的ROI验证真值v2。随后复验polygon合法、同图零交叠、sourceGroup与train零交叠、文件树哈希及278/278原分辨率边界；全部通过后才允许预注册第三轮单变量训练。旧val/test/holdout及其派生物继续禁止训练或调参，产品保持HOLD。
