# 训练提速措施与实测指标（v1）

- 日期：2026-09-10
- 范围：**只诊断、不改配置**。未改动任何预注册计划 JSON、训练合同、源码、权重或数据角色。
- 测量对象：`nail-texture-development-cycle-012-2026-09-10-v1`（cycle012）训练合同与其真实数据集。
- 原始详细报告（方法学、逐项推导、证据事件全记录）：
  `E:/AI Project/Codex/JiaRu_image/分析输出/development-training-throughput-2026-09-10/training-throughput-diagnosis-v1.md` / `-v1.json`（`df5ea969…3ec8`）
- 相关白皮书章节：§12.25（训练吞吐诊断）；变更记录 v1.1.633 / v1.1.634。

---

## 1. 措施总表（按可采纳优先级排序）

| 排名 | 措施 | 性质 | 实测/预期收益 | 是否可直接采纳 |
| --- | --- | --- | --- | --- |
| 1 | `workers: 0 → 8` | **纯吞吐，零语义代价** | 每 epoch 省 **8.57 s**；稳态 34.41→约 **25.8 s**；cycle012 12 轮 490.8→约 **387.9 s**（**约 22%**） | 需预注册 + 同数据同种子对照证明"指标中性"后才可写入 |
| 2 | `cache: 'ram'` | 纯吞吐 | 在 `workers=8` 之上仅再省约 **1.4 s**，且增加内存压力 | 可选叠加，收益有限 |
| 3 | `batch: 4 → 16` | **改变语义** | 可压缩按迭代摊薄的约 9.4 s 训练器开销 | ❌ 必须作为新假设预注册，不得当纯提速项 |
| 4 | `imgsz` / `epochs` / 逐轮绘图开关 | **改变语义或证据集** | 改变运行分辨率、训练预算或证据产物 | ❌ 同上，须各自预注册 |

**核心结论：唯一"零语义代价"的提速杠杆是 `workers: 0 → 8`。** 其余要么收益极小，要么改变训练语义。

> ⚠️ **`workers` 位于冻结合同 `fixedTrainingContract.workers = 0` 内**，且改变它会**切换数据增强的 RNG 流**（`fliplr` 等随机项命中的图片不同），因此模型数值结果不会逐位相同。其"指标中性"**必须实测验证**（同数据、同种子的对照运行，配合 Wilson 判定分辨率），不能假定。当前 58 张正图规模下噪声远大于此类扰动，但仍需证据而非推断。

---

## 2. 基线实测指标

### 2.1 参考运行（cycle012 实际训练）

| 项目 | 数值 |
| --- | --- |
| 轮数 | 12 epoch |
| 总时长 | **490.815 s** |
| 首轮 | 67.3587 s（含一次性建立） |
| 稳态每轮 | **38.5 s** |
| 合同 | `workers=0`、`batch=4`、`imgsz=512`、`mosaic=0`、`close_mosaic=0`、`mask_ratio=1`、`overlap_mask=false`、`distillation=true`、`optimizer=AdamW`、`lr0=0.0002` |
| 来源 | cycle012 `results.csv` 逐 epoch 差分 |

### 2.2 每 epoch 时间构成（真实 Ultralytics 训练器实测）

测量方式：真实训练器、真实数据集、**隔离副本**，`workers=0`、无蒸馏、其余合同一致，跑 2 epoch，由回调分别计时训练段与验证段。

| 阶段 | 首轮 | 稳态（epoch 2） |
| --- | --- | --- |
| 训练段 | 38.19 s | **26.06 s** |
| 验证段 | 13.41 s | **8.35 s** |
| 合计 | 51.60 s | **34.41 s** |

稳态训练段（26.06 s）再分解：

| 组成 | 秒 | 说明 |
| --- | --- | --- |
| 数据加载（`workers=0`，串行阻塞） | **8.16** | 实测，非估算 |
| 学生 前向+损失+反向 | **7.80** | 88 次 × 88.6 ms；纯前向仅 8.2 ms |
| 教师 前向（蒸馏） | 0.70 | 88 次 × 8.0 ms |
| 优化器/EMA/梯度累积/日志/Windows 同步等未细分 | 9.39 | 由前三项相减得到 |

**蒸馏总代价 ≈ 4.1 s/epoch（约 11%）**：以 cycle012 含蒸馏实测 38.5 s/epoch 与无蒸馏稳态 34.41 s 之差计。

### 2.3 数据加载实测（真实 350 train / 106 val，batch=4）

| workers | 训练段 | 验证段 | 每张图（训练） |
| --- | --- | --- | --- |
| 0 | **8.16 s** | **2.19 s** | 23.32 ms |
| 4 | 2.06 s | 0.49 s | 5.87 ms |
| 8 | **1.43 s** | **0.35 s** | **4.09 ms** |

`workers=0` 时数据加载在**主进程内串行执行并阻塞 GPU**；`workers>0` 时由后台进程预取并与 GPU 计算重叠。这是 `workers: 0→8` 能拿到 8.57 s/epoch 的机制来源（约 5.7 倍加速）。

### 2.4 GPU 侧实测（batch=4，512²）

| 项目 | 每批 |
| --- | --- |
| 学生纯前向 | 8.2 ms |
| 学生 前向+损失+反向 | **88.6 ms** |
| 教师纯前向 | 8.0 ms |

训练步成本约为纯前向的 **10.8 倍**，说明瓶颈在**损失与正样本分配器的延迟**，而非卷积算力。

### 2.5 验证段的真实成本

单独测量 106 张 val：

| 配置 | 墙钟 |
| --- | --- |
| `plots=false` | **14.00 s** |
| `plots=true` | **17.67 s** |

Ultralytics 自报的逐图 `speed` 合计仅约 **0.49 s/epoch**（预处理 0.492 + 推理 3.009 + 损失 0.001 + 后处理 1.089 ms/图 × 106）。
⇒ **验证成本约 94% 来自指标累积、绘图与验证器建立，而不是推理。** 训练内验证因复用验证器而降为 8.35 s。

---

## 3. 采纳前置条件（写入任何新预注册计划前必须满足）

1. **预注册**：`workers: 0→8`（及可选 `cache='ram'`）只能作为**工程参数**写入新计划，且必须显式声明它位于原冻结合同内、属于**受控变更**，不得静默就地改写既有计划。
2. **指标中性对照**：必须跑**同数据、同种子**的 A/B 对照，证明该改动只影响吞吐、不改变判定结论。
3. **判定分辨率自觉**：任何相对门结论必须同时给出 **Wilson 95% 区间**与同分母最小可判别差。当前 58 张正图规模下，"少漏 1—2 张图"的相对信号落在噪声内（13/58 的 95% 区间 `[0.13594,0.34661]` 覆盖 10/58），相对门不得单独作为分支保留/关闭依据。
4. **隔离与守卫**：所有吞吐基准必须在**隔离数据集副本**上运行，并预装训练器等价的只读图片守卫。
5. **禁止顺手改动**：`batch`/`imgsz`/`epochs`/逐轮绘图开关不得作为纯提速项混入工程变更。

---

## 4. 复现路径

脚本与原始结果留存于 `C:/Users/YaoYinyu/.workbuddy/tmp/`：

| 脚本 | 产出 | 内容 |
| --- | --- | --- |
| `bench-real-dataloader.py` | `bench-real-dataloader-result.json` | 数据加载各 workers 档位 + GPU 训练步/前向 |
| `bench-trainer-epoch.py --workers 0 --epochs 2` | `bench-trainer-epoch-result.json` | 真实训练器每 epoch 训练/验证分段 |
| `bench-val-only.py` | `bench-val-only-result.json` | 验证段单独计时，`plots` 开关对比 |
| `restore-cycle012-dataset.py` | — | 本次证据恢复脚本（逐文件哈希校验后才写入） |

运行环境：Intel i9-14900HX（32 逻辑核）/ 31.63 GB RAM / RTX 4060 Laptop 8 GB / torch 2.12.1+cu126 / ultralytics 8.4.92；
训练解释器 `C:\Users\YaoYinyu\AppData\Local\Programs\Python\Python313\python.exe`（本机唯一装有 torch+ultralytics+CUDA 者）。

---

## 5. 边界与未做事项

- 未训练新权重、未推理、未消费受保护评估集（旧 val30 / test100 / 发布留出）。
- 未改动循环013预注册计划与训练启动门；未新增候选 / ONNX / `releaseIdentity`。
- candidate57 固定 **TEST HOLD**，产品继续 **HOLD**。
- 本轮另记录并修复一次**证据完整性事件**（10 张缺 EOI 的 JPEG 被 Ultralytics `check_image` 原地重存），已按 SHA-256 逐文件恢复并重放 `verified`；详见原始报告第 8 节与白皮书 §12.25。

---

## 6. 交叉引用

- 详细诊断报告：`E:/AI Project/Codex/JiaRu_image/分析输出/development-training-throughput-2026-09-10/training-throughput-diagnosis-v1.md`
- 白皮书：`docs/technical-whitepaper.md` §12.25、§13 v1.1.633 / v1.1.634
- 目标文件：`docs/nail-texture-current-goal.md`（当前状态第 53—55 条、下一步计划第 4 条）
- 持久规则：`AGENTS.md`（只读图片守卫、Ultralytics 手工基准三坑）

> 本文档由 **WorkBuddy** 编写（2026-09-10）。

---

## 7. 从训练吞吐转为发布关键路径（2026-09-13）

循环013已由零推理候选天花板证明不可达并以零训练、零推理预算关闭。循环014容量审计进一步确认，现有58张clean开发正图扩到352张需新增294张；加上必须来源隔离的全新calibration30与positive holdout100，最低新增正图需求为424张。当前已知候选清单最多10图/8来源组，且均为`trainingUse=prohibited`、完整mask审核未开始，最乐观仍缺414张；Beta100与新增训练正图不在这个最低数内。因此发布关键路径的加速顺序调整为：候选供给零推理可达性、正图采集与审核并行化、一次短程单变量训练、配方冻结后的发布证据流水线；`workers=8`继续作为训练阶段约22%的受控吞吐优化。容量报告`positive-capacity-audit-v1.json` SHA-256为`a4d0f14d…240d`，只读取元数据并完成深重放，未读图片像素、未训练、未推理或消费受保护数据。 **〔本段由 Codex 新增〕**

可执行提速措施按关键路径排序：

1. 在训练前用理论上限关闭不可达方案，避免浪费唯一训练和评估预算；
2. 将源图筛选、逐甲mask审核、合法性/零交叠复核和角色冻结拆成来源组原子分片并行推进，但每张图仍必须完成原分辨率终审；
3. 新训练计划固定`workers=8`，仅在隔离副本上验证吞吐和指标中性，预期训练墙钟缩短约22%；
4. 候选配方锁定前并行准备calibration、正负发布留出、浏览器/真机/Beta模板和自动审计入口，数据保持密封；
5. 任何一次短程实验同时输出质量、吞吐、候选数和浏览器预算结论，失败立即关闭，不做同输入重训或阈值扫描。 **〔本段由 Codex 新增〕**

## 8. 循环014固定多裁片路线实测（2026-09-13）

零推理几何分析显示，四个0.70方形裁片可覆盖clean98基线21/21个漏检真值；五次推理按历史桌面P95线性投影为668.5 ms，但最坏投影797.5 ms距800 ms门仅2.5 ms，尚未包含裁剪、坐标映射与去重开销。路线报告v2 SHA-256为`a82725c4…eec6`，因此只允许一次零训练开发试验，不允许启动训练或正式晋升。 **〔本段由 Codex 新增〕**

预注册计划`ab3c9b22…8d79`复用冻结全图预测，只在clean98隔离副本上执行392个裁片评估。深重放报告`be0a9884…a673`显示：漏检实例21→18，但漏检图片仍13；完整mask 297→302；重复5→10、假阳性10→28，加权杂散率`0.0960452`→`0.20762712`，直接可提取仍29/58。四项开发门只有召回、完整率通过，漏甲图片率和杂散率失败，`allRequired=false`；固定多裁片路线按合同关闭，不再投入浏览器实现或训练。 **〔本段由 Codex 新增〕**

该实测说明“更多候选”会用3枚召回收益换来18个新增假阳性，不能缩短发布工期。`workers=8`仍可缩短单次12轮训练约1.72分钟，但414张剩余正图即使按100张/日审核也需约4.14日，按25张/日则约16.56日；因此唯一已被证据支持的日历工期加速是把来源隔离采集、源图筛选、mask标注和原分辨率复核并行化。MediaPipe逐指ROI若继续研究，必须作为独立分支先证明关键点覆盖与失败转人工，不能用已证伪的固定网格兜底。 **〔本段由 Codex 新增〕**

## 9. 循环015分阶段数据门（2026-09-14纠错）

当前v2角色重隔离审计`source-supply-audit-v2.json`（`de954b63…1c00`）证明，旧333图库存只有7图/7来源组仍无cycle012 train、旧开发val66、clean98、历史val30或已消费test100交叠。v4源图门只留下`01063`，但止损审计`repair-stoploss-audit-v1.json`（`5b79eee9…60b2`）进一步确认：其第15枚必需甲面只剩被相邻手遮挡的狭窄甲尖，普通SAM三版本实为同一字节快照，连同ranked与后续18候选共三类独立方案均返修失败。该图排除后旧库存可返修容量为0，首批50张必须全部新采；避免继续人工重建这15个不成立的真值，是本轮实际节省的返工。 **〔本段由 Codex 更新〕**

质量迭代采用两个有统计意义的触发点：先把clean开发正图从58补到148，补足90张批准正图后才运行一次预注册单变量短训，以判定杂散率轴；随后继续补到352图，再判定绝对漏甲率。未达到148图前按1、10或20张小批次反复训练无法给出可判别结论，只会增加训练、评估和路线切换开销。采集侧采用三条来源组隔离流水线：开发扩容、全新calibration30、全新positive holdout100；可以并行收集和审核，但角色在标注或模型辅助前冻结，禁止互换。当前总最低新批准正图缺口424图，Beta100与新增训练正图另计。 **〔本段由 Codex 更新〕**

## 10. 循环015供给规模与工期路线验证（2026-09-14）

机器审计`acceleration-route-audit-v1.json`（`c142416b…9445`）绑定历史1166张源图审核、160张精确授权候选、120张/636 mask最终真值、循环014容量和当前止损报告。历史源图通过率为`565/1166=0.48456261`，已通过源图门候选的最终真值批准率为`120/160=0.75`，后者Wilson 95%区间为`[0.67757630, 0.81070060]`。因此50张源图合格候选点估计只有37.5张批准图、按下界只有33.88张，无法补足clean 58→148所需90张。路线已改为至少133张源图合格候选；若从未经筛选原图开始，点估计需248张，叠加两个漏斗的Wilson下界后保守容量包络为292张。 **〔本段由 Codex 新增〕**

这项调整可真实减少两类无效工期：停止对旧库存失败图继续返修，并取消90张批准图之前的零碎训练。来源筛选、标注和原分辨率终审按冻结来源组组成流水线，前一组通过源图门即可进入下一工序，不必等全部原图收齐。`workers=8`仍只提供约22.3%的训练墙钟收益，且必须做同数据同种子指标中性对照。历史比例是容量规划证据，不保证新批次批准率；人工审核并行后的日历提速尚无实测，因此不得写成已实现的百分比。 **〔本段由 Codex 新增〕**

## 11. 循环015首个生成供给批次（2026-09-14）

在本地旧库存归零后，使用OpenAI内置图像生成建立3张互相独立的正图候选：单手裸粉透明低对比长甲、双手中深肤色深酒红短甲、单手斜侧视透明/冰蓝长甲。三图均复制到项目素材区并在任何标注或模型辅助前冻结文件名、图片SHA-256和独立`sourceGroup`；精确模型ID不可核验，未宣称具体生成模型。原分辨率逐图复核为3张通过、0排除，共20枚完整可见甲面，无甲面触边、遮挡、低清、文字或水印。 **〔本段由 Codex 新增〕**

审计`generated-source-intake-audit-v1.json`（`f1e2c91e…5769`）绑定声明`f6163e8b…0fe8`、当前角色供给审计和133张路线审计，深重放确认3图/3来源组与development train、旧开发val、clean98、历史val30和已消费test100在文件名、图片哈希与来源组上均零交叠。三图只进入完整mask标注候选，继续`trainingUse=prohibited`；当前源图合格进度3/133，下一滚动批量为10张、目标13/133。该小批量验证了新采集到冻结的入口，但尚未产生训练真值或质量提升指标。 **〔本段由 Codex 新增〕**

## 12. 循环015累计生成供给扩至7张（2026-09-14）

继续执行10张滚动批次时，前4个独立运行形成深肤色低对比短甲、暗背景深色长甲、双手十指多色短甲和强斜视金属长尖甲，原分辨率审核4/4通过、共25枚完整甲面。v2声明`26e11425…5668`逐字段保留前3项并追加4项；审计`43c321f1…a9cf`深重放确认累计7图/7来源组/45甲与五类既有角色在文件名、图片哈希和来源组上零交叠，旧v1报告也继续可重放。全部候选仍禁止训练，尚未产生完整mask或模型质量提升。 **〔本段由 Codex 新增〕**

本轮第5个追加请求被内置服务以`usage_limit_reached`拒绝，因此检查点如实停在7/133，额度恢复后补6张至13/133。该限制只延后数据入口，不改变133张源图合格目标、90张批准图训练触发点或任何发布门；训练墙钟优化仍不是当前关键路径。 **〔本段由 Codex 新增〕**

## 13. 循环015首个13张滚动检查点（2026-09-14）

额度恢复后以6个独立运行补齐13张检查点，新增透明低对比、深肤色双手、平面花卉、极短自然甲、黑白几何双手和银色镜面斜视场景。原分辨率审核6/6通过、40枚完整甲面；累计13图/13来源组/85甲。v3声明`2069b804…f0e6`绑定v2前序冻结，审计`2db7e07d…be6a`深重放确认与五类既有角色三重零交叠，v1/v2旧报告保持可重放。 **〔本段由 Codex 新增〕**

达到13张后，关键路径从单纯供给切换为来源组流水：13张立即进入完整mask候选和逐甲原分辨率终审，源图供给同时滚动至23/133。这样缩短源筛完成到真值批准之间的空档，但不允许源图门替代mask门；累计90张批准图之前仍不启动零碎训练。 **〔本段由 Codex 新增〕**

## 14. 循环015标注流水线首轮实测（2026-09-14）

首轮实测证明，训练质量提升的当前加速点是减少人工标注返工。解剖复核先剔除六指异常图并补入合格替换图，避免在错误源图上制作和审核6个无效mask；复制隔离工作区同时完成规范真值查重、来源组分片和角色隔离，避免后续再返工数据身份。循环012权重一次生成76/85个首轮候选，按图计数把人工优先级收敛到4张返修图，其余9张直接进入边界终审，但计数相等仍不替代逐甲审核。 **〔本段由 Codex 新增〕**

两轮通用SAM各产出40个候选，几何通过数从30降到28，原分辨率仍暴露皮肤、整指和多色图案碎片。该结果证伪“继续调通用框/角点提示即可快速批准”的路线；后续只对已定位的问题甲执行一次覆盖全部颜色区的多正点与邻肤负点，仍失败就立刻人工polygon。这样把自动化用于容易样本，把人工时间集中在少数困难边界，且不通过降低完整甲面门来制造速度。 **〔本段由 Codex 新增〕**

当前实际瓶颈为4张返修图的完整边界与85甲逐甲2倍终审，训练墙钟仍不在关键路径。`workers=8`约22%的收益保留为达到90张新批准真值后的训练前置对照；在此之前反复训练或继续通用SAM扫描都会增加工期而不会产生可发布质量证据。 **〔本段由 Codex 新增〕**

## 15. PCA长轴多正点SAM的实际提速验证（2026-09-14）

首批13图完成后，机器审计`annotation-acceleration-audit-v1.json`（SHA-256 `cda87495…5900`）深重放通过。对9张“完整甲数与粗候选数一致”的图片，逐甲2倍审核确认原YOLO边界0/45可接受；用粗polygon的PCA长轴生成质心及长轴两端三个内部正点，配合扩展框与角点负提示后，SAM2.1 Large一次运行产出45/45候选、0 fallback、0错误，几何45/45通过，原分辨率视觉44/45可接受。仅1枚包含近甲根指腹的候选转人工polygon，人工边界干预相对45枚全量返修减少44次，即`97.78%`。

该路线因此作为下一批计数正确候选的默认标注路径：粗模型负责候选数与大致定位，PCA多点SAM负责完整边界，人工只处理原分辨率复核失败的残差。计数不足、过多、复杂双手或边界仍污染的图片继续使用混合返修，不把97.78%外推为所有场景收益。每枚甲仍需原分辨率视觉、polygon合法性和同图零交叠门；几何通过不自动批准真值。

整批最终结果为13图/85 mask、几何85/85、同图交叠0、终审13通过/0返修/0排除。冻结角色是`development-evaluation-extension`，正确唯一索引`d60d72a3…ab64`保持`trainingUse=prohibited`；一次错误train角色终结已由`67332145…e0dc`显式作废并保留历史。当前可计入clean扩展的批准进度为13/90，下一端到端里程碑为23/90；达到90前仍不启动零碎训练。
